# 📐 وثيقة التصميم التقني — نظام الجدولة والأحداث
## Academic Hub — Scheduling & Events System (TDD)

| البند | التفاصيل |
|---|---|
| **النظام** | نظام الجدولة والأحداث (Scheduling & Events) |
| **المنتج** | Academic Hub — MVP v3.5 |
| **إصدار الوثيقة** | 1.1 — مواءمة مع MVP v3.5 (قرار #19: الأحداث الثقافية) + توحيد افتراض القاعات/المعامل (A-04) |
| **مستوى العمق المعتمد** | 🟡 Production — لم يحدد المستخدم المستوى، فافترضنا Production لأن النظام هو العمود الفقري الذي تعتمد عليه أنظمة الحضور والامتحانات والقاعات (المرحلة 2 في خارطة الاعتماديات) |
| **الوثائق المرجعية** | MVP Functions v3.5 (الأقسام 3.1، 4.1، 5.1، 5.2) · قالب TDD v1.0 |

---

## القسم 0: بروتوكول التحليل المتسلسل 🧠

### 0.1 — إعادة صياغة المشكلة (Problem Restatement)

المطلوب تصميم نظام جدولة مركزي يعرض للطالب جدولاً موحّداً يضم **7 أنواع أحداث متمايزة** (محاضرة، معمل، سمنار، امتحان، بحث، واجب، حدث ثقافي)، لكل نوع بطاقة عرض ووظائف مختلفة. الأحداث تدخل النظام عبر **أربعة مسارات إنشاء مختلفة**: (أ) اقتراح من المحاضر يخضع لموافقة إدارية إذا تطلّب مورداً فيزيائياً، (ب) نشر مباشر من المحاضر للواجبات والبحوث، (ج) جدولة حصرية من الإداري للامتحانات، (د) نشر مباشر من الإداري للأحداث الثقافية/الترفيهية (قرار #19). يجب أن يعمل الجدول بالكامل دون اتصال بعد أول مزامنة، وأن يُصدر إشعارات استباقية وفورية عند التغييرات.

**غموض مكتشف:** وثيقة MVP لا تحدد هل الأحداث المتكررة (محاضرة أسبوعية طوال الفصل) تُنشأ كسلسلة مرة واحدة أم كل حدث على حدة — حُسم في الافتراض A-03 والقرار [D-06]. **غموض ثانٍ اكتُشف أثناء إعداد هذه الوثيقة:** جهة إنشاء الحدث الثقافي لم تكن محددة في MVP v3.4 — حُسم بالقرار #19 (الإداري حصراً) ووُثّق في MVP v3.5.

### 0.2 — الأسئلة الخمسة الحاسمة

1. **من المستخدم الفعلي؟**
   ثلاثة أدوار: طلاب (~5,000 — الافتراض A-01) يقرأون جداولهم عدة مرات يومياً بذروة بين 7:30–9:00 صباحاً، محاضرون (~200) ينشئون أحداثاً بمعدل 2–5 أسبوعياً لكل محاضر، وإداريون (~20) يراجعون الطلبات ويجدولون الامتحانات (ذروة موسمية قبل فترة الامتحانات) وينشئون الأحداث الثقافية.

2. **ما العملية الأثقل؟**
   **Read-heavy بامتياز** — بنسبة تقريبية 97:3 (قراءة:كتابة). قراءة الجدول تحدث آلاف المرات يومياً؛ الكتابة (إنشاء/تعديل/موافقة) بالعشرات. هذا يوجّه التصميم نحو: فهرسة قوية للقراءة، كاش محلي (Hive)، ومزامنة تفاضلية (delta sync) بدل إعادة جلب كامل.

3. **ماذا لو توقف النظام ساعة؟**
   الطالب يستمر بالعمل من الكاش المحلي (متطلب صريح في MVP 3.1.2) — لذا القراءة تتحمل توقفاً. لكن **الكتابة الحرجة** (فتح جلسة حضور مرتبطة بحدث، دخول امتحان ضمن نافذة زمنية) لا تتحمل توقفاً وقت الامتحانات. الهدف: توافرية 99.5% شهرياً (≈ 3.6 ساعات توقف مسموح)، مع منع أي صيانة مجدولة خلال فترات الامتحانات.

4. **ما البيانات الأكثر حساسية؟**
   ليست بيانات الأحداث ذاتها (شبه عامة داخل الجامعة)، بل **صحة نطاق الرؤية**: طالب يرى أحداث مواد غير مسجّل فيها = تسريب (خصوصاً مواعيد امتحانات لم تُنشر لدفعته)، ومحاضر يعدّل حدث مادة ليست له = عبث بسجلات رسمية. الدفاع الأساسي: RLS على مستوى الصف.

5. **أضيق عنق زجاجة متوقع خلال أول 6 أشهر؟**
   استعلام «جدول الطالب الأسبوعي» (join عبر enrollments → events) عند ذروة الصباح: ~5,000 طالب × 3 فتحات للتطبيق = 15,000 استعلام في نافذة 90 دقيقة ≈ 2.8 استعلام/ثانية متوسطاً مع رشقات (bursts) تصل 30/ثانية. قابل للاحتواء بفهرس مركّب + delta sync يجعل معظم الفتحات لا تجلب شيئاً (304-like). العنق الثاني: رشقة إشعارات FCM عند تعديل حدث لمادة كبيرة (500 طالب دفعة واحدة) — تُعالج بمعالجة غير متزامنة (queue).

### 0.3 — الافتراضات المعلنة (Explicit Assumptions)

| # | الافتراض | مستوى الثقة | تأثيره لو كان خاطئاً |
|---|---|---|---|
| A-01 | حجم المستخدمين: ~5,000 طالب، ~200 محاضر، ~20 إداري، ~300 مادة نشطة بالفصل | متوسط | لو 10x: نحتاج read replicas مبكراً + تقسيم رشقات الإشعارات؛ البنية تتحمل حتى ~50K طالب قبل تغيير معماري |
| A-02 | المنصة: تطبيق Flutter موبايل + Supabase (PostgreSQL + PostgREST + Edge Functions + Realtime) — مستنتج من ذكر MVP الصريح لـ Hive وRLS | عالي | لو backend مخصص: تنتقل عقود API كما هي، وتُعاد كتابة RLS كـ middleware — تصميم الـ Schema لا يتغير |
| A-03 | لا أحداث متكررة (recurring) في MVP — كل تكرار أسبوعي يُنشأ كصفوف مستقلة عبر أداة «تكرار حتى تاريخ» في واجهة الإنشاء (توليد client-side ثم إدراج دفعة) | متوسط | لو طُلب RRULE حقيقي: تعقيد كبير (استثناءات، تعديل سلسلة كاملة) — مسجّل ديناً تقنياً في 2.3 |
| A-04 | نظام إدارة القاعات (5.1) يملك جدول `rooms` موحّداً يغطي القاعات والمعامل معاً (عبر حقل `kind: ROOM / LAB`)؛ نظامنا يستهلكه بمفتاح خارجي واحد (`room_id`) فقط ولا يديره | عالي | لو لم يُبنَ بعد: ننشئ جدول rooms مبسّطاً هنا ويُسلَّم لاحقاً لنظام القاعات؛ ولو فُصل المعمل بجدول مستقل: نضيف FK ثانياً وقيد EXCLUDE مماثلاً عليه |
| A-05 | جدولي `courses` و`enrollments` (بحالة ACTIVE) موجودان من نظام المصادقة والتسجيل (المرحلة 1 في خارطة البناء) | عالي | النظام لا يعمل بدونهما — اعتمادية صلبة يجب اكتمالها أولاً |
| A-06 | التوقيت: منطقة زمنية واحدة للجامعة، لكن التخزين UTC دائماً والعرض بتوقيت الجهاز | عالي | لو فروع بمناطق مختلفة: التخزين UTC يحمينا أصلاً — تغيير طفيف بالعرض فقط |
| A-07 | الإشعارات تُسلَّم عبر FCM؛ متطلب التسليم: ≥ 95% خلال 60 ثانية (الإشعار الاستباقي «قبل 30 دقيقة» يُجدول محلياً على الجهاز من بيانات الكاش، فلا يعتمد على الشبكة) | عالي | لو FCM محجوب: الإشعارات المحلية المجدولة تغطي الاستباقي؛ التغييرات تصل عند أول مزامنة |

### 0.4 — نطاق العمل (Scope Fence)

**✅ داخل النطاق:**
- نمذجة وتخزين الأحداث السبعة بكامل حقولها المتمايزة (بطاقات القسم 3.1.1 من MVP)
- دورة حياة الحدث: مسودة ← قيد المراجعة ← منشور / مرفوض ← معدَّل / ملغى
- سير الموافقات: اقتراح المحاضر (محاضرة/معمل/سمنار) ← مراجعة الإداري
- النشر المباشر للواجبات والبحوث (نوع التسليم + مكان التسليم الورقي)
- النشر المباشر للأحداث الثقافية/الترفيهية من الإداري (قرار #19)
- الجدولة الحصرية للامتحانات من الإداري (بما فيها الإدراج الدفعي Top-Down)
- كشف تعارضات القاعة/الوقت وقت الحجز والموافقة
- استعلام جدول الطالب المُفلتر (أسبوعي/يومي) + المزامنة التفاضلية للأوفلاين
- إطلاق أحداث الإشعارات (event published/updated/cancelled) وتسليمها لنظام الإشعارات
- ملاحظة المحاضر على الحدث (أونلاين / مؤجلة)

**❌ خارج النطاق (منع scope creep):**
- آلية تسجيل الحضور نفسها (BLE/QR) — وثيقة Attendance TDD المستقلة؛ نوفّر فقط `event_id` وحقل `requires_attendance`
- محتوى الامتحان وبنك الأسئلة والمراقبة الصوتية (نظام الامتحانات) — نوفّر فقط الحدث ونافذته الزمنية
- رفع ملفات تسليم البحوث/الواجبات وتصحيحها (مستودع الملفات + نظام أعمال المحاضر) — نوفّر فقط بيانات الموعد النهائي ونوع التسليم
- إدارة بيانات القاعات ذاتها (سعة، موقع) — نظام إدارة القاعات
- بنية تسليم الإشعارات (tokens، قنوات) — نظام الإشعارات؛ نحن نُنتج الأحداث فقط
- الأحداث المتكررة بمحرك RRULE (انظر A-03 والدين التقني)

---

## القسم 1: الملخص التنفيذي وسياق المشكلة 📋

### 1.1 — بيان المشكلة

اليوم الأكاديمي للطالب مبعثر بين إعلانات ورقية ومجموعات واتساب وجداول Excel متغيرة، فيفوّت الطلاب محاضرات معدَّلة وامتحانات منقولة القاعة، ويستهلك المحاضرون والإداريون ساعات أسبوعياً بالتنسيق اليدوي لحجز القاعات وحلّ تعارضاتها. تكلفة عدم الحل: غيابات ظالمة تُحتسب على الطلاب، تعارضات قاعات تُكتشف لحظة وقوعها، وانعدام مصدر حقيقة واحد (Single Source of Truth) تعتمد عليه أنظمة الحضور والامتحانات والنتائج.

### 1.2 — الهدف القابل للقياس

تمكين 5,000 طالب من رؤية جدولهم الشخصي المُحدَّث (بأنواعه السبعة) خلال أقل من ثانيتين من فتح التطبيق — متصلاً أو غير متصل — مع وصول إشعار أي تغيير إلى ≥ 95% من الطلاب المعنيين خلال 60 ثانية، وتصفير تعارضات القاعات المنشورة (0 تعارض في الأحداث بحالة PUBLISHED).

### 1.3 — معايير النجاح (Success Criteria)

| معيار | القيمة المستهدفة | كيف يُقاس |
|---|---|---|
| زمن استجابة استعلام الجدول p95 | < 300ms (server-side) | Supabase logs / APM |
| زمن فتح الجدول من الكاش (أوفلاين) | < 500ms | تتبّع أداء داخل التطبيق |
| تسليم إشعار التغيير | ≥ 95% خلال 60 ثانية | سجل تسليم FCM |
| تعارضات قاعات في أحداث منشورة | 0 (يمنعها قيد قاعدة البيانات) | استعلام تدقيق يومي |
| نجاح المزامنة التفاضلية | ≥ 99% من الجلسات | تقارير أخطاء التطبيق |
| حجم حمولة المزامنة عند عدم وجود تغييرات | < 1KB | قياس شبكة |

### 1.4 — ما ليس هذا النظام (Anti-Goals)

- **ليس تقويماً شخصياً:** لا أحداث خاصة ينشئها الطالب لنفسه — الجدول انعكاس رسمي لمواده حصراً.
- **ليس نظام حجز ذاتي للقاعات:** المحاضر «يطلب» والإداري «يقرر» — لا حجز فوري تلقائي.
- **ليس محرك تقويم عام (Google Calendar clone):** لا دعوات، لا مشاركين اختياريين، لا RSVP، لا تكامل مع تقاويم خارجية.
- **ليس نظام الحضور ولا الامتحانات:** يوفّر لهما «العمود الفقري الزمني» فقط.

---

## القسم 2: القيود التقنية واختيار التقنيات 🔧

### 2.1 — القيود المفروضة (Hard Constraints)

| نوع القيد | التفصيل | مصدره |
|---|---|---|
| تقني | عمل كامل أوفلاين بعد أول مزامنة عبر Hive | MVP 3.1.2 (صريح) |
| تقني | فصل صارم بين الأدوار عبر RLS | MVP 1.1 — المبادئ الحاكمة |
| تنظيمي | جدولة الامتحان حصرية للإداري — يُفرض تقنياً لا واجهةً فقط | قرار معتمد #10 |
| تنظيمي | إنشاء الحدث الثقافي حصري للإداري — نشر مباشر | قرار معتمد #19 |
| تنظيمي | كل قرار موافقة/رفض يُوثَّق ولا يُحذف | MVP 5.0 |
| فريق/زمن | فريق صغير (2–3 مطورين) وMVP بمراحل — يفرض تفضيل الحلول المُدارة (Managed) على البنية الذاتية | افتراض مستنتج من طبيعة MVP |
| تقني | الاعتمادية على جداول المرحلة 1 (auth, courses, enrollments) | خارطة البناء — MVP القسم 8 |

### 2.2 — مصفوفة اختيار التقنيات

#### [D-01] طبقة: قاعدة البيانات

| الخيار | المزايا | العيوب | لماذا قُبل/رُفض |
|---|---|---|---|
| **PostgreSQL عبر Supabase ✅** | RLS أصيل (متطلب صريح)؛ `EXCLUDE` constraints لمنع تعارض القاعات على مستوى المحرك؛ Realtime مدمج؛ يخدم write throughput المطلوب (< 10 كتابة/ثانية) بهامش 100x | نقطة ربط بمزوّد (vendor lock-in جزئي)؛ RLS المعقدة قد تبطئ الاستعلامات إن أُسيء كتابتها؛ حدود اتصالات على الخطط الصغيرة (تُدار بـ pooling) | متطلبات MVP (RLS + Hive) تشير لهذا المسار أصلاً؛ الفريق الصغير يستفيد من Auth/Storage/Realtime الجاهزة |
| MySQL ذاتي الاستضافة ❌ | مألوف، رخيص | لا RLS أصيل (نحتاج طبقة تطبيق كاملة للتفويض)؛ لا EXCLUDE constraints — كشف التعارض يصبح منطق تطبيق هشّاً أمام السباقات | يفقدنا أقوى ضمانتين في التصميم (6 و7 من معايير النجاح) |
| MongoDB ❌ | مرونة schema لأنواع الأحداث المختلفة | العلاقات (enrollments→events→rooms) علائقية بطبيعتها؛ لا قيود تفرّد زمني؛ ضمانات التعارض تتطلب معاملات يدوية معقدة | المشكلة علائقية، والمرونة المطلوبة يغطيها عمود JSONB واحد |

#### [D-02] طبقة: نمذجة أنواع الأحداث السبعة

| الخيار | المزايا | العيوب | لماذا قُبل/رُفض |
|---|---|---|---|
| **جدول واحد بعمود نوع + حقول مشتركة + JSONB للحقول الخاصة (Single Table Inheritance) ✅** | استعلام «جدول الطالب» = جدول واحد بلا UNION (أهم مسار قراءة)؛ فهرسة موحدة؛ المزامنة التفاضلية أبسط (updated_at واحد) | حقول nullable حسب النوع؛ JSONB أضعف في التحقق (يُعوَّض بـ CHECK constraints لكل نوع + تحقق طبقة API) | النظام read-heavy 97:3 والقراءة دائماً «كل الأنواع معاً» — أي تقسيم يعاقب المسار الأهم |
| جدول لكل نوع (Class Table Inheritance) ❌ | تحقق صارم لكل نوع | استعلام الجدول = UNION من 7 جداول أو 7 استعلامات؛ الفهارس والـ RLS تتضاعف ×7 | يعاقب مسار القراءة الحرج لصالح نقاء نظري |
| EAV (كيان-خاصية-قيمة) ❌ | مرونة قصوى | استعلامات كارثية الأداء، تحقق شبه مستحيل | إفراط هندسي مرفوض بمبدأ «البساطة المنضبطة» |

#### [D-03] طبقة: سير الموافقات (Approval Workflow)

| الخيار | المزايا | العيوب | لماذا قُبل/رُفض |
|---|---|---|---|
| **آلة حالات (status) على جدول events نفسه + جدول تدقيق `event_reviews` للقرارات ✅** | الحدث كيان واحد طوال حياته (لا نسخ بين جداول)؛ سجل القرارات محفوظ ولا يُحذف (قيد MVP 5.0)؛ استعلام «طلباتي وحالتها» للمحاضر بسيط | صفوف PENDING/REJECTED تعيش مع المنشورة (تُعزل بفهرس جزئي وRLS) | أبسط تصميم يحقق التوثيق الدائم + تتبّع الحالة (MVP 4.1) |
| جدول طلبات منفصل يُنسخ منه إلى events عند القبول ❌ | فصل نظيف | ازدواج schema؛ خطر تباعد النسختين؛ تعديل الطلب بعد القبول يتطلب مزامنة يدوية | تعقيد بلا مقابل حقيقي في هذا الحجم |
| محرك workflow خارجي (Temporal/Camunda) ❌ | قوي للمسارات المعقدة | المسار خطوة واحدة (اقتراح←قرار)؛ بنية تشغيلية إضافية كاملة | إفراط هندسي صريح |

#### [D-04] طبقة: كشف تعارض القاعات

| الخيار | المزايا | العيوب | لماذا قُبل/رُفض |
|---|---|---|---|
| **قيد `EXCLUDE USING gist` على (room_id, tstzrange) للأحداث المنشورة + فحص استباقي بطبقة API ✅** | ضمانة نهائية على مستوى المحرك — يستحيل نشر تعارض حتى مع طلبين متزامنين (يحسم صف 1 في جدول 6.1)؛ الفحص الاستباقي يعطي رسالة ودّية قبل الاصطدام بالقيد | يتطلب امتداد `btree_gist`؛ رسالة خطأ القيد خام وتحتاج ترجمة في الـ API | الفحص التطبيقي وحده يحتوي فجوة سباق (TOCTOU)؛ القيد يغلقها نهائياً |
| فحص تطبيقي فقط (SELECT ثم INSERT) ❌ | بسيط | سباق زمني: طلبان متزامنان يمران معاً | يكسر معيار النجاح «0 تعارض» |
| قفل تشاؤمي على صف القاعة ❌ | يمنع السباق | يُسلسل كل الكتابات على القاعة؛ خطر deadlocks؛ أعقد من القيد التصريحي | القيد يعطي نفس الضمانة بلا كلفة القفل |

#### [D-05] طبقة: الأوفلاين والمزامنة

| الخيار | المزايا | العيوب | لماذا قُبل/رُفض |
|---|---|---|---|
| **كاش Hive للقراءة فقط + Delta Sync بـ `updated_at` (مع جلب soft-deleted للتنظيف) ✅** | يحقق متطلب MVP حرفياً؛ لا تعارضات مزامنة إطلاقاً لأن الطالب لا يكتب على الأحداث أوفلاين؛ حمولة شبه صفرية عند عدم التغيير | تسجيل الحضور أوفلاين خارج نطاقنا (يخص Attendance TDD)؛ يتطلب soft delete (محسوم في 4.4) | أبسط مزامنة صحيحة؛ الكتابة الوحيدة للطالب (الحضور) نظام آخر أصلاً |
| مزامنة ثنائية الاتجاه (CRDT / PowerSync) ❌ | تدعم كتابة أوفلاين | تعقيد كبير لدعم كتابة لا يحتاجها الطالب في هذا النظام | حل لمشكلة غير موجودة |

#### [D-06] طبقة: الأحداث المتكررة

| الخيار | المزايا | العيوب | لماذا قُبل/رُفض |
|---|---|---|---|
| **توليد الصفوف مسبقاً (Materialized Occurrences) — واجهة الإنشاء تولّد صفاً لكل أسبوع حتى نهاية الفصل، مربوطة بـ `series_id` اختياري ✅** | كل حدث مستقل: يُعدَّل/يُلغى/تُعلَّق عليه ملاحظة منفرداً (متطلب «مؤجلة/أونلاين» لأسبوع بعينه)؛ استعلام الجدول يبقى بسيطاً؛ الحضور يرتبط بصف ملموس | فصل من 15 أسبوعاً × 300 مادة × ~3 أحداث ≈ 13,500 صف/فصل — حجم زهيد؛ تعديل «كل السلسلة» = UPDATE على series_id | RRULE يعقّد كل استعلام واستثناء؛ الحجم الفعلي لا يبرره |
| RRULE (معيار iCalendar) ❌ | تخزين مضغوط | فك التكرار عند كل قراءة؛ الاستثناءات (EXDATE) معقدة؛ ربط الحضور بـ «occurrence افتراضي» هشّ | مسجّل في الديون التقنية إن كبر المنتج |

#### [D-07] طبقة: إيصال التغييرات والإشعارات

| الخيار | المزايا | العيوب | لماذا قُبل/رُفض |
|---|---|---|---|
| **جدول Outbox (`event_outbox`) يملؤه Trigger عند النشر/التعديل/الإلغاء + Edge Function مجدولة كل 30 ثانية تدفع لنظام الإشعارات (FCM) ✅** | نمط Transactional Outbox: التغيير وسجلّ إشعاره في معاملة واحدة — لا إشعار مفقود؛ إعادة المحاولة آمنة (at-least-once + إزالة تكرار بمفتاح الحدث في العميل — يحسم صف «رسالة مكررة» في 6.1) | تأخير حتى 30 ثانية (ضمن هدف الـ 60 ثانية)؛ جدول إضافي يحتاج تنظيفاً دورياً | أبسط ضمان تسليم موثوق بلا بنية رسائل خارجية |
| Webhook مباشر من داخل معاملة الكتابة ❌ | فوري | فشل FCM يُفشل الكتابة أو يضيع الإشعار (اختيار أحلاهما مرّ) | يكسر الموثوقية |
| Kafka/RabbitMQ ❌ | تسليم صناعي المستوى | بنية تشغيلية كاملة لفريق من 2–3 لعشرات الرسائل بالدقيقة | إفراط هندسي |

### 2.3 — الديون التقنية المقبولة عمداً

| الدين | لماذا نقبله الآن | متى يُسدَّد |
|---|---|---|
| لا محرك RRULE — توليد صفوف مسبق [D-06] | الحجم زهيد والاستقلالية مطلوبة | إذا تجاوزت الأحداث ~200K صف/فصل أو طُلبت جداول متغيرة ديناميكياً |
| polling كل 30 ثانية للـ Outbox بدل دفع فوري | ضمن هدف الـ 60 ثانية | إذا شُدّد الهدف إلى < 10 ثوانٍ: التحويل لـ pg_notify / Realtime trigger |
| لا نسخة كاملة من history التعديلات على الحدث (نحفظ آخر حالة + سجل المراجعات فقط) | يكفي متطلبات MVP التوثيقية | عند طلب تدقيق كامل: جدول `event_versions` بنمط append-only |
| الفحص الاستباقي للتعارض في API يكرّر منطق القيد | تجربة مستخدم أفضل، والقيد هو الضامن | لا يُسدَّد — ازدواج مقصود (defense in depth) |

---

## القسم 3: معمارية النظام والمخططات 🏗️

### 3.1 — المخطط العام (System Context)

\`\`\`mermaid
graph TB
    subgraph Clients["تطبيق Flutter"]
        ST[👤 الطالب<br/>قراءة الجدول + كاش Hive]
        LC[👤 المحاضر<br/>اقتراح/نشر أحداث]
        AD[👤 الإداري<br/>مراجعة + جدولة امتحانات + أحداث ثقافية]
    end

    subgraph Supabase["Supabase (Backend)"]
        AUTH[Auth + JWT<br/>الدور داخل الـ claims]
        API[PostgREST + Edge Functions<br/>عقود API + فحص استباقي للتعارض]
        DB[(PostgreSQL<br/>events + RLS + EXCLUDE constraint)]
        OUTBOX[Edge Function مجدولة<br/>قارئ الـ Outbox]
    end

    NOTIF[نظام الإشعارات<br/>FCM — نظام خارجي عن نطاقنا]
    ATT[نظام الحضور<br/>يستهلك event_id]
    ROOMS[نظام إدارة القاعات<br/>مصدر جدول rooms]

    ST --> API
    LC --> API
    AD --> API
    ST -.->|أوفلاين| ST
    API --> AUTH
    API --> DB
    DB -->|trigger يملأ outbox| OUTBOX
    OUTBOX --> NOTIF
    ATT -.->|FK: event_id| DB
    ROOMS -.->|FK: room_id| DB
\`\`\`

### 3.2 — جدول المكونات والمسؤوليات

| المكون | مسؤوليته الوحيدة | ماذا لو سقط؟ | استراتيجية التعافي |
|---|---|---|---|
| PostgreSQL (جدول events + القيود) | مصدر الحقيقة الوحيد للأحداث وضمانة عدم التعارض | لا كتابة إطلاقاً؛ القراءة تستمر من كاش Hive على الأجهزة | نسخ احتياطي PITR يومي؛ failover مُدار من Supabase؛ RTO مستهدف < 15 دقيقة |
| PostgREST / Edge Functions | تنفيذ عقود API + الفحص الاستباقي + الإدراج الدفعي للامتحانات | نفس أثر سقوط DB للعمليات؛ الجدول المتزامن سابقاً متاح أوفلاين | إعادة تشغيل مُدارة؛ العميل يعيد المحاولة بـ exponential backoff |
| RLS Policies | فرض نطاق الرؤية والكتابة لكل دور | (لا تسقط منفردة — جزء من DB) لكن policy خاطئة = تسريب | اختبارات تكامل إلزامية لكل policy قبل أي نشر (القسم 7) |
| قارئ الـ Outbox | تحويل صفوف outbox إلى دفعات إشعارات | تتراكم الإشعارات دون ضياع (الصفوف محفوظة) | التشغيل التالي يلتقط المتراكم؛ تنبيه إذا عمر أقدم صف غير معالج > 5 دقائق |
| كاش Hive (على الجهاز) | عرض الجدول أوفلاين + مصدر الإشعارات الاستباقية المحلية | فقدان الكاش = إعادة مزامنة كاملة عند أول اتصال | إعادة بناء تلقائية؛ لا بيانات تُفقد (القراءة فقط) |

### 3.3 — تدفق البيانات لأهم العمليات (Critical Path Sequences)

**العملية 1: اقتراح المحاضر لمحاضرة ← موافقة الإداري ← النشر والإشعار**

\`\`\`mermaid
sequenceDiagram
    participant L as المحاضر
    participant A as API
    participant D as PostgreSQL
    participant O as Outbox Worker
    participant N as نظام الإشعارات
    participant S as الطالب

    L->>A: POST /events (lecture, room_id, time)
    A->>D: فحص استباقي للتعارض (نقطة فشل: نتيجة قديمة — القيد يضمن لاحقاً)
    A->>D: INSERT event (status=PENDING)
    D-->>A: 201 + event_id
    A-->>L: تم الإرسال — قيد المراجعة ⏳

    Note over A,D: --- لاحقاً: مراجعة الإداري ---
    A->>D: BEGIN; INSERT event_reviews(APPROVED);<br/>UPDATE events SET status=PUBLISHED
    D->>D: قيد EXCLUDE يتحقق هنا نهائياً<br/>(نقطة فشل: تعارض ظهر بعد الفحص → 409 للإداري)
    D->>D: Trigger يُدرج صف outbox (event_published)
    D-->>A: COMMIT
    O->>D: قراءة outbox غير المعالج (كل 30 ثانية)
    O->>N: دفعة إشعارات لطلاب المادة (نقطة فشل: FCM بطيء → retry بـ backoff)
    N-->>S: 🔔 «محاضرة جديدة أُضيفت لجدولك»
\`\`\`

**العملية 2: الطالب يفتح جدوله (المسار الأثقل — مع الأوفلاين)**

\`\`\`mermaid
sequenceDiagram
    participant S as تطبيق الطالب
    participant H as كاش Hive
    participant A as API
    participant D as PostgreSQL

    S->>H: اقرأ الجدول فوراً (< 500ms)
    H-->>S: عرض الجدول من الكاش
    S->>A: GET /schedule?since=<last_sync_at>
    alt متصل
        A->>D: SELECT مفلتر بـ RLS + updated_at > since<br/>(نقطة فشل: استعلام بطيء → الفهرس المركب 4.3)
        D-->>A: الدلتا فقط (غالباً 0 صف، < 1KB)
        A-->>S: 200 + التغييرات + server_time
        S->>H: دمج الدلتا + إعادة جدولة الإشعارات المحلية (قبل 30 دقيقة)
    else غير متصل
        S->>S: البقاء على الكاش + شارة «آخر تحديث منذ...»
    end
\`\`\`

**العملية 3: الجدولة الدفعية للامتحانات من الإداري (Top-Down)**

\`\`\`mermaid
sequenceDiagram
    participant AD as الإداري
    participant A as Edge Function: exams/bulk
    participant D as PostgreSQL

    AD->>A: POST /exams/bulk (قائمة: مادة+قاعة+وقت)
    A->>A: تحقق: الدور admin حصراً (قرار #10)
    A->>D: BEGIN + INSERT جميع الصفوف (status=PUBLISHED)
    alt تعارض في أي صف
        D-->>A: exclusion_violation على الصف X
        A->>D: ROLLBACK (الكل أو لا شيء)
        A-->>AD: 409 + تحديد الصفوف المتعارضة بالاسم والوقت
    else لا تعارض
        D-->>A: COMMIT (+ صفوف outbox لكل امتحان)
        A-->>AD: 201 + ملخص المُدرج
    end
\`\`\`

### 3.4 — قرارات معمارية جوهرية

#### [D-08] Monolith (داخل Supabase) لا Microservices

| الخيار | المزايا | العيوب | القرار |
|---|---|---|---|
| **خدمة واحدة (DB + API + Functions في Supabase) ✅** | معاملة واحدة تضمن اتساق (الحدث + المراجعة + الـ outbox)؛ فريق 2–3 يشغّلها؛ لا شبكة داخلية تفشل | حدود التوسع المستقل لكل مكوّن (غير مطلوب قبل ~50K مستخدم — A-01) | مقبول |
| Microservice مستقل للجدولة ❌ | استقلالية نشر | معاملات موزعة لمسار الموافقة؛ كلفة تشغيل غير مبررة | مرفوض |

#### [D-09] كتابة متزامنة (Sync) + إشعارات غير متزامنة (Async)

المسار الحرج (إنشاء/موافقة/تعديل) متزامن بالكامل داخل معاملة واحدة — المستخدم يعرف النتيجة فوراً بما فيها التعارض. كل ما هو «إخطار الآخرين» غير متزامن عبر الـ Outbox [D-07]. الحدّ الملموس: أي عملية كتابة تتجاوز 2 ثانية server-side تُعتبر خللاً يستوجب تنبيهاً (القسم 6.5).

---

## القسم 4: نماذج البيانات وتصميم قاعدة البيانات 🗄️

### 4.1 — مخطط الكيانات (ERD)

\`\`\`mermaid
erDiagram
    COURSES ||--o{ EVENTS : "لها أحداث"
    COURSES ||--o{ ENROLLMENTS : "يسجَّل فيها"
    USERS ||--o{ ENROLLMENTS : "طالب"
    USERS ||--o{ EVENTS : "أنشأه (محاضر/إداري)"
    ROOMS ||--o{ EVENTS : "تستضيف (nullable)"
    EVENTS ||--o{ EVENT_REVIEWS : "قرارات المراجعة"
    EVENTS ||--o{ EVENT_OUTBOX : "إشعارات معلّقة"
    EVENTS }o--|| EVENT_SERIES : "سلسلة اختيارية"
\`\`\`

> `USERS`, `COURSES`, `ENROLLMENTS` مملوكة لنظام المصادقة والتسجيل (A-05)، و`ROOMS` (الموحّد للقاعات والمعامل — A-04) لنظام إدارة القاعات — تظهر هنا كاعتماديات FK فقط.

### 4.2 — تعريف الجداول (Schema)

\`\`\`sql
-- الامتدادات المطلوبة
CREATE EXTENSION IF NOT EXISTS btree_gist;   -- لقيد منع تعارض القاعات [D-04]

-- أنواع معرّفة
CREATE TYPE event_type AS ENUM
  ('LECTURE','LAB','SEMINAR','EXAM','RESEARCH','ASSIGNMENT','CULTURAL');
CREATE TYPE event_status AS ENUM
  ('PENDING','PUBLISHED','REJECTED','CANCELLED');
CREATE TYPE submission_type AS ENUM ('ONLINE','PAPER');

-- event_series: تجميع اختياري لتكرارات الحدث الأسبوعي [D-06]
CREATE TABLE event_series (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    created_by  UUID NOT NULL REFERENCES users(id),
    created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- events: الجدول المركزي — صف لكل حدث بكل أنواعه [D-02]
CREATE TABLE events (
    id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    course_id     UUID REFERENCES courses(id),
        -- nullable: الحدث الثقافي قد لا يتبع مادة
    type          event_type NOT NULL,
    status        event_status NOT NULL DEFAULT 'PENDING',
    title         TEXT NOT NULL CHECK (char_length(title) BETWEEN 3 AND 120),
    description   TEXT CHECK (char_length(description) <= 2000),

    -- الزمن: TIMESTAMPTZ إلزامي — التخزين UTC دائماً (A-06)
    start_at      TIMESTAMPTZ,
    end_at        TIMESTAMPTZ,
    deadline_at   TIMESTAMPTZ,   -- للواجب/البحث بدل نافذة زمنية

    -- المورد الفيزيائي (nullable — الواجب/البحث/الثقافي بلا قاعة)
    -- يشير لجدول rooms الموحّد (قاعات + معامل — A-04)
    room_id       UUID REFERENCES rooms(id),

    -- حقول التسليم (واجب/بحث فقط)
    submission_type      submission_type,
    submission_location  TEXT,   -- مكان التسليم عند الورقي (MVP 3.1.1)

    -- حقول متمايزة قليلة الاستخدام (موضوع السمنار، اسم المقدِّم،
    -- مدة الامتحان بالدقائق، مكان الحدث الثقافي النصّي عند عدم
    -- ارتباطه بقاعة...) — JSONB مضبوط بـ CHECK لكل نوع
    meta          JSONB NOT NULL DEFAULT '{}'::jsonb,

    -- ملاحظة المحاضر على البطاقة (أونلاين / مؤجلة) — MVP 3.1.1
    lecturer_note TEXT CHECK (char_length(lecturer_note) <= 300),

    -- مشتق من النوع ومخزّن للوضوح ولاستهلاك نظام الحضور
    requires_attendance BOOLEAN NOT NULL DEFAULT false,

    series_id     UUID REFERENCES event_series(id),
    created_by    UUID NOT NULL REFERENCES users(id),

    -- soft delete (محسوم في 4.4) + تتبّع المزامنة التفاضلية [D-05]
    deleted_at    TIMESTAMPTZ,
    created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at    TIMESTAMPTZ NOT NULL DEFAULT now(),

    -- سلامة النوع: كل نوع يفرض حقوله
    CONSTRAINT chk_timed_events CHECK (
        type NOT IN ('LECTURE','LAB','SEMINAR','EXAM','CULTURAL')
        OR (start_at IS NOT NULL AND end_at IS NOT NULL AND end_at > start_at)
    ),
    CONSTRAINT chk_deadline_events CHECK (
        type NOT IN ('RESEARCH','ASSIGNMENT')
        OR (deadline_at IS NOT NULL AND submission_type IS NOT NULL)
    ),
    CONSTRAINT chk_paper_location CHECK (
        submission_type IS DISTINCT FROM 'PAPER'
        OR submission_location IS NOT NULL
    ),
    CONSTRAINT chk_room_required CHECK (
        type NOT IN ('LECTURE','LAB','SEMINAR','EXAM')
        OR room_id IS NOT NULL
    ),
    CONSTRAINT chk_course_required CHECK (
        type = 'CULTURAL' OR course_id IS NOT NULL
    ),

    -- الضمانة النهائية: لا حدثان منشوران بنفس القاعة بزمن متداخل [D-04]
    CONSTRAINT no_room_overlap EXCLUDE USING gist (
        room_id WITH =,
        tstzrange(start_at, end_at) WITH &&
    ) WHERE (status = 'PUBLISHED' AND room_id IS NOT NULL AND deleted_at IS NULL)
);

-- event_reviews: سجل قرارات المراجعة — append-only لا يُحذف (MVP 5.0) [D-03]
CREATE TABLE event_reviews (
    id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    event_id     UUID NOT NULL REFERENCES events(id),
    reviewer_id  UUID NOT NULL REFERENCES users(id),  -- الإداري
    decision     TEXT NOT NULL CHECK (decision IN ('APPROVED','REJECTED')),
    reason       TEXT,          -- إلزامي منطقياً عند الرفض (يُفرض في API)
    -- الإداري قد يعدّل القاعة/الوقت النهائيين عند القبول (MVP 5.1.2)
    final_room_id  UUID REFERENCES rooms(id),
    final_start_at TIMESTAMPTZ,
    final_end_at   TIMESTAMPTZ,
    created_at   TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- event_outbox: نمط Transactional Outbox للإشعارات [D-07]
CREATE TABLE event_outbox (
    id            BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    event_id      UUID NOT NULL REFERENCES events(id),
    change_kind   TEXT NOT NULL CHECK
                  (change_kind IN ('PUBLISHED','UPDATED','CANCELLED')),
    payload       JSONB NOT NULL,      -- لقطة الحقول المتغيرة للإشعار
    processed_at  TIMESTAMPTZ,         -- NULL = بانتظار المعالجة
    attempts      INT NOT NULL DEFAULT 0,
    created_at    TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- trigger صيانة updated_at (أساس المزامنة التفاضلية)
CREATE OR REPLACE FUNCTION touch_updated_at() RETURNS trigger AS $$
BEGIN NEW.updated_at = now(); RETURN NEW; END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER trg_events_touch BEFORE UPDATE ON events
FOR EACH ROW EXECUTE FUNCTION touch_updated_at();
\`\`\`

### 4.3 — استراتيجية الفهرسة (Indexing Strategy)

| الفهرس | الاستعلام الذي يخدمه | تكلفته على الكتابة |
|---|---|---|
| `(course_id, start_at) WHERE status='PUBLISHED' AND deleted_at IS NULL` (جزئي مركّب) | جدول الطالب الأسبوعي — المسار الأثقل (السؤال 0.2/2) | منخفضة — فهرس جزئي لا يشمل PENDING/REJECTED |
| `(updated_at)` على events | المزامنة التفاضلية `updated_at > since` | منخفضة — عمود واحد، الكتابة نادرة (3%) |
| `(created_by, status)` على events | شاشة المحاضر «طلباتي وحالتها» (MVP 4.1) | منخفضة |
| `(status) WHERE status='PENDING'` (جزئي) | لوحة الإداري لمراجعة الطلبات | شبه معدومة — صفوف PENDING قليلة |
| فهرس GiST من قيد `no_room_overlap` (تلقائي) | كشف التعارض + استعلامات إشغال القاعة زمنياً | متوسطة — GiST أثقل من B-tree، مقبولة لأن الكتابة نادرة |
| `(processed_at) WHERE processed_at IS NULL` على outbox (جزئي) | قارئ الـ Outbox كل 30 ثانية | شبه معدومة |

### 4.4 — أسئلة يجب الإجابة عنها صراحة

- **الترحيلات (Migrations):** كل التغييرات بأسلوب expand-and-contract (إضافة عمود nullable ← نشر الكود ← backfill ← فرض القيد). قيمة جديدة في ENUM تُضاف بـ `ADD VALUE` (لا يقفل الجدول). لا `ALTER` قافل خلال ساعات الذروة (7–10 صباحاً) أو فترات الامتحانات إطلاقاً.
- **الحذف: soft delete على `events`** عبر `deleted_at` — لثلاثة أسباب: (1) نظام الحضور يشير إلى أحداث قد تُلغى بعد تسجيل حضورها، (2) المزامنة التفاضلية تحتاج إيصال «هذا الحدث حُذف» للأجهزة الأوفلاين (حذف صلب = دلتا لا تُرى)، (3) قيد MVP «يُوثَّق ولا يُحذف». **hard delete** فقط لصفوف outbox المعالَجة الأقدم من 30 يوماً (تنظيف مجدول). `event_reviews` لا تُحذف أبداً.
- **التوسع:** عند A-01 (~13.5K صف/فصل ≈ 27K/سنة) لا حاجة لأي تقسيم قبل سنوات. عتبة القرار: إذا تجاوز الجدول ~5M صف أو تدهور p95 فوق 300ms رغم الفهارس ← partitioning بـ `RANGE` على `start_at` بمفتاح الفصل الدراسي (أقدم فصول للأرشفة الطبيعية).
- **التوافقية (Consistency):** الكتابة الحرجة (النشر، الموافقة، منع التعارض) **قوية strict** — معاملة واحدة + قيد المحرك، لا تفاوض هنا. **eventual consistency مقبولة** في: وصول الإشعارات (≤ 60 ثانية)، وكاش الأجهزة (بحكم الأوفلاين أصلاً — تُعالج بشارة «آخر تحديث منذ X»).

---

## القسم 5: عقود الـ API والواجهات 🔌

### 5.1 — مبادئ التصميم

- **[D-10] REST عبر PostgREST + Edge Functions** للعمليات المركّبة (الموافقة، الإدراج الدفعي، المزامنة). GraphQL مرفوض (لا حاجة لمرونة استعلام العميل — الشاشات محددة سلفاً)، gRPC مرفوض (عميل موبايل + لا خدمات داخلية متعددة).
- **النسخ:** بادئة مسار `/api/v1/` — كسر العقد يستلزم `/v2/` مع تشغيل متوازٍ فصلاً دراسياً كاملاً (الأجهزة الأوفلاين تتأخر بالتحديث).
- **صيغة الخطأ الموحدة:**

\`\`\`json
{
  "error": {
    "code": "ROOM_CONFLICT",
    "message": "رسالة قابلة للعرض للمستخدم بالعربية",
    "details": [ { "field": "start_at", "issue": "..." } ]
  }
}
\`\`\`

- **المصادقة:** JWT من Supabase Auth في ترويسة `Authorization: Bearer` — الدور (student/lecturer/admin) ضمن الـ claims ويُعاد فرضه بـ RLS (الدفاع المزدوج).

### 5.2 — توثيق الـ Endpoints

#### `GET /api/v1/schedule`
**الغرض:** جلب جدول المستخدم (كامل أو دلتا) مفلتراً على نطاقه | **المصادقة:** مطلوبة — كل الأدوار | **Rate Limit:** 60 طلباً/دقيقة/مستخدم

**المدخلات (Query):**

\`\`\`json
{
  "from": "ISO-8601 — اختياري، بداية النطاق الزمني (افتراضي: بداية الأسبوع الحالي)",
  "to": "ISO-8601 — اختياري (افتراضي: from + 14 يوماً، الأقصى 60 يوماً)",
  "since": "ISO-8601 — اختياري: طابع آخر مزامنة، وجوده يحوّل الرد إلى دلتا فقط (تشمل الملغى/المحذوف soft)"
}
\`\`\`

**المخرجات الناجحة (200):**

\`\`\`json
{
  "server_time": "2026-09-14T08:00:00Z",
  "events": [
    {
      "id": "uuid",
      "type": "LECTURE",
      "status": "PUBLISHED",
      "course": { "id": "uuid", "name": "..." },
      "title": "...",
      "start_at": "ISO-8601",
      "end_at": "ISO-8601",
      "room": { "id": "uuid", "name": "قاعة 3" },
      "lecturer_note": "المحاضرة أونلاين هذا الأسبوع",
      "requires_attendance": true,
      "deadline_at": null,
      "submission_type": null,
      "submission_location": null,
      "meta": {},
      "deleted_at": null,
      "updated_at": "ISO-8601"
    }
  ]
}
\`\`\`

**حالات الخطأ:**

| كود | متى تحدث | استجابة العميل المتوقعة |
|---|---|---|
| 400 | `to` قبل `from` أو نطاق > 60 يوماً | عرض رسالة التحقق |
| 401 | توكن منتهٍ/غائب | إعادة توجيه لتسجيل الدخول (البقاء على الكاش أوفلاين) |
| 429 | تجاوز rate limit | retry مع exponential backoff |
| 500 | خطأ داخلي | البقاء على الكاش + إعادة المحاولة لاحقاً |

---

#### `POST /api/v1/events`
**الغرض:** إنشاء حدث — من المحاضر: قاعة (PENDING) أو واجب/بحث (نشر مباشر PUBLISHED)؛ من الإداري: حدث ثقافي/ترفيهي (نشر مباشر PUBLISHED — قرار #19) | **المصادقة:** lecturer لمواده المسندة (الأنواع LECTURE/LAB/SEMINAR/RESEARCH/ASSIGNMENT) أو admin (النوع CULTURAL حصراً) — تُفرض بـ RLS | **Rate Limit:** 30 طلباً/دقيقة/مستخدم

**المدخلات:**

\`\`\`json
{
  "type": "LECTURE | LAB | SEMINAR | RESEARCH | ASSIGNMENT — مطلوب (lecturer). CULTURAL — admin حصراً (قرار #19). EXAM مرفوض هنا نهائياً (قرار #10)",
  "course_id": "uuid — مطلوب لغير CULTURAL، من مواد المحاضر",
  "title": "string — مطلوب، 3–120 حرفاً",
  "description": "string — اختياري، ≤ 2000 حرف",
  "start_at": "ISO-8601 — مطلوب للأنواع الزمنية (يشمل CULTURAL)",
  "end_at": "ISO-8601 — مطلوب للأنواع الزمنية، > start_at",
  "room_id": "uuid — مطلوب للمحاضرة/المعمل/السمنار؛ اختياري للـ CULTURAL (عند غيابه يُدخل المكان نصياً في meta.location)",
  "deadline_at": "ISO-8601 — مطلوب للواجب/البحث",
  "submission_type": "ONLINE | PAPER — مطلوب للواجب/البحث",
  "submission_location": "string — مطلوب عند PAPER",
  "meta": "object — اختياري (موضوع السمنار، اسم المقدِّم، مكان الحدث الثقافي النصّي...)",
  "repeat_weekly_until": "ISO-8601 — اختياري: يولّد صفوف السلسلة الأسبوعية [D-06]"
}
\`\`\`

**المخرجات الناجحة (201):**

\`\`\`json
{
  "id": "uuid",
  "status": "PENDING | PUBLISHED — PENDING للأنواع المتطلبة لموافقة (محاضرة/معمل/سمنار)، وPUBLISHED للنشر المباشر (واجب/بحث من المحاضر، ثقافي من الإداري)",
  "series_id": "uuid | null",
  "conflict_warning": null,
  "created_at": "ISO-8601"
}
\`\`\`

**حالات الخطأ:**

| كود | متى تحدث | استجابة العميل المتوقعة |
|---|---|---|
| 400 | حقول ناقصة/غير متسقة مع النوع (CHECK constraints مترجمة) | إبراز الحقل الخاطئ |
| 401 | توكن منتهٍ | إعادة توجيه لتسجيل الدخول |
| 403 | النوع EXAM (قرار #10)، أو CULTURAL من دور غير admin (قرار #19)، أو مادة ليست للمحاضر، أو الدور ليس lecturer/admin | رسالة «صلاحية غير كافية» |
| 409 | الفحص الاستباقي وجد تعارض قاعة (تحذير قبل الإرسال للمراجعة؛ وللـ CULTURAL المرتبط بقاعة: منع مباشر لأنه يُنشر فوراً) | عرض الحدث المتعارض واقتراح وقت آخر |
| 422 | `repeat_weekly_until` يولّد > 30 تكراراً | رفض مع بيان الحد |
| 429 | تجاوز rate limit | retry مع backoff |
| 500 | خطأ داخلي | رسالة عامة + إعادة محاولة |

---

#### `POST /api/v1/events/{id}/review`
**الغرض:** قرار الإداري (قبول مع القاعة/الوقت النهائيين، أو رفض بسبب) | **المصادقة:** admin حصراً | **Rate Limit:** 60 طلباً/دقيقة/مستخدم

**المدخلات:**

\`\`\`json
{
  "decision": "APPROVED | REJECTED — مطلوب",
  "reason": "string — مطلوب عند REJECTED (MVP 5.1.2)، ≤ 500 حرف",
  "final_room_id": "uuid — اختياري عند APPROVED لتعديل القاعة",
  "final_start_at": "ISO-8601 — اختياري عند APPROVED",
  "final_end_at": "ISO-8601 — اختياري عند APPROVED"
}
\`\`\`

**المخرجات الناجحة (200):**

\`\`\`json
{
  "event_id": "uuid",
  "status": "PUBLISHED",
  "review_id": "uuid"
}
\`\`\`

**حالات الخطأ:**

| كود | متى تحدث | استجابة العميل المتوقعة |
|---|---|---|
| 400 | REJECTED بلا سبب، أو أوقات نهائية غير متسقة | إبراز الحقل |
| 401 | توكن منتهٍ | تسجيل دخول |
| 403 | الدور ليس admin | رسالة صلاحية |
| 404 | الحدث غير موجود أو محذوف soft | تحديث القائمة |
| 409 | (أ) الحدث ليس PENDING — قرار مسبق أو ألغاه المحاضر؛ (ب) قيد EXCLUDE اصطدم بتعارض ظهر بعد الفحص | تحديث الشاشة وعرض الحالة الحقيقية / عرض التعارض |
| 429 | تجاوز rate limit | retry مع backoff |
| 500 | خطأ داخلي | إعادة محاولة — العملية idempotent-safe (409 عند التكرار) |

---

#### `POST /api/v1/exams/bulk`
**الغرض:** الجدولة الدفعية للامتحانات (Top-Down) — ذرّية بالكامل | **المصادقة:** admin حصراً | **Rate Limit:** 10 طلبات/دقيقة/مستخدم

**المدخلات:**

\`\`\`json
{
  "idempotency_key": "uuid — مطلوب: يمنع الإدراج المزدوج عند إعادة الإرسال",
  "exams": [
    {
      "course_id": "uuid — مطلوب",
      "title": "string — مطلوب",
      "room_id": "uuid — مطلوب",
      "start_at": "ISO-8601 — مطلوب",
      "end_at": "ISO-8601 — مطلوب",
      "meta": { "duration_minutes": 90 }
    }
  ]
}
\`\`\`

**المخرجات الناجحة (201):**

\`\`\`json
{
  "inserted": 42,
  "event_ids": ["uuid", "..."]
}
\`\`\`

**حالات الخطأ:**

| كود | متى تحدث | استجابة العميل المتوقعة |
|---|---|---|
| 400 | قائمة فارغة أو > 200 امتحان بالطلب الواحد | تقسيم الدفعة |
| 401 | توكن منتهٍ | تسجيل دخول |
| 403 | الدور ليس admin (يشمل أي محاولة من محاضر — قرار #10) | رسالة صلاحية |
| 409 | تعارض قاعة/وقت في أي صف ← ROLLBACK كامل، والرد يعدّد الصفوف المتعارضة بالفهرس والتفاصيل | تصحيح المتعارض وإعادة الإرسال بنفس idempotency_key |
| 409 | idempotency_key مستخدم سابقاً بنجاح | عرض النتيجة السابقة — لا إدراج مزدوج |
| 429 | تجاوز rate limit | retry مع backoff |
| 500 | خطأ داخلي | إعادة الإرسال آمنة بفضل idempotency_key |

---

#### `PATCH /api/v1/events/{id}`
**الغرض:** تعديل حدث (تغيير وقت/قاعة/ملاحظة «أونلاين/مؤجلة») — يُطلق إشعار UPDATED | **المصادقة:** منشئ الحدث (محاضر لمواده) أو admin؛ تعديل EXAM وCULTURAL: admin حصراً | **Rate Limit:** 30 طلباً/دقيقة/مستخدم

**المدخلات:** أي مجموعة جزئية من حقول الإنشاء + `lecturer_note` + `expected_updated_at` (ISO-8601 — للتحكم التفاؤلي بالتزامن).

**المخرجات الناجحة (200):** الحدث كاملاً بعد التعديل.

**حالات الخطأ:**

| كود | متى تحدث | استجابة العميل المتوقعة |
|---|---|---|
| 400 | حقول غير متسقة مع النوع | إبراز الحقل |
| 401 | توكن منتهٍ | تسجيل دخول |
| 403 | ليس المنشئ/الإداري، أو محاضر يعدّل EXAM أو CULTURAL | رسالة صلاحية |
| 404 | غير موجود أو محذوف | تحديث القائمة |
| 409 | `expected_updated_at` ≠ الحالي (عدّله شخص آخر) — يحسم صف التعديل المتزامن في 6.1 | جلب النسخة الأحدث والدمج يدوياً |
| 409 | تغيير الوقت/القاعة اصطدم بقيد التعارض | عرض التعارض |
| 429 | تجاوز rate limit | retry مع backoff |
| 500 | خطأ داخلي | إعادة محاولة |

---

#### `DELETE /api/v1/events/{id}`
**الغرض:** إلغاء حدث (soft — status=CANCELLED + deleted_at) — يُطلق إشعار CANCELLED | **المصادقة:** كما في PATCH | **Rate Limit:** 30 طلباً/دقيقة/مستخدم

**المخرجات الناجحة (200):** `{ "id": "uuid", "status": "CANCELLED" }`

**حالات الخطأ:** 401 / 403 / 404 كما أعلاه؛ 409 إذا الحدث ملغى أصلاً (idempotent عملياً — العميل يعتبره نجاحاً)؛ 429 / 500 كالمعتاد.

### 5.3 — العقود بين الأنظمة الداخلية

| المستهلك | العقد | الضمانة |
|---|---|---|
| نظام الإشعارات | صفوف `event_outbox` بحمولة `{event_id, change_kind, course_id, title, changed_fields}` | **at-least-once** — على نظام الإشعارات إزالة التكرار بمفتاح `(outbox.id)`؛ الترتيب مضمون لكل حدث (id تسلسلي) |
| نظام الحضور | قراءة `events` مباشرة: `id`, `requires_attendance`, `start_at`, `end_at`, `status` | لا يفتح جلسة إلا لحدث `PUBLISHED` غير محذوف — يُفرض بـ FK + شرط في نظام الحضور |
| نظام إدارة القاعات | يملك جدول `rooms` الموحّد (قاعات + معامل — A-04)؛ نظامنا FK فقط | حذف قاعة لها أحداث مستقبلية منشورة محظور (`ON DELETE RESTRICT`) |

---

## القسم 6: حالات الحافة، أنماط الفشل، والأمان 🛡️

### 6.1 — جرد حالات الحافة (Edge Case Inventory)

| السيناريو | ماذا يحدث حالياً في التصميم؟ | المعالجة |
|---|---|---|
| طلبان متزامنان يعدلان نفس الحدث | `expected_updated_at` (optimistic locking) في PATCH | الثاني يستلم 409 ويجلب الأحدث — لا كتابة ضائعة |
| موافقتان إداريتان متزامنتان على نفس الطلب | UPDATE مشروط بـ `status='PENDING'` داخل المعاملة | الثانية تجد 0 صف متأثر ← 409 «قرار مسبق» |
| موافقة على طلبين متزامنين لنفس القاعة والوقت | الفحص الاستباقي قد يمرّ للاثنين (TOCTOU) | قيد `EXCLUDE` يُفشل الثاني حتماً عند COMMIT ← 409 [D-04] |
| المستخدم يرسل مدخلات 10x المتوقع | حدود صريحة: title ≤ 120، description ≤ 2000، دفعة امتحانات ≤ 200، تكرار ≤ 30، نطاق جدول ≤ 60 يوماً + حد حجم جسم الطلب 100KB | 400/422 برسالة محددة قبل ملامسة DB |
| انقطاع الشبكة في منتصف كتابة | الكتابة معاملة ذرّية؛ العميل لا يعرف النتيجة | `idempotency_key` للدفعي؛ إعادة إرسال review تصطدم بـ 409 الآمن؛ إنشاء حدث مكرر يكشفه المحاضر بشاشة «طلباتي» (قبول واعٍ للمخاطرة — أثره حذف يدوي) |
| FCM بطيء (وليس ساقطاً) | قارئ الـ Outbox بـ timeout 10 ثوانٍ لكل دفعة | فشل الدفعة يترك `processed_at=NULL` مع `attempts+1`؛ بعد 5 محاولات ← تنبيه تشغيلي؛ لا يوقف بقية الصفوف |
| رسالة مكررة من الـ Outbox (at-least-once) | ممكنة بالتصميم | نظام الإشعارات يزيل التكرار بـ `outbox.id`؛ والإشعار المكرر للطالب مزعج لا خطير (worst case مقبول) |
| المناطق الزمنية / DST / انحراف ساعة الجهاز | تخزين TIMESTAMPTZ (UTC) حصراً؛ كل رد يتضمن `server_time` | العميل يحسب الانحراف ويصحّح العدادات التنازلية ونوافذ «زر الحضور/الامتحان» — ساعة الجهاز لا تُعتمد لأي قرار |
| طالب أوفلاين أسبوعاً وحدث في كاشه أُلغي | soft delete + الدلتا تشمل المحذوفات | أول مزامنة تُنظّف الكاش وتلغي إشعاراته المحلية المجدولة |
| المحاضر يلغي طلبه بينما الإداري يقرّره | كما في الموافقتين المتزامنتين | من يصل ثانياً يستلم 409 بالحالة الحقيقية |
| إلغاء محاضرة بعد تسجيل طلاب حضورهم فيها | FK من الحضور إلى الحدث + soft delete | سجلات الحضور تبقى سليمة مرتبطة بحدث CANCELLED — قرار احتسابها لنظام النتائج لا لنا |
| حدث ثقافي بلا مادة | `course_id` nullable مع CHECK يخصّ CULTURAL فقط؛ إنشاؤه حصري للإداري (قرار #19) | يُعرض للجميع (سياسة RLS خاصة بالنوع) |
| `repeat_weekly_until` يتقاطع مع عطلة | لا تقويم عطلات في MVP | المحاضر/الإداري يلغي التكرارات الفردية يدوياً — موثّق كقيد معروف |

### 6.2 — تحليل أنماط الفشل (Failure Mode Analysis)

| المكون | نمط الفشل | الاحتمالية | الأثر | الكشف | التعافي |
|---|---|---|---|---|---|
| PostgreSQL | تعطل كامل | منخفضة | 🔴 حرج (لا كتابة؛ القراءة تنجو بالكاش) | health check كل 30 ثانية | failover مُدار؛ RTO < 15 دقيقة؛ PITR للبيانات |
| PostgreSQL | تدهور أداء (فهرس منسي/خطة سيئة) | متوسطة | 🟡 بطء الجدول بالذروة | تنبيه p95 > 300ms لمدة 5 دقائق | `pg_stat_statements` + خطة الاستعلام؛ الكاش يمتص الأثر على الطلاب |
| قارئ الـ Outbox | توقف عن المعالجة | متوسطة | 🟡 تأخر الإشعارات (لا ضياع) | تنبيه: أقدم صف غير معالج > 5 دقائق | إعادة تشغيل — الالتقاط تلقائي من حيث توقف |
| RLS Policy | ثغرة بعد تعديل (تسريع/تسريب) | منخفضة | 🔴 تسريب نطاق رؤية | اختبارات تكامل للأدوار في CI قبل كل نشر | rollback فوري للـ policy؛ مراجعة سجلات الوصول |
| كاش Hive | تلف ملف الكاش على جهاز | منخفضة | 🟢 جهاز واحد | فشل فتح الصندوق (box) | مسح وإعادة مزامنة كاملة تلقائياً |
| FCM | انقطاع إقليمي | منخفضة | 🟡 لا إشعارات push | ارتفاع أخطاء الدفع | الإشعارات الاستباقية محلية أصلاً (A-07)؛ التغييرات تصل بأول فتح للتطبيق |
| ساعة الخادم | انحراف (clock skew) | منخفضة جداً | 🟡 نوافذ زمنية خاطئة | NTP مُدار من المزود | مسؤولية Supabase؛ `server_time` بالعقد يعزل أجهزة العملاء |

### 6.3 — نموذج التهديدات (STRIDE)

| التهديد | مثال ملموس على هذا النظام | الدفاع المحدد |
|---|---|---|
| Spoofing | طالب ينتحل JWT محاضر لنشر «إلغاء محاضرة» مزيف | توقيع JWT من Supabase Auth (RS256)؛ الدور من claims الخادم لا من جسم الطلب؛ انتهاء التوكن ≤ ساعة مع refresh |
| Tampering | محاضر يعدّل حدث امتحان أو حدث ثقافي أو حدث مادة زميله عبر استدعاء API مباشر (تجاوز الواجهة) | RLS: `UPDATE` مشروط بـ `created_by = auth.uid()` وإسناد المادة؛ EXAM وCULTURAL محصوران بدور admin في policies مستقلة — الواجهة ليست خط الدفاع |
| Repudiation | إداري ينكر رفضه طلب قاعة | `event_reviews` append-only بـ `reviewer_id` + طابع زمني؛ لا صلاحية DELETE عليه لأي دور |
| Information Disclosure | طالب يستعلم مواعيد امتحانات دفعات أخرى قبل نشرها له | RLS للقراءة: `PUBLISHED` ∧ (تسجيل ACTIVE في المادة ∨ النوع CULTURAL)؛ صفوف PENDING غير مرئية لغير المنشئ والإداري |
| Denial of Service | سكربت يغرق `GET /schedule` أو يرسل دفعات امتحانات ضخمة | Rate limits لكل endpoint (القسم 5.2)؛ حد 200 صف للدفعة؛ حد نطاق 60 يوماً؛ delta sync يجعل الطلب الطبيعي < 1KB أصلاً |
| Elevation of Privilege | محاضر يستدعي `/exams/bulk` مباشرة، أو ينشئ حدثاً بنوع CULTURAL | فحص الدور في Edge Function **+** RLS تمنع INSERT بنوعي EXAM وCULTURAL لغير admin — طبقتان مستقلتان (القراران #10 و#19 مفروضان تقنياً مرتين) |

### 6.4 — قائمة تدقيق أمنية إلزامية

- [x] **المصادقة والتفويض:** Supabase Auth (JWT RS256)؛ التحقق في PostgREST تلقائياً + RLS على كل جدول (`events`, `event_reviews`, `event_outbox` — الأخير بلا وصول لأي دور عميل، للخدمة فقط)
- [x] **تشفير البيانات:** in-transit: TLS 1.2+ إلزامي؛ at-rest: AES-256 (مُدار من المزود)؛ كاش Hive بصندوق مشفّر بمفتاح في التخزين الآمن للجهاز (Keychain/Keystore)
- [x] **إدارة الأسرار:** مفاتيح FCM وservice_role في Supabase Vault / أسرار Edge Functions — ليست في الكود ولا في التطبيق إطلاقاً (`service_role` لا يغادر الخادم أبداً)
- [x] **حقن SQL:** PostgREST parameterized بطبيعته؛ أي SQL يدوي في Edge Functions عبر استعلامات مُعاملة (prepared) حصراً؛ JSONB `meta` يُتحقق منه ببنية schema قبل الإدراج
- [x] **سجلات التدقيق:** يُسجَّل: كل قرار مراجعة، كل تعديل/إلغاء حدث (من، متى، ماذا تغيّر في payload الـ outbox). **لا يُسجَّل:** توكنات، ولا أي PII خارج معرّف المستخدم

### 6.5 — الملاحظة والمراقبة (Observability)

| # | المقياس | عتبة التنبيه |
|---|---|---|
| 1 | p95 لاستجابة `GET /schedule` | > 300ms لمدة 5 دقائق متصلة |
| 2 | عمر أقدم صف outbox غير معالج | > 5 دقائق |
| 3 | معدل أخطاء 5xx على كل الـ endpoints | > 1% من الطلبات في نافذة 10 دقائق |
| 4 | صفوف outbox بـ attempts ≥ 5 | ≥ 1 صف (تحقيق يدوي فوري) |
| 5 | استعلام تدقيق يومي: تعارضات في PUBLISHED (يجب أن يكون 0 دائماً — canary للقيد) | ≥ 1 = خلل جوهري، تصعيد فوري |

---

## القسم 7: خطة التنفيذ وخارطة الطريق 🗺️

### 7.1 — ترتيب المخاطر (Risk-First Ordering)

**أخطر افتراض تقني:** أن قيد `EXCLUDE USING gist` مع الفهرس الجزئي يمنع فعلاً كل سباقات التعارض تحت حمل متزامن، وأن المزامنة التفاضلية بـ `updated_at` لا تُسقط تغييرات (خصوصاً soft deletes) لأجهزة غابت طويلاً. كلاهما يُختبر في المرحلة 0 قبل بناء أي شيء فوقهما.

### 7.2 — المراحل

#### المرحلة 0: إثبات الجدوى (Spike) — 3 أيام
- **الهدف:** التحقق من [D-04] و[D-05]
- **المهام:** (1) schema مصغّر بقيد EXCLUDE، (2) سكربت يطلق 100 إدراج متزامن لنفس القاعة والوقت، (3) نموذج delta sync مع soft delete وجهاز «غائب» يتحقق من وصول كل التغييرات
- **المخرج القابل للاختبار:** سكربتا اختبار قابلان للتكرار مع نتائج موثقة
- **معيار النجاح/الفشل:** نجاح واحد بالضبط من الـ 100 إدراج المتزامن + صفر تغيير مفقود في السيناريو الغائب. **إن فشل EXCLUDE:** الخطة البديلة قفل استشاري `pg_advisory_xact_lock(room_id)` — أبطأ لكنه صحيح

#### المرحلة 1: الـ Schema والـ RLS — أسبوع
- **المهام:** (1) migrations كاملة للقسم 4.2، (2) الفهارس، (3) سياسات RLS للأدوار الثلاثة، (4) بيانات seed تجريبية، (5) اختبارات تكامل لكل policy (طالب لا يرى PENDING، محاضر لا يدرج EXAM ولا CULTURAL، ...)
- **الاعتماديات:** المرحلة 0 + جداول المرحلة 1 من MVP (A-05)
- **المخرج القابل للاختبار:** حزمة اختبارات RLS تمر بالكامل في CI
- **اختبارات المرحلة:** تكامل على DB حقيقية (لا mocks للـ RLS)

#### المرحلة 2: مسارات الكتابة (إنشاء + مراجعة + دفعي) — أسبوع ونصف
- **المهام:** (1) `POST /events` بالتحقق الكامل لكل نوع (بما فيها CULTURAL من الإداري — قرار #19)، (2) توليد السلسلة الأسبوعية، (3) `POST /events/{id}/review` بمعاملة ذرّية، (4) `POST /exams/bulk` بالـ idempotency، (5) PATCH/DELETE مع optimistic locking، (6) الفحص الاستباقي للتعارض
- **الاعتماديات:** المرحلة 1
- **المخرج القابل للاختبار:** دورة كاملة عبر curl: اقتراح ← موافقة ← نشر ← تعديل ← إلغاء، مع كل أكواد الخطأ الموثقة في 5.2
- **اختبارات المرحلة:** unit للتحقق لكل نوع + تكامل لسيناريوهات 409 المتزامنة (إعادة تشغيل اختبار السباق من المرحلة 0 عبر API)

#### المرحلة 3: جدول الطالب + الأوفلاين — أسبوع ونصف
- **المهام:** (1) `GET /schedule` كامل + دلتا، (2) طبقة Hive والدمج، (3) بطاقات الأنواع السبعة في Flutter، (4) جدولة الإشعارات المحلية «قبل 30 دقيقة» وإعادة بنائها عند كل مزامنة، (5) شارة «آخر تحديث منذ...»
- **الاعتماديات:** المرحلة 2 (بيانات حقيقية للعرض)
- **المخرج القابل للاختبار:** فتح الجدول بوضع الطيران بعد أول مزامنة + استلام الدلتا عند العودة
- **اختبارات المرحلة:** unit لمنطق الدمج (يشمل soft delete) + اختبار E2E لسيناريو «غائب أسبوعاً»

#### المرحلة 4: الـ Outbox والإشعارات — أسبوع
- **المهام:** (1) triggers ملء الـ outbox، (2) Edge Function المجدولة + retry/attempts، (3) التسليم لواجهة نظام الإشعارات، (4) تنظيف الصفوف المعالجة، (5) لوحة مقاييس القسم 6.5 وتنبيهاتها
- **الاعتماديات:** المرحلة 2 + جاهزية نظام الإشعارات (أو stub مؤقت)
- **المخرج القابل للاختبار:** تعديل حدث ← وصول push خلال ≤ 60 ثانية، وقياس موثّق
- **اختبارات المرحلة:** تكامل للـ outbox (فشل FCM المُحاكى لا يُضيع صفاً)

#### المرحلة 5: التقسية والتسليم — أسبوع
- **المهام:** (1) اختبار حمل: 30 طلب/ثانية على `/schedule` والتحقق من p95، (2) مراجعة قائمة 6.4 بنداً بنداً، (3) اختبار تدقيق التعارضات اليومي (canary)، (4) توثيق runbook للتنبيهات، (5) خطة rollback مجربة
- **الاعتماديات:** كل المراحل
- **المخرج القابل للاختبار:** تقرير حمل + كل بنود Definition of Done ✅

### 7.3 — مخطط الاعتماديات

\`\`\`mermaid
graph LR
    P0[مرحلة 0: Spike<br/>3 أيام] --> P1[مرحلة 1: Schema + RLS<br/>أسبوع]
    P1 --> P2[مرحلة 2: مسارات الكتابة<br/>1.5 أسبوع]
    P2 --> P3[مرحلة 3: الجدول + الأوفلاين<br/>1.5 أسبوع]
    P2 --> P4[مرحلة 4: Outbox + إشعارات<br/>أسبوع]
    P3 --> P5[مرحلة 5: التقسية والتسليم<br/>أسبوع]
    P4 --> P5
\`\`\`

> المرحلتان 3 و4 قابلتان للتوازي بين مطوّرين — الإجمالي التقديري: **6–7 أسابيع** لمطوّرَين.

### 7.4 — تعريف "الانتهاء" (Definition of Done)

- [ ] كل الاختبارات تمر (تغطية ≥ 85% للمسارات الحرجة: التحقق لكل نوع، المراجعة، كشف التعارض، دمج الدلتا)
- [ ] توثيق الـ API (القسم 5) مطابق للسلوك الفعلي ومحدَّث
- [ ] مقاييس القسم 6.5 الخمسة تعمل وتنبيهاتها مضبوطة ومجرّبة (تنبيه تجريبي واحد على الأقل لكل مقياس)
- [ ] تمت مراجعة قائمة الأمان (6.4) بالكامل مع توقيع المراجع
- [ ] اختبار الحمل يثبت p95 < 300ms عند 30 طلب/ثانية
- [ ] استعلام تدقيق التعارضات اليومي يعمل ونتيجته 0
- [ ] خطة rollback موثقة ومجرّبة (استعادة migration + إبطال policy)

---

## ملحق: خريطة تغطية متطلبات MVP (Traceability)

| متطلب MVP | أين غُThere was an error while generating response. Please try again later.