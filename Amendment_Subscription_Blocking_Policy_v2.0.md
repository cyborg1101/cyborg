# 📄 وثيقة تعديل رسمية — سياسة الحجب عند تأخر الاشتراك (v2.0)
## Academic Hub — Subscription Blocking Policy Amendment

---

## بيانات ضبط الوثيقة

| البند | التفاصيل |
|---|---|
| **اسم الوثيقة** | تعديل رسمي — سياسة الحجب عند تأخر سداد الاشتراك |
| **رقم الإصدار** | Amendment v2.0 |
| **طبيعة الوثيقة** | وثيقة تعديل مُلزمة — بنفس نمط «تعديل v1.6» — تُدمج لاحقاً في الإصدارات القادمة للوثائق المتأثرة |
| **الوثائق المتأثرة** | MVP Functions v3.6 · TDD Core Data Layer v2.1 · TDD Notifications v1.1 |
| **الوثائق المُراجَعة وغير المتأثرة** | TDD File Repository v1.1 · TDD Scheduling & Events v2.0 · TDD Room Management v1.0 |
| **حالة الوثيقة** | معتمدة — مُلزمة فور الاعتماد |
| **مالك الوثيقة** | رامز |

---

## 1. القرار الجديد (المُلزم)

### 1.1 — نص القرار

عند تأخر الطالب عن سداد اشتراك التطبيق، تُحجب **كافة ميزات التطبيق باستثناء اثنتين حصراً**:

1. **الامتحانات** — بطاقات الامتحان تبقى ظاهرة، وتسجيل حضور الامتحان ودخوله يبقيان فعّالين بالكامل (بلا تغيير عن القرار #15 الأصلي).
2. **نظام الدفعات المالية بالكامل (القناتان)** — استثناء جديد صريح: الطالب المحجوب يستطيع عرض بيانات الحسابات البنكية، رفع صور إشعارات التحويل، متابعة حالة طلباته، إعادة الرفع عند الرفض، واستقبال إشعارات الدفع — **حتى يتمكن من فك الحظر عن نفسه ذاتياً دون تدخل إداري**.

ويُضاف حجب جديد صريح:

3. **النتائج النهائية محجوبة عن الطالب المتأخر** — لا يستطيع الطالب رؤية درجاته المعتمدة إلا باشتراك نشط. النتيجة تُعتمد وتُنشر بشكل طبيعي (لا تغيير في `approve_final_results`)، لكن **رؤيتها** مشروطة بـ `has_active_subscription()`.

### 1.2 — جدول المقارنة (قبل / بعد)

| الوظيفة | قبل التعديل (قرار #15 الأصلي) | بعد التعديل (هذه الوثيقة) |
|---|---|---|
| بطاقات الامتحان + حضوره + دخوله | مستثناة من الحجب ✅ | مستثناة من الحجب ✅ (بلا تغيير) |
| نظام الدفعات (القناتان) | غير محدد صراحة — كان مفتوحاً في RLS **بالمصادفة** لا بالتوثيق | **مستثنى صراحة — مسار فك الحظر الذاتي** ✅ |
| النتائج النهائية (`final_results`) | مرئية للطالب دائماً (لا فحص اشتراك في السياسة) | **محجوبة حتى سداد الاشتراك** 🔒 |
| حالة الاشتراك (`subscription_status`) | غير محدد | **مقروءة لصاحبها دائماً** (شاشة السداد تحتاجها) ✅ |
| الإشعارات | مستثناة (قرار NT-10 في Notifications TDD) | مستثناة (بلا تغيير — تشمل إشعارات الدفع) ✅ |
| كل الوظائف الأخرى (جدول، ملفات، دردشة...) | محجوبة | محجوبة (بلا تغيير) 🔒 |

### 1.3 — الترقيم في سجل القرارات

- **القرار #15 (مُعدَّل):** سياسة الحجب تشمل جميع الميزات باستثناء (أ) دخول الامتحانات والنصفي والنهائي وبطاقاتها وحضورها، و(ب) نظام الدفعات المالية بالكامل بقناتيه؛ الحجب server-side عبر RLS حصراً و`has_active_subscription()` مصدر الحقيقة الوحيد.
- **القرار #24 (جديد):** النتيجة النهائية المعتمدة لا تُعرض للطالب إلا باشتراك نشط — الحجب على مستوى سياسة RLS في `final_results`، والاعتماد والنشر الإداريان لا يتأثران.

### 1.4 — المبرر

الصياغة السابقة أنتجت طريقاً مسدوداً منطقياً: طالب محجوب لا يستطيع الوصول لشاشة الدفع = لا يستطيع فك الحظر أبداً إلا بتدخل خارجي. الاستثناء الجديد يجعل دورة (تأخر → حجب → دفع → فك حجب) ذاتية بالكامل. وحجب النتائج يضيف حافزاً تشغيلياً مشروعاً للسداد دون المساس بالحق الأكاديمي (الامتحانات نفسها غير محجوبة أبداً).

---

## 2. الأثر التفصيلي على كل وثيقة

### 2.1 — MVP Functions v3.6 (تعديلات نصية — تُطبق في v3.7)

| الموضع | النص الحالي | النص الجديد |
|---|---|---|
| §3.7.2 «سياسة التأخر عن السداد» | «حجب كافة ميزات التطبيق باستثناء دخول الامتحانات (النصفي والنهائي) — بطاقات الامتحان تبقى ظاهرة وفعّالة بالكامل أثناء الحجب» | «حجب كافة ميزات التطبيق باستثناء: (1) دخول الامتحانات (النصفي والنهائي) — بطاقات الامتحان تبقى ظاهرة وفعّالة بالكامل، و(2) **نظام الدفعات المالية بالكامل بقناتيه** — ليتمكن الطالب من فك الحظر ذاتياً بالسداد. **النتائج النهائية تبقى محجوبة**: لا يرى الطالب درجاته المعتمدة إلا بعد سداد الاشتراك (قرار #24)» |
| §3.1.2 سياسة العرض عند الحجب | بلا تغيير — بطاقات الامتحان فقط تظهر في الجدول | بلا تغيير |
| §5.6.1 جمع النتائج واعتمادها | — | تُضاف ملاحظة: «نشر النتيجة لطلاب المادة فوراً» يعني إتاحتها في `final_results` — **رؤية الطالب لها مشروطة باشتراك نشط (قرار #24)**؛ الاعتماد الذرّي نفسه (قرار #22) لا يتأثر |
| سجل القرارات — قرار #15 | الصيغة القديمة | الصيغة المعدّلة في §1.3 أعلاه |
| سجل القرارات — قرار #24 | غير موجود | يُضاف بنص §1.3 أعلاه |

### 2.2 — TDD Core Data Layer v2.1 (تعديل SQL فعلي — يُطبق كـ migration)

**(أ) `final_results` — التغيير الجوهري الوحيد في الـ Schema/RLS:**

السياسة الحالية `student_own_final` تسمح للطالب برؤية نتيجته **بدون فحص اشتراك**. تُستبدل بالكامل (الـ SQL الكامل في القسم 3).

**(ب) `payment_requests` — لا تغيير SQL، توثيق فقط:**

السياسات الحالية (`pay_student`, `pay_insert`, `pay_review`) لا تتضمن `has_active_subscription()` — وهذا الآن **قرار موثَّق صراحة لا مصادفة**: يُضاف تعليق SQL يثبّت ذلك ويمنع أي مطور مستقبلي من «إصلاحه» بإضافة فحص اشتراك يكسر مسار فك الحظر.

**(ج) `subscription_status` — سياسة قراءة جديدة:**

شاشة السداد تحتاج عرض حالة الاشتراك و`paid_until` للطالب المحجوب — تُضاف سياسة SELECT لصاحب الصف (القسم 3).

**(د) جرد حالات الحافة §6.1 — تحديث صف «طالب متأخر عن الاشتراك»:**

الصياغة الجديدة: «يختفي كل شيء من الجدول عدا بطاقات الامتحانات، ويبقى تسجيل حضور الامتحان ودخوله فعّالين، **ويبقى نظام الدفعات بالكامل مفتوحاً (مسار فك الحظر الذاتي — قرار #15 المعدَّل)**، **وتُحجب النتائج النهائية حتى السداد (قرار #24)**».

**(هـ) اختبارات pgTAP — إضافات إلزامية (القسم 4).**

### 2.3 — TDD Notifications v1.1 (إضافة حالة حافة واحدة)

- **NT-10 يبقى كما هو** — الإشعارات تصل للطالب المحجوب، بما فيها إشعارات الدفع (تأكيد/رفض/استحقاق). هذا الآن أكثر أهمية: إشعار رفض الدفعة هو ما يدفع الطالب لإعادة الرفع وفك الحظر.
- **حالة حافة جديدة تُضاف لجدول §6.1:** إشعار `final_result_published` يصل لطالب محجوب → **يُرسل الإشعار طبيعياً** (حافز للسداد)، وعند فتحه يصطدم العميل بحجب RLS على `final_results` فيعرض شاشة «سدّد اشتراكك لعرض نتيجتك» مع رابط مباشر لنظام الدفعات. لا يُذكر أي جزء من الدرجة في نص الإشعار نفسه.

### 2.4 — TDD File Repository v1.1 (مُراجَعة — لا تغيير)

متوافقة كما هي: `sign-download` و`files_upload` يرفضان الطالب المحجوب بـ `SUBSCRIPTION_BLOCKED`، وشاشة الخطأ توجّه لنظام الدفعات — الذي أصبح الآن مفتوحاً فعلاً للطالب المحجوب، فاكتملت الدورة.

### 2.5 — TDD Scheduling & Events v2.0 (مُراجَعة — لا تغيير)

متوافقة كما هي: سياسة `events_visibility` تُظهر بطاقات الامتحان فقط للمحجوب — لا علاقة لها بالدفعات ولا بالنتائج.

### 2.6 — TDD Room Management v1.0 (مُراجَعة — لا تغيير)

لا منطق اشتراك فيها إطلاقاً — الطالب ليس فاعلاً في نظام القاعات.

---

## 3. الترحيل (Migration) — SQL كامل جاهز للتطبيق

\`\`\`sql
-- ============================================================
-- Amendment v2.0: سياسة الحجب عند تأخر الاشتراك
-- (1) حجب النتائج النهائية عن الطالب غير المشترك (قرار #24)
-- (2) توثيق انفتاح مسار الدفع صراحة (قرار #15 المعدَّل)
-- (3) سياسة قراءة حالة الاشتراك لصاحبها
-- ============================================================

BEGIN;

-- (1) final_results: الطالب يرى نتيجته فقط مع اشتراك نشط.
--     المحاضر المعيَّن والإداري لا يتأثران بالحجب إطلاقاً.
DROP POLICY IF EXISTS student_own_final ON final_results;
CREATE POLICY student_own_final ON final_results FOR SELECT
    USING (
        (student_id = auth.uid() AND has_active_subscription())
        OR my_role() = 'admin'
        OR (my_role() = 'lecturer' AND teaches(course_id))
    );

-- (2) payment_requests: لا تغيير على السياسات القائمة —
--     غياب has_active_subscription() منها قرار متعمَّد وموثَّق:
--     الدفع هو مسار فك الحظر الذاتي ولا يجوز حجبه تحت أي ظرف.
--     ⚠️ ممنوع إضافة فحص اشتراك لأي سياسة على هذا الجدول مستقبلاً.
COMMENT ON TABLE payment_requests IS
    'قناتا الدفع (قرار #14). مفتوح دائماً حتى للطالب المحجوب اشتراكياً — مسار فك الحظر الذاتي (قرار #15 المعدَّل / Amendment v2.0). لا يُضاف فحص اشتراك لأي سياسة هنا.';

-- (3) subscription_status: الطالب يقرأ حالته (شاشة السداد تعرض paid_until)
ALTER TABLE subscription_status ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS subscription_self_read ON subscription_status;
CREATE POLICY subscription_self_read ON subscription_status FOR SELECT
    USING (student_id = auth.uid() OR my_role() = 'admin');

COMMIT;
\`\`\`

**ملاحظة تطبيقية:** الحجب في الواجهة (إخفاء شاشة النتائج، إظهار شاشة «سدّد اشتراكك») تحسين UX فقط — **الطبقة الأمنية الوحيدة هي RLS أعلاه** (نفس مبدأ MVP §3.7.2: أي قفل في الواجهة وحده عديم القيمة أمنياً). وواجهات الدفع في العميل يجب ألا تُغلَّف بأي فحص اشتراك.

---

## 4. مصفوفة اختبارات القبول (تُضاف لحزمة pgTAP: دور × عملية × مؤسسة)

| # | السيناريو (طالب محجوب: `has_active_subscription()` = false) | النتيجة المطلوبة |
|---|---|---|
| T-01 | SELECT على `events` | بطاقات الامتحان فقط ✅ |
| T-02 | `record_attendance` لجلسة امتحان + `start_exam_attempt` | ينجحان ✅ |
| T-03 | INSERT في `payment_requests` (كلتا القناتين) | ينجح ✅ |
| T-04 | SELECT دفعاته السابقة في `payment_requests` | ينجح ✅ |
| T-05 | SELECT صفه في `subscription_status` | ينجح ✅ |
| T-06 | SELECT نتيجته في `final_results` | **صفر صفوف** 🔒 |
| T-07 | نفس الطالب بعد تأكيد الدفعة وتحديث `paid_until` → SELECT على `final_results` | نتيجته تظهر ✅ (فك الحظر ذاتي كامل الدورة) |
| T-08 | `sign-download` لملف مادة | 403 SUBSCRIPTION_BLOCKED 🔒 (بلا تغيير) |
| T-09 | محاضر المادة + إداري المؤسسة → SELECT على `final_results` لطالب محجوب | يريان النتيجة ✅ (الحجب على الطالب فقط) |
| T-10 | كل ما سبق عبر حدود مؤسسة أخرى | صفر وصول 🔒 (العزل المؤسسي فوق كل شيء) |

---

## 5. سجل التوافق النهائي

| الوثيقة | الحالة | الإجراء |
|---|---|---|
| MVP Functions v3.6 | متأثرة | تعديلات §2.1 تُدمج في الإصدار v3.7 |
| TDD Core Data Layer v2.1 | متأثرة | migration القسم 3 + تحديث §6.1 — يُدمج في v2.2 |
| TDD Notifications v1.1 | متأثرة جزئياً | حالة حافة `final_result_published` تُضاف في v1.2 |
| TDD File Repository v1.1 | متوافقة | لا إجراء |
| TDD Scheduling & Events v2.0 | متوافقة | لا إجراء |
| TDD Room Management v1.0 | متوافقة | لا إجراء |

*— نهاية الوثيقة — Subscription Blocking Policy Amendment v2.0 —*