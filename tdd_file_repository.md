# 📐 وثيقة التصميم التقني — نظام مستودع الملفات (File Repository System TDD)
> **الإصدار:** 1.0 | **المستوى:** 🟡 Production | **الوثائق المرجعية:** MVP Functions v3.5 (القسمان 3.3 و 4.3.1) · Attendance TDD · Scheduling & Events TDD v1.1

---

## القسم 0: بروتوكول التحليل المتسلسل 🧠

### 0.1 — إعادة صياغة المشكلة

المطلوب بناء مستودع ملفات مركزي مقسّم حسب المادة الدراسية، يجمع في موضع واحد ملفات المحاضر الرسمية (محاضرات، مراجع) واجتهادات الطلاب المقبولة. الطالب يتصفح ويحمّل يدوياً ويرفع اجتهاداته التي تمر بسير مراجعة (قيد المراجعة → مقبول/مرفوض)، والمحاضر يرفع مباشرة ويراجع الاجتهادات. النظام يجب أن يدعم استئناف التحميل المنقطع، وفتح PDF والصور دون اتصال، وإدارة التخزين المحلي على جهاز الطالب. الغموض الوحيد المكتشف: وثيقة MVP لا تحدد حداً أقصى لحجم الملف ولا أنواع الملفات المسموحة — سيُعالج في الافتراضات.

### 0.2 — الأسئلة الخمسة الحاسمة

1. **من المستخدم الفعلي؟** ~2,000 طالب و ~80 محاضراً (حسب افتراضات الوثائق السابقة). ذروة الاستخدام: أول 30 دقيقة بعد نشر ملف محاضرة جديد (إشعار جماعي → موجة تحميل متزامنة قد تصل إلى 500 تحميل متزامن لنفس الملف)، وليلة الامتحانات.
2. **قراءة أم كتابة؟** قراءة بشكل ساحق — نسبة تقريبية **200:1**. كتابة واحدة (رفع ملف) تقابلها مئات التحميلات. هذا يوجّه التصميم نحو CDN/التخزين الكائني وليس تمرير الملفات عبر الخادم.
3. **ماذا لو توقف النظام ساعة؟** إزعاج وليس كارثة — الطالب لا يستطيع تحميل ملفات جديدة، لكن الملفات المحمّلة مسبقاً تعمل أوفلاين (Hive + التخزين المحلي). المطلوب: availability بحدود 99.5% (توقف ≤ 3.6 ساعة/شهر مقبول).
4. **أكثر البيانات حساسية؟** ملفات المحاضر قبل النشر ليست سرية بطبيعتها، لكن **الاجتهادات المرفوضة/قيد المراجعة** يجب ألا يراها إلا صاحبها والمحاضر، و**ملاحظات المحاضر على الملفات** يجب ألا يراها الطالب إطلاقاً. الخطر الأكبر: رابط تحميل عام يسمح لغير المسجلين في المادة (أو المحجوبين لتأخر الاشتراك) بالوصول.
5. **أضيق عنق زجاجة خلال 6 أشهر؟** كلفة نطاق التحميل (egress bandwidth) الناتجة عن موجات التحميل المتزامن بعد كل إشعار ملف جديد. ملف PDF بحجم 20MB × 500 طالب = 10GB لكل محاضرة منشورة.

### 0.3 — الافتراضات المعلنة

| # | الافتراض | مستوى الثقة | تأثيره لو كان خاطئاً |
|---|---|---|---|
| A-01 | البنية الخلفية Supabase (Postgres + Storage + RLS) اتساقاً مع بقية وثائق TDD للمشروع ومبدأ RLS في MVP §1.1 | عالي | إعادة كتابة القسمين 2 و 4 — المنطق يبقى صالحاً |
| A-02 | الحد الأقصى لحجم الملف الواحد: **50MB** للمحاضر، **25MB** لاجتهاد الطالب | متوسط | تعديل قيمة config واحدة — لا تغيير معماري |
| A-03 | الأنواع المسموحة: PDF, PNG, JPG, PPTX, DOCX, XLSX, ZIP (قائمة بيضاء صريحة) | متوسط | توسيع القائمة البيضاء فقط |
| A-04 | إجمالي التخزين المتوقع سنة أولى: ~150GB (80 محاضراً × 8 مواد وسطياً × 25 ملفاً × 8MB متوسط + اجتهادات) | متوسط | مراجعة خطة التكلفة — لا تغيير معماري |
| A-05 | حذف الملف من المحاضر = soft delete (إخفاء) وليس حذفاً فيزيائياً فورياً | عالي | تعديل سلوك endpoint واحد |
| A-06 | سياسة حجب الاشتراك (MVP §3.7.2) تُطبَّق على هذا النظام: الطالب المحجوب لا يحمّل ولا يرفع | عالي | إزالة شرط واحد من سياسات RLS |
| A-07 | «العنصر» الذي تُربط به الملفات (مسار: مادة ← محاضرة/عنصر ← ملفات) هو حدث من نظام الجدولة أو تصنيف حر يُنشئه المحاضر | متوسط | تعديل مفتاح الربط في جدول `repo_folders` |

### 0.4 — نطاق العمل (Scope Fence)

- ✅ **داخل النطاق:** رفع/تحميل الملفات، سير مراجعة الاجتهادات، تمييز مصدر الملف، استئناف التحميل، الفتح أوفلاين (PDF/صور)، إدارة التخزين المحلي، ملاحظات المحاضر على الملفات، إشعار «ملف جديد» (كمُنتِج حدث لنظام الإشعارات)، تطبيق حجب الاشتراك.
- ❌ **خارج النطاق:** معاينة PPTX/DOCX داخل التطبيق (تحميل وفتح خارجي فقط)، التحرير التعاوني، نسخ الإصدارات (versioning) للملف الواحد، فحص الفيروسات (دين تقني موثّق §2.3)، البحث داخل محتوى الملفات (full-text search)، بنية نظام الإشعارات نفسه (نظام مستقل — نحن نُنتج الحدث فقط)، ضغط/تحويل الملفات على الخادم.

---

## القسم 1: الملخص التنفيذي وسياق المشكلة 📋

### 1.1 — بيان المشكلة

ملفات المواد الدراسية اليوم مبعثرة بين مجموعات واتساب وذاكرات USB وبريد إلكتروني، فيضيع على الطالب ملف المحاضرة قبل الامتحان، ويعيد المحاضر إرسال نفس الملف عشرات المرات، وتضيع اجتهادات الطلاب الجيدة (ملخصات، حلول) لأنه لا قناة رسمية لنشرها. كلفة عدم الحل: ساعات مهدرة أسبوعياً لكل طالب في البحث عن الملفات، وغياب أي أرشيف مؤسسي للمحتوى التعليمي.

### 1.2 — الهدف القابل للقياس

تمكين أي طالب من الوصول إلى أي ملف من ملفات مواده المسجّل فيها **خلال أقل من 10 ثوانٍ من فتح التطبيق** (3 نقرات: مادة ← عنصر ← ملف)، مع وصول إشعار الملف الجديد إلى 100% من طلاب المادة خلال 60 ثانية من الرفع.

### 1.3 — معايير النجاح

| معيار | القيمة المستهدفة | كيف يُقاس |
|---|---|---|
| زمن بدء التحميل (توليد الرابط الموقّع) p95 | < 500ms | لوحة Supabase / logs |
| نجاح استئناف التحميل المنقطع | ≥ 95% من المحاولات | telemetry من التطبيق |
| فتح PDF محمّل مسبقاً أوفلاين | 100% نجاح، < 2s | اختبار وضع الطيران |
| زمن دورة مراجعة الاجتهاد (رفع → إشعار المحاضر) | < 60s | قياس end-to-end |
| كلفة التخزين + النطاق شهرياً | ≤ 40$ عند 2,000 مستخدم | فاتورة المزود |

### 1.4 — ما ليس هذا النظام (Anti-Goals)

- ليس Google Drive: لا مجلدات حرة للطالب، لا مشاركة روابط خارجية، لا تحرير.
- ليس نظام تسليم واجبات: تسليم البحوث/الواجبات يمر عبر نظام الجدولة والتصحيح (MVP §4.3.2) وليس عبر المستودع.
- ليس أرشيفاً بنسخ إصدارات: رفع ملف معدَّل = ملف جديد، والقديم يُحذف يدوياً إن أراد المحاضر.
- ليس شبكة اجتماعية: لا إعجابات ولا تعليقات على الملفات — التعليقات مكانها نظام الدردشة.

---

## القسم 2: القيود التقنية واختيار التقنيات 🔧

### 2.1 — القيود المفروضة (Hard Constraints)

| نوع القيد | التفصيل | مصدره |
|---|---|---|
| تقني | فرض العزل بين الأدوار عبر RLS حصراً | MVP §1.1 (مبدأ حاكم) |
| تقني | عمل أوفلاين للملفات المحمّلة (PDF/صور) بعد أول مزامنة | MVP §3.3 |
| تقني | العميل Flutter مع Hive للتخزين المحلي | MVP §3.1.2 + الوثائق السابقة |
| ميزانية | فريق صغير — تفضيل الخدمات المُدارة على تشغيل بنية ذاتية | مبدأ «البساطة المنضبطة» MVP §1.1 |
| تنظيمي | حجب الطالب متأخر الاشتراك عن الميزات (عدا الامتحانات) | MVP §3.7.2 / A-06 |

### 2.2 — مصفوفة اختيار التقنيات

#### [D-01] طبقة: تخزين الملفات الفعلي (Object Storage)

| الخيار | المزايا | العيوب | لماذا قُبل/رُفض |
|---|---|---|---|
| **Supabase Storage ✅** | تكامل أصيل مع RLS وPostgres؛ روابط موقّعة مدمجة؛ TUS للرفع المستأنَف؛ CDN مدمج؛ لا خدمة إضافية تُدار | حدود حجم أقل مرونة من S3 الخام؛ ارتباط بمزود واحد (vendor lock-in)؛ egress يُحتسب على الباقة | قُبل: يحقق A-01 ويلغي الحاجة لكتابة طبقة تفويض مستقلة للملفات — سياسة RLS واحدة تحكم الجدول والملف معاً |
| S3 مباشرة + CloudFront ❌ | الأرخص على النطاق الكبير؛ مرونة قصوى | يتطلب كتابة طبقة تفويض كاملة (Lambda/Edge) لفرض عزل الأدوار؛ حساب AWS منفصل يُدار؛ يكسر توحيد الـ stack | رُفض: كلفة هندسية عالية لفريق صغير مقابل توفير غير مُثبت عند 150GB |
| Firebase Storage ❌ | قواعد أمان مرنة؛ SDK ناضج لـ Flutter | يفصل الأمان عن Postgres RLS (نظامان للتفويض = مصدران للخطأ)؛ لا استعلام علائقي على البيانات الوصفية | رُفض: يخالف مبدأ RLS الموحّد |

#### [D-02] طبقة: تسليم الملفات للطالب (Download Delivery)

| الخيار | المزايا | العيوب | لماذا قُبل/رُفض |
|---|---|---|---|
| **روابط موقّعة قصيرة العمر (Signed URLs, TTL = 15 دقيقة) ✅** | التحميل مباشرة من CDN دون المرور بالخادم (يحل عنق الزجاجة §0.2-س5)؛ يدعم HTTP Range للاستئناف؛ التفويض يحدث مرة عند توليد الرابط | الرابط قابل للمشاركة خلال الـ 15 دقيقة (خطر مقبول وموثّق §6.3)؛ يتطلب إعادة توليد عند انتهاء الصلاحية أثناء تحميل طويل | قُبل: الوحيد الذي يفصل التفويض عن نقل البيانات |
| تمرير الملف عبر API الخادم ❌ | تحكم كامل بكل بايت؛ إلغاء فوري للوصول | الخادم يصبح عنق زجاجة: 500 تحميل متزامن × 20MB يقتل أي خطة Supabase | رُفض: يفشل رياضياً أمام نمط الاستخدام (§0.2-س1) |
| ملفات عامة (Public bucket) ❌ | أبسط تنفيذ ممكن | أي شخص يملك الرابط يحمّل — يكسر عزل المواد وحجب الاشتراك معاً | رُفض: مخالفة أمنية مباشرة |

#### [D-03] طبقة: آلية الرفع المستأنَف (Resumable Upload)

| الخيار | المزايا | العيوب | لماذا قُبل/رُفض |
|---|---|---|---|
| **بروتوكول TUS عبر Supabase Storage ✅** | معيار مفتوح مدعوم أصلاً في Supabase؛ حزمة `tus_client` جاهزة لـ Flutter؛ استئناف بعد انقطاع الشبكة من نقطة التوقف | تعقيد أعلى قليلاً من رفع multipart بسيط | قُبل: شبكات الطلاب غير مستقرة (نفس مبرر وضع الطوارئ في Attendance TDD) |
| رفع HTTP بسيط (single PUT) ❌ | أبسط كود ممكن | انقطاع عند 90% من ملف 25MB = إعادة من الصفر — تجربة سيئة على شبكات الجوال | رُفض للملفات > 6MB؛ **يُستخدم كمسار سريع للملفات الصغيرة ≤ 6MB** |
| رفع مجزّأ مخصص (custom chunking) ❌ | تحكم كامل | إعادة اختراع TUS مع أخطاء جديدة | رُفض: لا قيمة مضافة |

#### [D-04] طبقة: عارض الملفات أوفلاين في التطبيق

| الخيار | المزايا | العيوب | لماذا قُبل/رُفض |
|---|---|---|---|
| **حزم Flutter محلية (pdfx للـ PDF + عارض الصور المدمج) + فهرس Hive ✅** | عرض كامل دون إنترنت؛ لا كلفة خادم؛ الملف على قرص الجهاز والفهرس (المسار + الحالة) في Hive | ذاكرة الجهاز محدودة — يعالجها قسم إدارة التخزين المحلي (MVP §3.3) | قُبل: يحقق متطلب الأوفلاين حرفياً |
| عارض ويب مضمّن (WebView + رابط) ❌ | لا حزم إضافية | يتطلب إنترنت — يكسر المتطلب مباشرة | رُفض |
| تحويل الملفات لصيغة موحدة على الخادم ❌ | تجربة عرض موحدة | خدمة تحويل تُدار + كلفة معالجة + خارج النطاق §0.4 | رُفض |

### 2.3 — الديون التقنية المقبولة عمداً

| الدين | لماذا الآن | متى يُسدد |
|---|---|---|
| لا فحص فيروسات على الملفات المرفوعة | قائمة بيضاء صارمة للأنواع + التحقق من الترويسة الثنائية (magic bytes) تخفف الخطر؛ خدمة فحص = كلفة وتعقيد | عند فتح أنواع ملفات تنفيذية أو تجاوز 5,000 مستخدم |
| لا إلغاء تكرار للملفات (deduplication) | نفس الملف قد يُرفع لمادتين — التكرار مقبول عند 150GB | عند تجاوز كلفة التخزين 100$/شهر — يُضاف عمود `sha256` جاهز من اليوم الأول تمهيداً |
| الرابط الموقّع لا يُلغى قبل انتهاء TTL | حذف الملف يمنع التوليد الجديد لكن الرابط الحي يعمل ≤ 15 دقيقة | مقبول دائماً — TTL قصير بما يكفي |

---

## القسم 3: معمارية النظام والمخططات 🏗️

### 3.1 — المخطط العام (System Context)

\`\`\`mermaid
graph TB
    ST[📱 تطبيق الطالب<br/>Flutter + Hive + ملفات محلية]
    LC[📱 تطبيق المحاضر<br/>Flutter]
    subgraph Supabase
        API[PostgREST API<br/>+ RLS]
        EF[Edge Function<br/>finalize-upload / sign-download]
        DB[(Postgres<br/>البيانات الوصفية)]
        SO[(Storage Buckets<br/>الملفات الفعلية + CDN)]
    end
    NT[🔔 نظام الإشعارات<br/>خارجي - مستهلك أحداث]

    ST -->|تصفح / حالة الاجتهادات| API
    ST -->|رفع TUS / تحميل برابط موقّع| SO
    ST -->|طلب رابط تحميل| EF
    LC -->|رفع / مراجعة / ملاحظات| API
    LC -->|رفع TUS| SO
    API --> DB
    EF --> DB
    EF --> SO
    DB -->|trigger: ملف نُشر| NT
\`\`\`

### 3.2 — جدول المكونات والمسؤوليات

| المكون | مسؤوليته الوحيدة | ماذا لو سقط؟ | استراتيجية التعافي |
|---|---|---|---|
| Postgres (بيانات وصفية) | سجل الملفات والاجتهادات والملاحظات + فرض RLS | لا تصفح ولا رفع؛ الملفات المحمّلة تعمل أوفلاين | نسخ احتياطي يومي مُدار؛ RPO ساعة واحدة |
| Storage + CDN | حفظ وتسليم البايتات | لا تحميل جديد؛ التصفح يعمل (بيانات وصفية فقط) | إعادة المحاولة من العميل مع backoff |
| Edge Function `sign-download` | التحقق من الأهلية (تسجيل + اشتراك) ثم توليد رابط موقّع | لا تحميلات جديدة | stateless — إعادة نشر فورية |
| Edge Function `finalize-upload` | التحقق من اكتمال الرفع + magic bytes + إنشاء سجل الملف | ملفات مرفوعة بلا سجل (يتيمة) | مهمة تنظيف دورية تحذف الكائنات اليتيمة > 24 ساعة |
| فهرس Hive المحلي | تتبّع الملفات المحمّلة (المسار، الحجم، تاريخ التحميل) | فقدان الفهرس = إعادة تحميل — لا فقدان بيانات مصدرية | إعادة بناء بمسح مجلد التطبيق |

### 3.3 — تدفق البيانات لأهم عمليتين (Critical Path)

**العملية 1: رفع اجتهاد طالب حتى نشره**

\`\`\`mermaid
sequenceDiagram
    participant S as الطالب
    participant SO as Storage (TUS)
    participant EF as finalize-upload
    participant DB as Postgres
    participant L as المحاضر
    S->>SO: رفع مستأنَف (chunks)
    Note over S,SO: نقطة فشل: انقطاع الشبكة → استئناف من آخر chunk
    SO-->>S: اكتمل الرفع (object path)
    S->>EF: finalize(object_path, course_id, folder_id)
    EF->>SO: قراءة أول 512 بايت (magic bytes)
    Note over EF: نقطة فشل: النوع لا يطابق الامتداد → حذف الكائن + رفض
    EF->>DB: INSERT file(status=PENDING, origin=STUDENT)
    DB-->>L: إشعار «اجتهاد جديد للمراجعة»
    L->>DB: UPDATE status=ACCEPTED
    Note over DB: trigger → نظام الإشعارات: «ملف جديد في المادة»
\`\`\`

**العملية 2: تحميل ملف مع الاستئناف**

\`\`\`mermaid
sequenceDiagram
    participant S as الطالب
    participant EF as sign-download
    participant DB as Postgres
    participant CDN as Storage/CDN
    S->>EF: طلب تحميل file_id
    EF->>DB: تحقق: مسجّل بالمادة؟ الاشتراك نشط؟ الملف منشور؟
    Note over EF,DB: نقطة فشل: محجوب اشتراكياً → 403 برسالة واضحة
    EF-->>S: رابط موقّع (TTL 15min)
    S->>CDN: GET مع Range: bytes=0-
    Note over S,CDN: انقطاع عند 60% → GET جديد بـ Range: bytes=N-
    CDN-->>S: 206 Partial Content
    S->>S: حفظ محلي + تسجيل في فهرس Hive
\`\`\`

### 3.4 — قرارات معمارية جوهرية

#### [D-05] ملفات المحاضر والاجتهادات: جدول واحد أم جدولان؟

| الخيار | المزايا | العيوب | لماذا قُبل/رُفض |
|---|---|---|---|
| **جدول واحد `files` بعمود `origin` وعمود `review_status` ✅** | «الاجتهاد المقبول يُنشر في مستودع المادة» (MVP §3.3) — أي أن المقبول *يصبح* ملف مستودع؛ استعلام تصفح واحد؛ RLS واحدة | صفوف المحاضر تحمل `review_status` غير ذي معنى (يُملأ ACCEPTED تلقائياً) | قُبل: يطابق النموذج الذهني للمتطلب حرفياً ويلغي JOIN في أهم مسار قراءة |
| جدولان منفصلان + نسخ عند القبول ❌ | فصل نظيف للمسؤوليات | نسخ السجل عند القبول = ازدواجية ومزامنة؛ استعلامان للتصفح | رُفض |
| جدولان + UNION VIEW ❌ | لا نسخ | تعقيد RLS على الـ VIEW؛ أداء أسوأ | رُفض |

#### [D-06] فرض حجب الاشتراك: أين؟

| الخيار | المزايا | العيوب | لماذا قُبل/رُفض |
|---|---|---|---|
| **في Edge Function `sign-download` + سياسة RLS على الرفع ✅** | نقطة فرض واحدة لكل اتجاه؛ رسالة خطأ واضحة (403 SUBSCRIPTION_BLOCKED) قابلة للعرض | منطق الحجب في مكانين (function + policy) | قُبل: التحميل لا يمر عبر RLS أصلاً (رابط موقّع) فلا بديل عن الـ function |
| في واجهة التطبيق فقط ❌ | صفر كود خلفي | يُتجاوز بتعديل العميل — ليس أماناً | رُفض |

---

## القسم 4: نماذج البيانات وتصميم قاعدة البيانات 🗄️

### 4.1 — مخطط الكيانات (ERD)

\`\`\`mermaid
erDiagram
    COURSES ||--o{ REPO_FOLDERS : contains
    REPO_FOLDERS ||--o{ FILES : groups
    COURSES ||--o{ FILES : scoped_to
    USERS ||--o{ FILES : uploaded_by
    USERS ||--o{ FILE_NOTES : writes
    FILES ||--o{ FILE_NOTES : annotated_by
    FILES {
        uuid id PK
        uuid course_id FK
        uuid folder_id FK
        uuid uploader_id FK
        text origin "LECTURER | STUDENT"
        text review_status "PENDING | ACCEPTED | REJECTED"
    }
\`\`\`

### 4.2 — تعريف الجداول (Schema)

\`\`\`sql
-- repo_folders: «العنصر» في مسار التصفح (مادة ← عنصر ← ملفات) — MVP §3.3
CREATE TABLE repo_folders (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    course_id   UUID NOT NULL REFERENCES courses(id),
    -- ربط اختياري بحدث جدولة (محاضرة) أو تصنيف حر ينشئه المحاضر (A-07)
    event_id    UUID REFERENCES schedule_events(id),
    title       TEXT NOT NULL CHECK (char_length(title) BETWEEN 1 AND 120),
    sort_order  INT  NOT NULL DEFAULT 0,      -- ترتيب يدوي من المحاضر
    created_by  UUID NOT NULL REFERENCES users(id),
    created_at  TIMESTAMPTZ NOT NULL DEFAULT now(),
    deleted_at  TIMESTAMPTZ                    -- soft delete (A-05)
);

-- files: السجل الموحّد لملفات المحاضر واجتهادات الطلاب [D-05]
CREATE TABLE files (
    id             UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    course_id      UUID NOT NULL REFERENCES courses(id),   -- denormalized عمداً: أهم شرط تصفية و RLS
    folder_id      UUID REFERENCES repo_folders(id),       -- NULL = جذر المادة
    uploader_id    UUID NOT NULL REFERENCES users(id),
    origin         TEXT NOT NULL CHECK (origin IN ('LECTURER','STUDENT')),
    -- سير المراجعة: ملفات المحاضر تولد ACCEPTED مباشرة (فرضه trigger أدناه)
    review_status  TEXT NOT NULL DEFAULT 'PENDING'
                   CHECK (review_status IN ('PENDING','ACCEPTED','REJECTED')),
    reject_reason  TEXT,                                    -- اختياري عند الرفض (MVP §3.3)
    reviewed_by    UUID REFERENCES users(id),
    reviewed_at    TIMESTAMPTZ,
    display_name   TEXT NOT NULL CHECK (char_length(display_name) BETWEEN 1 AND 200),
    storage_path   TEXT NOT NULL UNIQUE,                    -- مسار الكائن في Bucket
    mime_type      TEXT NOT NULL,                           -- بعد التحقق من magic bytes
    size_bytes     BIGINT NOT NULL CHECK (size_bytes > 0 AND size_bytes <= 52428800), -- ≤ 50MB (A-02)
    sha256         TEXT NOT NULL,                           -- تمهيد للـ dedup المستقبلي (§2.3)
    created_at     TIMESTAMPTZ NOT NULL DEFAULT now(),
    deleted_at     TIMESTAMPTZ                              -- soft delete (A-05)
);

-- file_notes: ملاحظات المحاضر على الملفات — لا يراها الطالب إطلاقاً (MVP §4.3.1)
CREATE TABLE file_notes (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    file_id     UUID NOT NULL REFERENCES files(id),
    author_id   UUID NOT NULL REFERENCES users(id),        -- محاضر حصراً (RLS)
    body        TEXT NOT NULL CHECK (char_length(body) BETWEEN 1 AND 2000),
    created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- trigger: ملف المحاضر يُنشر فوراً؛ اجتهاد الطالب يبدأ PENDING دائماً
CREATE OR REPLACE FUNCTION enforce_review_status() RETURNS trigger AS $$
BEGIN
    IF NEW.origin = 'LECTURER' THEN
        NEW.review_status := 'ACCEPTED';
        NEW.reviewed_at   := now();
    ELSE
        NEW.review_status := 'PENDING';  -- لا يستطيع الطالب حقن ACCEPTED
    END IF;
    RETURN NEW;
END; $$ LANGUAGE plpgsql;

CREATE TRIGGER trg_files_review BEFORE INSERT ON files
FOR EACH ROW EXECUTE FUNCTION enforce_review_status();
\`\`\`

**أهم سياسات RLS (نموذج مختصر):**

\`\`\`sql
ALTER TABLE files ENABLE ROW LEVEL SECURITY;

-- الطالب يرى: الملفات المنشورة لمواده النشطة + اجتهاداته هو بأي حالة (لسجل المتابعة)
CREATE POLICY student_read ON files FOR SELECT USING (
    deleted_at IS NULL AND (
        (review_status = 'ACCEPTED' AND is_enrolled_active(auth.uid(), course_id))
        OR uploader_id = auth.uid()
    )
);

-- المحاضر يرى كل ملفات مواده المسندة (بما فيها PENDING/REJECTED للمراجعة)
CREATE POLICY lecturer_read ON files FOR SELECT USING (
    is_course_lecturer(auth.uid(), course_id)
);

-- الرفع: مسجّل نشط + غير محجوب اشتراكياً (A-06) — أو محاضر المادة
CREATE POLICY upload ON files FOR INSERT WITH CHECK (
    uploader_id = auth.uid() AND (
        is_course_lecturer(auth.uid(), course_id)
        OR (is_enrolled_active(auth.uid(), course_id)
            AND NOT is_subscription_blocked(auth.uid()))
    )
);

-- المراجعة: محاضر المادة فقط يعدّل review_status
CREATE POLICY review ON files FOR UPDATE USING (
    is_course_lecturer(auth.uid(), course_id)
);
\`\`\`

### 4.3 — استراتيجية الفهرسة

| الفهرس | الاستعلام الذي يخدمه | تكلفته على الكتابة |
|---|---|---|
| `(course_id, folder_id, created_at DESC) WHERE deleted_at IS NULL AND review_status='ACCEPTED'` (partial) | التصفح — 90% من قراءات النظام | ضئيلة: partial index يستثني المرفوض/المحذوف |
| `(uploader_id, created_at DESC) WHERE origin='STUDENT'` (partial) | «سجل اجتهاداتي وحالاتها» للطالب | ضئيلة — يخص صفوف الطلاب فقط |
| `(course_id, review_status) WHERE review_status='PENDING'` (partial) | قائمة «قيد المراجعة» للمحاضر | شبه معدومة — الجدول الجزئي صغير دائماً |
| `file_notes(file_id)` | عرض ملاحظات المحاضر على ملف | ضئيلة — كتابة الملاحظات نادرة |
| UNIQUE على `storage_path` | منع سجلين لنفس الكائن (idempotency للـ finalize) | إلزامي رغم كلفته — يحمي التكامل |

### 4.4 — أسئلة يجب الإجابة عنها صراحة

- **الترحيل (Migrations):** إضافة أعمدة nullable فقط بدون downtime؛ أي تغيير على CHECK constraints يمر بنمط: إضافة constraint بـ `NOT VALID` ثم `VALIDATE` لاحقاً.
- **الحذف:** soft delete للسجلات (`deleted_at`)؛ الكائن الفيزيائي يُحذف بمهمة تنظيف بعد 30 يوماً — يتيح التراجع عن الحذف الخاطئ ويُبقي سجلات التدقيق سليمة.
- **التوسع:** عند 100 مليون صف (غير متوقع قبل سنوات) partitioning على `course_id` بالـ hash — لكن عند A-04 (~40K ملف/سنة) لا حاجة لأي شيء قبل 5 سنوات.
- **التوافقية:** eventual consistency مقبولة في: عدّاد الملفات الجديدة وإشعارات النشر (تأخر ≤ 60s). مرفوضة في: حالة الاجتهاد (الطالب يجب أن يرى ACCEPTED/REJECTED الحقيقية فوراً عند الفتح) وسياسات الوصول (RLS لحظية بطبيعتها).

---

## القسم 5: عقود الـ API والواجهات 🔌

### 5.1 — مبادئ التصميم

**[D-07]** PostgREST (المولَّد من Supabase) للقراءة والعمليات البسيطة المحكومة بـ RLS + **Edge Functions** حصراً للعمليات المركّبة (`finalize-upload`, `sign-download`) — بديل REST مخصص كامل رُفض (خادم يُدار بلا مبرر)، وبديل GraphQL رُفض (لا استعلامات متداخلة معقدة في هذا النظام). النسخ عبر مسار `/v1/`. صيغة الخطأ الموحدة: `{"error": {"code": "STRING_CODE", "message": "...", "details": {}}}`.

### 5.2 — توثيق الـ Endpoints

#### `POST /functions/v1/sign-download`
**الغرض:** التحقق من الأهلية وتوليد رابط تحميل موقّع | **المصادقة:** JWT (طالب/محاضر) | **Rate Limit:** 30 طلباً/دقيقة/مستخدم

**المدخلات:**
\`\`\`json
{
  "file_id": "uuid — مطلوب"
}
\`\`\`

**المخرجات الناجحة (200):**
\`\`\`json
{
  "url": "https://...signed-url...",
  "expires_at": "ISO-8601 (بعد 15 دقيقة)",
  "size_bytes": 8388608,
  "sha256": "hex — للتحقق من سلامة الملف بعد التحميل"
}
\`\`\`

**حالات الخطأ:**
| كود | متى تحدث | استجابة العميل المتوقعة |
|---|---|---|
| 400 | `file_id` ليس UUID صالحاً | رسالة تحقق |
| 401 | توكن منتهٍ | تحديث الجلسة / إعادة الدخول |
| 403 `NOT_ENROLLED` | غير مسجّل بالمادة أو تسجيله غير نشط | إخفاء الملف من الواجهة |
| 403 `SUBSCRIPTION_BLOCKED` | حجب لتأخر الاشتراك (A-06) | شاشة «سدّد اشتراكك» مع رابط الدفعات |
| 404 | الملف محذوف أو غير منشور (PENDING/REJECTED لغير صاحبه) | إزالة من القائمة المحلية |
| 429 | تجاوز 30 طلباً/دقيقة | retry مع exponential backoff |
| 500 | فشل توليد التوقيع | retry مرة واحدة ثم رسالة خطأ |

#### `POST /functions/v1/finalize-upload`
**الغرض:** إغلاق عملية رفع TUS مكتملة وإنشاء سجل الملف | **المصادقة:** JWT | **Rate Limit:** 10 طلبات/دقيقة/مستخدم

**المدخلات:**
\`\`\`json
{
  "storage_path": "string — مسار الكائن المرفوع، مطلوب",
  "course_id": "uuid — مطلوب",
  "folder_id": "uuid — اختياري (NULL = جذر المادة)",
  "display_name": "string — مطلوب، 1-200 حرف",
  "sha256": "hex string — محسوب على العميل، مطلوب"
}
\`\`\`

**المخرجات الناجحة (201):**
\`\`\`json
{
  "file_id": "uuid",
  "review_status": "ACCEPTED إن كان الرافع محاضراً، PENDING إن كان طالباً",
  "created_at": "ISO-8601"
}
\`\`\`

**حالات الخطأ:**
| كود | متى تحدث | استجابة العميل المتوقعة |
|---|---|---|
| 400 `INVALID_INPUT` | حقل ناقص أو اسم خارج الحدود | رسالة تحقق |
| 401 | توكن منتهٍ | إعادة الدخول |
| 403 `NOT_AUTHORIZED` | ليس محاضر المادة ولا طالباً مسجلاً نشطاً | إخفاء زر الرفع |
| 403 `SUBSCRIPTION_BLOCKED` | طالب محجوب اشتراكياً | شاشة السداد |
| 409 `ALREADY_FINALIZED` | نفس `storage_path` سُجّل سابقاً (إعادة إرسال) | اعتبارها نجاحاً — idempotent |
| 413 `FILE_TOO_LARGE` | > 50MB محاضر / > 25MB طالب | رسالة بالحد المسموح |
| 415 `TYPE_MISMATCH` | magic bytes لا تطابق الامتداد أو نوع خارج القائمة البيضاء (A-03) — **يُحذف الكائن فوراً** | رسالة «نوع الملف غير مدعوم» |
| 422 `HASH_MISMATCH` | sha256 المرسل ≠ المحسوب على الخادم | إعادة الرفع |
| 429 | تجاوز الحد | backoff |
| 500 | خطأ داخلي | الكائن يبقى — retry آمن (idempotent عبر 409) |

#### `PATCH /rest/v1/files?id=eq.{file_id}` — مراجعة اجتهاد
**الغرض:** قبول/رفض اجتهاد طالب (محكوم بسياسة RLS `review`) | **المصادقة:** JWT محاضر المادة | **Rate Limit:** 60/دقيقة

**المدخلات:**
\`\`\`json
{
  "review_status": "ACCEPTED | REJECTED — مطلوب",
  "reject_reason": "string ≤ 500 حرف — اختياري، له معنى عند REJECTED فقط"
}
\`\`\`

**حالات الخطأ:** 400 (حالة غير صالحة) · 401 · 403 (RLS: ليس محاضر المادة → صفر صفوف متأثرة، يعامَل كـ 404) · 409 (الملف محذوف) · 429 · 500. القبول يُطلق trigger إشعار «ملف جديد في المادة» لجميع طلابها.

**بقية العمليات** (تصفح المجلدات والملفات، سجل اجتهادات الطالب، قائمة PENDING للمحاضر، إضافة/قراءة `file_notes`, soft delete) تتم عبر PostgREST مباشرة وتحكمها سياسات RLS في §4.2 حرفياً — لا عقود إضافية تُكتب لها.

### 5.3 — العقود بين الخدمات الداخلية

حدث واحد يُنتَج لنظام الإشعارات عبر Postgres trigger → صف في `notification_outbox` (نمط Outbox — ضمان **at-least-once** والمستهلك مسؤول عن إزالة التكرار بـ `event_id`):

\`\`\`json
{
  "event_id": "uuid",
  "type": "FILE_PUBLISHED",
  "course_id": "uuid",
  "file_id": "uuid",
  "origin": "LECTURER | STUDENT",
  "occurred_at": "ISO-8601"
}
\`\`\`

---

## القسم 6: حالات الحافة، أنماط الفشل، والأمان 🛡️

### 6.1 — جرد حالات الحافة

| السيناريو | ماذا يحدث حالياً في التصميم؟ | المعالجة |
|---|---|---|
| طلبان متزامنان يعدلان نفس المورد (محاضران يراجعان نفس الاجتهاد — بنك مشترك للمادة) | UPDATE على صف واحد — آخر كتابة تفوز | مقبول عمداً: القبول والرفض كلاهما قرار مخوَّل؛ `reviewed_by/reviewed_at` يوثّقان مَن حسم أخيراً |
| المستخدم يرسل مدخلات بحجم 10x المتوقع | حجم الملف يُرفض عند TUS (حد الـ bucket) وعند finalize (413)؛ الحقول النصية CHECK constraints | دفاع مزدوج: حد Storage + قيد DB |
| انقطاع الشبكة في منتصف عملية كتابة (رفع) | TUS يستأنف من آخر chunk؛ إن لم يُستدعَ finalize يبقى الكائن يتيماً | idempotency عبر UNIQUE(storage_path) + مهمة تنظيف لليتيم > 24 ساعة |
| انقطاع في منتصف التحميل | HTTP Range يستأنف؛ إن انتهى الرابط الموقّع (> 15 دقيقة) | العميل يطلب `sign-download` جديداً ويكمل بنفس Range — شفاف للمستخدم |
| خدمة خارجية بطيئة وليست ساقطة (Storage بطيء أثناء finalize) | قراءة magic bytes قد تعلّق | timeout = 5s على قراءة الترويسة → 500 و retry آمن |
| رسالة مكررة من قائمة الانتظار (حدث FILE_PUBLISHED مكرر) | نمط Outbox = at-least-once | المستهلك (نظام الإشعارات) يزيل التكرار بـ `event_id` |
| الساعة/التوقيت | `expires_at` يُحسب على الخادم حصراً؛ العميل لا يقرر انتهاء الرابط بساعته | clock skew على الجهاز بلا أثر؛ كل التوقيتات TIMESTAMPTZ |
| طالب رفع اجتهاداً ثم أُلغي تسجيله بالمادة قبل المراجعة | RLS القراءة تعتمد `is_enrolled_active` للمنشور، لكن `uploader_id = auth.uid()` يُبقي له رؤية اجتهاده | يظل الاجتهاد قابلاً للمراجعة؛ إن قُبل يظهر للطلاب النشطين فقط |
| المحاضر يحذف مجلداً يحوي ملفات | soft delete على المجلد | الملفات تبقى وتظهر في جذر المادة (folder_id يشير لمجلد محذوف → تُعرض كجذر في الواجهة) |
| امتلاء ذاكرة جهاز الطالب أثناء التحميل | فشل كتابة محلية | فحص المساحة المتاحة قبل بدء التحميل + شاشة «إدارة التخزين المحلي» (MVP §3.3) |

### 6.2 — تحليل أنماط الفشل

| المكون | نمط الفشل | الاحتمالية | الأثر | الكشف | التعافي |
|---|---|---|---|---|---|
| Postgres | تعطل كامل | منخفضة | 🔴 لا تصفح/رفع | health check المزود | failover مُدار < 60s؛ الأوفلاين يغطي القراءة المحلية |
| Storage/CDN | تعطل التسليم | منخفضة | 🟠 لا تحميلات جديدة | ارتفاع أخطاء 5xx من العميل (telemetry) | retry + backoff؛ الملفات المحمّلة تعمل |
| finalize-upload | فشل بعد الرفع وقبل السجل | متوسطة | 🟡 كائن يتيم + إحباط مستخدم | مهمة التنظيف تحصي اليتامى | retry آمن (idempotent)؛ تنبيه إن تجاوز اليتامى 50/يوم |
| مهمة التنظيف | لا تعمل | متوسطة | 🟢 تضخم تخزين بطيء | metric: عدد الكائنات بلا سجل | تشغيل يدوي؛ لا فقدان بيانات |
| فهرس Hive | تلف محلي | منخفضة | 🟢 يخص جهازاً واحداً | فشل فتح الصندوق | إعادة بناء بمسح مجلد الملفات المحلي |

### 6.3 — نموذج التهديدات (STRIDE)

| التهديد | مثال ملموس على هذا النظام | الدفاع المحدد |
|---|---|---|
| Spoofing | طالب ينتحل هوية محاضر ليرفع ملفاً «رسمياً» يظهر منشوراً فوراً | `origin` و `review_status` يفرضهما trigger من دور الحساب في DB — لا يُقبلان من العميل إطلاقاً |
| Tampering | تعديل جسم finalize لتسجيل ملف على مادة أخرى | سياسة RLS `upload` تتحقق من علاقة الرافع بالمادة على الخادم |
| Repudiation | محاضر ينكر رفض اجتهاد | `reviewed_by/reviewed_at` + سجل تدقيق على تغييرات `review_status` |
| Information Disclosure | مشاركة رابط موقّع خارج المنصة | TTL = 15 دقيقة + الرابط لملف واحد لا لـ bucket؛ خطر متبقٍ مقبول وموثّق [D-02] |
| Information Disclosure | طالب يقرأ ملاحظات المحاضر على الملفات | RLS على `file_notes`: SELECT لمحاضري المادة حصراً — لا سياسة قراءة للطالب أصلاً |
| Denial of Service | سيل طلبات sign-download لاستنزاف النطاق | rate limit 30/دقيقة/مستخدم + التحميل من CDN لا من الخادم |
| DoS (تخزيني) | طالب يرفع مئات الاجتهادات الضخمة | حد 25MB + حصة **20 اجتهاداً PENDING كحد أقصى** لكل طالب لكل مادة (CHECK عبر function) |
| Elevation of Privilege | طالب يستدعي PATCH review مباشرة | سياسة RLS `review` تتطلب `is_course_lecturer` — الطلب يؤثر على صفر صفوف |

### 6.4 — قائمة تدقيق أمنية إلزامية

- [x] المصادقة والتفويض: JWT (Google OAuth حسب MVP v3.4) + RLS على كل الجداول؛ Edge Functions تتحقق بنفس دوال `is_*`
- [x] التشفير: at-rest مُدار من المزود (AES-256)؛ in-transit TLS 1.2+ على كل المسارات بما فيها روابط CDN
- [x] الأسرار: مفاتيح service-role في أسرار Edge Functions حصراً — لا تصل للعميل أبداً
- [x] الحقن: PostgREST parameterized بطبيعته؛ لا SQL خام في الـ functions إلا عبر معاملات مربوطة
- [x] سجلات التدقيق: تغييرات review_status وحذف الملفات (مَن/متى)؛ **لا يُسجَّل** محتوى الملفات ولا ملاحظات المحاضر في اللوجات (PII)

### 6.5 — الملاحظة والمراقبة (Observability)

| المقياس | عتبة التنبيه |
|---|---|
| p95 لزمن sign-download | > 800ms لخمس دقائق متواصلة |
| نسبة 403 SUBSCRIPTION_BLOCKED من إجمالي الطلبات | > 20% (قد تعني خللاً في نظام الدفعات لا حجباً حقيقياً) |
| كائنات يتيمة مكتشفة يومياً | > 50 |
| نسبة 415 TYPE_MISMATCH | > 5% من عمليات finalize (محاولات إساءة أو خلل بالعميل) |
| استهلاك النطاق الشهري (egress) | > 80% من سقف الباقة |

---

## القسم 7: خطة التنفيذ وخارطة الطريق 🗺️

### 7.1 — ترتيب المخاطر (Risk-First Ordering)

أخطر افتراض تقني: **أن رفع TUS المستأنَف وتحميل HTTP Range يعملان بموثوقية على شبكات جوال متقطعة من Flutter عبر Supabase Storage** — إن فشل هذا، يسقط [D-02] و [D-03] معاً ونعيد النظر في طبقة التسليم بالكامل. لذلك هو موضوع المرحلة 0.

### 7.2 — المراحل

#### المرحلة 0: إثبات الجدوى (Spike) — 3 أيام
- **الهدف:** التحقق من الاستئناف (رفعاً وتحميلاً) على شبكة متقطعة
- **المخرج القابل للاختبار:** تطبيق Flutter تجريبي يرفع ملف 25MB بقطع الشبكة مرتين عمداً، ويحمّل ملف 30MB بنفس الشرط، مع تحقق sha256 نهائي
- **معيار النجاح/الفشل:** ≥ 95% نجاح عبر 40 محاولة مع انقطاعات مفتعلة؛ **إن فشل:** الخطة البديلة رفع مجزّأ مخصص عبر Edge Function (نقبل التعقيد المرفوض في [D-03])

#### المرحلة 1: نواة البيانات والأمان — 4 أيام
- **المهام:** (1) جداول §4.2 مع triggers (2) دوال `is_enrolled_active` / `is_course_lecturer` / `is_subscription_blocked` (3) سياسات RLS كاملة (4) الفهارس §4.3
- **الاعتماديات:** نظام المصادقة والتسجيل الأكاديمي (المرحلة 1 من خارطة MVP §8)
- **المخرج القابل للاختبار:** استعلامات SQL بأدوار مختلفة تثبت العزل (طالب لا يرى PENDING غيره، لا يرى file_notes، إلخ)
- **اختبارات هذه المرحلة:** اختبارات RLS آلية (pgTAP) تغطي كل صف في مصفوفة الصلاحيات

#### المرحلة 2: مسار الرفع والنشر — 5 أيام
- **المهام:** (1) `finalize-upload` مع magic bytes وحدود الحجم (2) تكامل TUS في العميل (3) واجهة رفع المحاضر (4) واجهة رفع اجتهاد الطالب + سجل الحالات (5) مهمة تنظيف اليتامى
- **الاعتماديات:** المرحلتان 0 و 1
- **المخرج القابل للاختبار:** محاضر يرفع فيُنشر فوراً؛ طالب يرفع فيظهر PENDING له وللمحاضر فقط
- **اختبارات هذه المرحلة:** integration على كل أكواد خطأ finalize (409, 413, 415, 422)

#### المرحلة 3: مسار التحميل والأوفلاين — 5 أيام
- **المهام:** (1) `sign-download` مع فحص الأهلية والحجب (2) مدير تحميل بالاستئناف + فهرس Hive (3) عارض PDF/صور أوفلاين (4) شاشة إدارة التخزين المحلي (حذف/إعادة تحميل)
- **الاعتماديات:** المرحلة 2
- **المخرج القابل للاختبار:** تحميل ملف، تفعيل وضع الطيران، فتحه بنجاح؛ طالب محجوب يرى شاشة السداد
- **اختبارات هذه المرحلة:** unit لمنطق Range/استئناف + اختبار يدوي موثّق لوضع الطيران

#### المرحلة 4: المراجعة والإشعارات والملاحظات — 4 أيام
- **المهام:** (1) واجهة مراجعة المحاضر (قبول/رفض مع سبب) (2) trigger + outbox لحدث FILE_PUBLISHED (3) file_notes للمحاضر (4) تصفح المجلدات وترتيبها
- **الاعتماديات:** المرحلة 2 + جاهزية نظام الإشعارات لاستهلاك الحدث
- **المخرج القابل للاختبار:** قبول اجتهاد → يظهر لكل طلاب المادة مع تمييز «اجتهاد طالب» + إشعار خلال 60s
- **اختبارات هذه المرحلة:** integration لسير المراجعة الكامل + إزالة تكرار الأحداث

### 7.3 — مخطط الاعتماديات

\`\`\`mermaid
graph LR
    P0[مرحلة 0: Spike الاستئناف] --> P2[مرحلة 2: الرفع والنشر]
    P1[مرحلة 1: البيانات و RLS] --> P2
    P2 --> P3[مرحلة 3: التحميل والأوفلاين]
    P2 --> P4[مرحلة 4: المراجعة والإشعارات]
\`\`\`

### 7.4 — تعريف «الانتهاء» (Definition of Done)

- [ ] كل الاختبارات تمر (تغطية ≥ 85% لمسارات finalize / sign-download / RLS)
- [ ] توثيق الـ API محدّث بكل أكواد الخطأ في §5.2
- [ ] مقاييس §6.5 الخمسة تعمل والتنبيهات مضبوطة بعتباتها
- [ ] قائمة الأمان §6.4 مراجَعة بالكامل
- [ ] خطة rollback: تعطيل الرفع بـ feature flag مع بقاء التصفح والتحميل — موثقة ومجربة

---

*— نهاية الوثيقة — File Repository System TDD v1.0 —*