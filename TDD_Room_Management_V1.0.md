# 🏛️ وثيقة التصميم التقني — نظام إدارة القاعات (Room Management TDD)
## منصة Academic Hub

---

## بيانات ضبط الوثيقة

| البند | التفاصيل |
|---|---|
| **اسم الوثيقة** | التصميم التقني لنظام إدارة القاعات (Room Management TDD) |
| **رقم الإصدار** | v1.0 |
| **مستوى العمق** | 🟡 Production |
| **الوثائق المرجعية** | MVP Functions v3.6 (§4.1، §5.1، القرارات #9، #10، #19، #20) · TDD Core Data Layer v2.1 الموحّدة (الجداول: `rooms`، `room_requests`، `events` + قيد `no_room_overlap`) · سجل القرارات المعماري D-01..D-06 |
| **حالة الوثيقة** | جاهزة للمراجعة الهندسية |
| **المرحلة في خارطة الطريق** | المرحلة 4 (MVP §8) — تعتمد على المراحل 1–3 |

### علاقة هذه الوثيقة بطبقة البيانات الأساسية

هذه الوثيقة **لا تعيد تعريف** الجداول `rooms` و`room_requests` و`events` — فهي معرَّفة ومملوكة في Core Data Layer TDD v2.1 (القسم 4.2). ما تملكه هذه الوثيقة حصراً:

- **منطق سير العمل (Workflow):** دورة حياة طلب الحجز من الاقتراح إلى النشر أو الرفض
- **الجدولة الإدارية الشاملة للامتحانات** (Top-Down — MVP §5.1.1)
- **عقود RPC** الخاصة بالحجز والمراجعة والجدولة الجماعية وكشف التعارضات
- **جدول واحد جديد:** `exam_schedule_batches` (تتبّع دفعات الجدولة الجماعية)

أي تعديل على الـ schema الأساسي يمر عبر وثيقة Core Data Layer — لا استثناءات.

---

## القسم 0: بروتوكول التحليل المتسلسل 🧠

### 0.1 — إعادة صياغة المشكلة (Problem Restatement)

القاعات والمعامل موارد فيزيائية محدودة تتنازع عليها ثلاثة أنواع أحداث يقترحها المحاضرون (محاضرة / معمل / سمنار) ونوع رابع يجدوله الإداري حصرياً (الامتحان). بدون نظام مركزي: تعارضات مزدوجة الحجز تُكتشف في القاعة نفسها أمام الطلاب، وامتحانات تتصادم زمنياً فتستحيل على طالب مسجَّل في المادتين، وطلبات محاضرين تضيع بلا مسار مراجعة موثَّق. النظام المطلوب: مسار طلب→مراجعة→نشر للأحداث الاعتيادية، وجدولة مركزية دفعة واحدة للامتحانات، مع **كشف تعارض آلي على مستوى محرك قاعدة البيانات لا على مستوى الواجهة** — وكل ذلك معزولاً بحدود المؤسسة (`institution_id`) في بيئة 10 مؤسسات.

### 0.2 — الأسئلة الخمسة الحاسمة

1. **من المستخدم الفعلي؟** المحاضر (مقدِّم طلبات — عشرات الطلبات/أسبوع/مؤسسة) والإداري (مراجع + مجدول امتحانات — ذروته أسبوع الجدولة الفصلية). الطالب مستفيد غير مباشر: يرى الحدث المنشور فقط.
2. **ما هي العملية الأثقل؟** الجدولة الجماعية للامتحانات: إدراج عشرات الأحداث في معاملة واحدة مع فحص تعارض لكل صف (قاعة × زمن × تداخل طلاب المواد). عملية نادرة (مرة/فصل/مؤسسة) لكنها الأعلى كلفة حسابية.
3. **ماذا لو توقف النظام ساعة؟** لا أثر تشغيلي فوري — الأحداث المنشورة سابقاً تُقرأ من كاش Hive لدى الطلاب. الأثر الوحيد: تأخر مراجعة الطلبات المعلّقة (مقبول تماماً).
4. **ما البيانات الأكثر حساسية؟** أسباب رفض الطلبات (تظهر للمحاضر المعني فقط)، وجدول الامتحانات **قبل نشره** (تسريبه المبكر عبر مؤسسة أو لطلاب قبل الاعتماد غير مقبول).
5. **أضيق عنق زجاجة خلال 6 أشهر؟** فحص GiST للتعارض عند نمو `events` — يُعالج بكون القيد جزئياً (`WHERE status='approved' AND deleted_at IS NULL`) وبفهرسة تبدأ بـ `institution_id`.

### 0.3 — الافتراضات المعلنة (Explicit Assumptions)

| # | الافتراض | مستوى الثقة | تأثيره لو كان خاطئاً |
|---|---|---|---|
| R-01 | ~50–200 قاعة/معمل لكل مؤسسة | عالي | لو تضاعف: لا تغيير — الأحجام صغيرة جوهرياً |
| R-02 | ~100 طلب حجز/أسبوع/مؤسسة كحد أقصى | عالي | لو تضاعف: لا تغيير معماري |
| R-03 | الجدولة الجماعية للامتحانات ≤ 200 امتحان/دفعة | متوسط | لو تجاوزت: تقسيم الدفعة على مستوى RPC (batching) |
| R-04 | القاعة مورد مؤسسي — لا مشاركة قاعات بين مؤسستين | عالي | لو تغير: قرار منتج جديد يكسر عزل المؤسسات — خارج النطاق كلياً |
| R-05 | كشف تداخل طلاب الامتحانات يعتمد على `enrollments` بحالة ACTIVE | عالي | مبني على المرحلة 1 المنجزة |

### 0.4 — نطاق العمل (Scope Fence)

- ✅ **داخل النطاق:** دورة حياة طلب الحجز (اقتراح → مراجعة → نشر/رفض) · الجدولة الإدارية الجماعية للامتحانات · كشف تعارض القاعات آلياً (قاعة × زمن) · كشف تصادم امتحانات الطالب الواحد · عرض توفر القاعات · عقود RPC · إشعارات الموافقة/الرفض · جدول `exam_schedule_batches`
- ❌ **خارج النطاق:** إنشاء الواجبات/البحوث (نشر مباشر — لا مورد فيزيائي، Scheduling TDD) · الأحداث الثقافية (نشر مباشر من الإداري — قرار #19، Scheduling TDD) · إدارة بيانات القاعات CRUD البسيطة (شاشات إدارية مباشرة عبر PostgREST + RLS) · تحسين توزيع القاعات آلياً (optimization solver) — القرار البشري أولاً (MVP §1.1) · جلسات الحضور داخل القاعة (Attendance TDD)

---

## القسم 1: الملخص التنفيذي وسياق المشكلة 📋

### 1.1 — بيان المشكلة

كل حدث فيزيائي في المنصة يحتاج قاعة وزمناً، والقرار #9 يفرض مراجعة إدارية لكل اقتراح محاضر، والقرار #10 يحصر جدولة الامتحانات بالإداري كلياً. بدون تطبيق هذين القرارين على مستوى قاعدة البيانات (لا الواجهة)، سيتمكن عميل معدَّل من نشر حدث دون موافقة أو جدولة امتحان من حساب محاضر. تكلفة عدم الحل: فوضى تشغيلية يومية (تعارضات قاعات) وخرق صريح لقرارات معتمدة.

### 1.2 — الهدف القابل للقياس

تمكين المحاضر من تقديم طلب حجز والإداري من مراجعته ونشره خلال معاملة ذرية واحدة، مع **صفر تعارض قاعة/زمن يمكن أن يصل حالة approved** (مضمون بقيد GiST على مستوى المحرك لا بفحص تطبيقي)، وجدولة فصل امتحانات كامل (حتى 200 امتحان) في أقل من 10 ثوانٍ مع تقرير تعارضات سطر-بسطر.

### 1.3 — معايير النجاح (Success Criteria)

| معيار | القيمة المستهدفة | كيف يُقاس |
|---|---|---|
| تعارض قاعة/زمن في حالة approved | 0 حالة — مستحيل بنيوياً | قيد `no_room_overlap` + اختبار إدراج متعمد |
| زمن مراجعة طلب (قبول + نشر) p95 | < 500ms | pg_stat_statements |
| جدولة جماعية لـ 200 امتحان | < 10s مع تقرير كامل | اختبار تكامل |
| محاضر يجدول امتحاناً (بأي مسار) | 0 حالة — مرفوض بـ CHECK + RLS + RPC | pgTAP: دور × عملية |
| تسريب طلب/جدول عبر مؤسسة | 0 حالة | pgTAP: دور × عملية × مؤسسة |
| إشعار المحاضر بنتيجة طلبه | ≤ 5s من القرار | trigger + قياس Realtime |

### 1.4 — ما ليس هذا النظام (Anti-Goals)

- ليس محرك تحسين (solver) لتوزيع القاعات — الإداري يقرر، النظام يكشف التعارض فقط
- ليس نظام حجز ذاتي — لا يحجز المحاضر قاعة مباشرة أبداً، كل شيء عبر المراجعة
- لا موافقة تلقائية تحت أي شرط ("قاعة فارغة" ليست مبرراً لتجاوز القرار البشري — MVP §1.1)
- لا إعادة جدولة تلقائية عند الإلغاء — الإلغاء يحرر المورد فقط، وإعادة الاستخدام قرار جديد

---

## القسم 2: القيود التقنية والقرارات 🔧

### 2.1 — القيود المفروضة (Hard Constraints)

| نوع القيد | التفصيل | مصدره |
|---|---|---|
| تنظيمي | المحاضرة/المعمل/السمنار: اقتراح محاضر + موافقة إدارية إلزامية | قرار #9 |
| تنظيمي | الامتحان: جدولة إدارية حصرية — المحاضر لا يقترحه بأي صيغة | قرار #10 |
| تقني | كشف التعارض على مستوى المحرك (EXCLUDE USING gist) لا التطبيق | Core Data Layer §4.2 |
| تقني | كل الجداول tenant-scoped + فهارس تبدأ بـ `institution_id` + RLS من JWT claim | قرار #20 / D-05 / D-08 |
| تنظيمي | كل قرار مراجعة يُوثَّق ولا يُحذف (سبب الرفض إلزامي) | MVP §5.0 |
| تقني | فور القبول يُنشر الحدث ويظهر في الجداول — عملية ذرية واحدة | MVP §5.1.2 |

### 2.2 — القرارات التصميمية

#### [RM-01] النشر عند القبول: صف `events` يُنشأ لحظة الموافقة — لا صف pending مسبق

| الخيار | المزايا | العيوب | لماذا قُبل/رُفض |
|---|---|---|---|
| **إنشاء `events` عند الموافقة فقط ✅** | جدول `events` يحتوي حصراً ما هو حقيقي · قيد GiST يعمل على approved فقط بلا ضجيج · الطلب المرفوض لا يترك أثراً في الجدولة | الطلب والحدث كيانان منفصلان (مقبول — `room_requests` هو سجل التدقيق) | أبسط وأنظف؛ الطالب لا يرى إلا الحقيقي |
| إنشاء event بحالة pending ثم قلبه approved ❌ | كيان واحد | جدول الأحداث يمتلئ بمسودات؛ تعقيد فلترة في كل استعلام طالب | يلوّث أثقل استعلام في النظام (جدول الطالب) |

#### [RM-02] فحص تعارض الجدولة الجماعية: داخل معاملة واحدة مع تقرير سطر-بسطر

| الخيار | المزايا | العيوب | لماذا قُبل/رُفض |
|---|---|---|---|
| **معاملة واحدة، فشل الصف لا يُفشل الدفعة، تقرير كامل ✅** | نفس نمط `import_enrollments_csv` المجرَّب · الإداري يصحح الصفوف الفاشلة ويعيد | يتطلب savepoint لكل صف | اتساق مع نمط معتمد في المنصة (Core Data Layer §5.2) |
| كل-شيء-أو-لا-شيء ❌ | ذThere was an error while generating response. Please try again later.