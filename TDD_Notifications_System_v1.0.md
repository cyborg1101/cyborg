# 🔔 وثيقة التصميم التقني — نظام الإشعارات والإعلانات
## Academic Hub — Notifications & Announcements System TDD

---

## بيانات ضبط الوثيقة

| البند | التفاصيل |
|---|---|
| **اسم الوثيقة** | وثيقة التصميم التقني — نظام الإشعارات والإعلانات |
| **رقم الإصدار** | v1.0 |
| **مستوى العمق** | 🟡 Production |
| **الوثائق المرجعية** | MVP Functions v3.5 (الأقسام 3.2 · 4.2 · 5.2) · Attendance TDD · Scheduling & Events TDD v1.1 |
| **الحالة** | مسودة للمراجعة |
| **مالك الوثيقة** | رامز |

---

# القسم 0: بروتوكول التحليل المتسلسل 🧠

## 0.1 — إعادة صياغة المشكلة

المطلوب تصميم نظام إشعارات وإعلانات موحّد يخدم ثلاثة أدوار بصلاحيات متمايزة: **الطالب** مستقبِل قراءة فقط (إعلانات البورد الأسود، تغييرات الجدول، تحديثات الملفات، دردشات مواده، تنبيهات الدفعات لقناتيها، وتذكير استباقي قبل 30 دقيقة من الأحداث الحضورية)، **المحاضر** مستقبِل للإعلانات المؤسسية والتنبيهات النظامية (نتائج طلبات القاعات، تعليقات الطلاب، رسائل الإدارة)، و**الإداري** هو الناشر الوحيد (البورد الأسود، إشعارات موجّهة لفئات محددة، تصنيف عاجل/مهم/عادي، تثبيت، جدولة نشر لاحق) إضافة إلى استقباله إشعارات الدردشات.

نقطة الغموض الوحيدة المكتشفة: **من يملك منطق «التذكير قبل 30 دقيقة»؟** — الحدث نفسه ملك نظام الجدولة، لكن التسليم ملك هذا النظام. القرار: نظام الجدولة **مصدر حدث (Event Producer)** وهذا النظام **قناة تسليم حصرية** — لا يولّد أي نظام آخر إشعاراته بنفسه (موثّق في [D-06]).

## 0.2 — الأسئلة الخمسة الحاسمة

1. **من المستخدم الفعلي؟** ~5,000 طالب + ~300 محاضر + ~30 إدارياً (افتراض A-01). ذروة الاستخدام: نشر جدول الامتحانات أو إعلان عاجل → **fan-out واحد إلى ~5,330 جهازاً خلال ثوانٍ**، تليها موجة قراءة (فتح التطبيق) خلال 10 دقائق.
2. **قراءة أم كتابة أثقل؟** قراءة بشدة — نسبة تقريبية **100:1**. الكتابة الوحيدة الثقيلة هي fan-out الإشعارات الفردية، وقد حُيّدت بنموذج هجين ([D-03]) يجعل الإعلان العام صفاً واحداً مهما كان عدد المستقبلين.
3. **ماذا لو توقف النظام ساعة؟** لا خسارة بيانات (المصادر تكتب في Postgres والإشعارات تُبنى منها) — الأثر: تأخر تسليم فقط. الاستثناء الحرج: تذكيرات ما قبل الحدث وإشعارات تغيير قاعة الامتحان — تأخرها ساعة يعني فوات الغرض كلياً. لذلك availability المستهدف للتسليم: **99.5% شهرياً**، مع اعتبار مسار «تغيير حدث خلال < ساعتين من موعده» مساراً حرجاً له مراقبة مستقلة (القسم 6.5).
4. **البيانات الأكثر حساسية؟** الإشعارات الموجّهة للفئات: **قائمة المتأخرين مالياً وقائمة متكرري الغياب**. تسريب انتماء طالب لإحداها = كشف معلومات مالية/سلوكية شخصية. الدفاع: الفئة تُحسب لحظة الإرسال ولا تُخزَّن كوسم على المستخدم، ونص الإشعار الواصل للطالب لا يكشف اسم الفئة لغيره (RLS صارم — القسم 6.3).
5. **أضيق عنق زجاجة خلال 6 أشهر؟** جدول `notifications` الفردي: بمعدل ~20 إشعاراً فردياً/طالب/أسبوع → ~2.6 مليون صف/فصل دراسي. المعالجة: فهرسة على `(user_id, created_at)` + سياسة أرشفة بعد 180 يوماً (القسم 4.4) — لا حاجة لـ partitioning قبل تجاوز ~20 مليون صف.

## 0.3 — الافتراضات المعلنة

| # | الافتراض | مستوى الثقة | تأثيره لو كان خاطئاً |
|---|---|---|---|
| A-01 | حجم المؤسسة ~5,000 طالب، ~300 محاضر، ~30 إدارياً | متوسط | لو 10x: يجب تفعيل partitioning مبكراً وتقسيم مواضيع FCM |
| A-02 | مستوى العمق 🟡 Production (لم يحدده الطلب صراحة) | عالٍ | لو Prototype: يمكن إسقاط الجدولة المؤجلة والمقاييس من النطاق الأول |
| A-03 | المنصة: Flutter + Supabase (Postgres/RLS/Realtime) — استناداً لذكر RLS وHive في وثيقة MVP | عالٍ | لو stack مختلف: تُستبدل الأقسام 2 و3 مع بقاء نموذج البيانات صالحاً |
| A-04 | تطبيق موبايل حصراً (Android أولوية) — لا نسخة ويب في الـ MVP | متوسط | لو وُجد ويب: تُضاف قناة Web Push وتتعقد إدارة التوكنات |
| A-05 | اتصال الإنترنت لدى الطلاب متقطع (سياق محلي) | عالٍ | يعزز قرار الاعتماد على المزامنة عند الفتح لا على الدفع الفوري وحده |
| A-06 | «تحديد الفئات» (متأخرون مالياً / متكررو غياب) يعتمد استعلامات على جداول أنظمة الدفعات والحضور الموجودة مسبقاً | عالٍ | لو غير جاهزة: تُبنى الفئات يدوياً (قائمة user_ids يلصقها الإداري) كحل مؤقت |

## 0.4 — نطاق العمل (Scope Fence)

- ✅ **داخل النطاق:**
  - إعلانات البورد الأسود: نشر / تصنيف (🔴🟡🟢) / تثبيت / جدولة نشر لاحق / استهداف (الكل، طلاب، محاضرون، فئة ديناميكية)
  - الإشعارات الفردية النظامية الواردة من الأنظمة الأخرى (جدولة، ملفات، دردشة، دفعات، حضور/التماسات)
  - تذكير استباقي قبل 30 دقيقة من الأحداث الحضورية (التسليم فقط — الأحداث ملك نظام الجدولة)
  - التسليم عبر 3 قنوات: Push (FCM) + داخل التطبيق (Realtime) + مزامنة عند الفتح (Hive)
  - تصفية الطالب: الكل / غير مقروء / مثبّت / حسب المادة — وتصفية المحاضر: الكل / غير مقروء
  - مركز إشعارات موحّد لكل دور، مع حالة قراءة لكل عنصر
- ❌ **خارج النطاق:**
  - محتوى الدردشة نفسه (نظام مستقل — نستقبل منه أحداثاً فقط)
  - البريد الإلكتروني والرسائل النصية كقنوات تسليم
  - تفضيلات إشعارات لكل مستخدم (كتم قنوات/مواد) — دين تقني مقبول (القسم 2.3)
  - القراءة الآلية لإشعارات التحويل البنكي (خارج نطاق الإصدار وفق MVP 5.5)
  - تحليلات فتح الإعلانات (open-rate dashboards)

---

# القسم 1: الملخص التنفيذي وسياق المشكلة 📋

## 1.1 — بيان المشكلة

تعتمد الجامعات حالياً على مجموعات واتساب ولوحات ورقية لإيصال المعلومات، فتضيع الإعلانات الحرجة (تغيير قاعة امتحان، استحقاق مالي) وسط الضجيج، ولا يمكن إثبات وصولها ولا استهداف فئة بعينها. كل نظام في Academic Hub (جدولة، دفعات، حضور، ملفات، دردشة) يحتاج إيصال أحداثه للمستخدم المناسب في وقتها — وبناء قناة تسليم داخل كل نظام على حدة يعني 5 تطبيقات متكررة وغير متسقة لنفس المنطق. تكلفة عدم الحل: طالب يفوته امتحان بسبب تغيير قاعة لم يصله = فشل مباشر في القيمة الجوهرية للمنصة.

## 1.2 — الهدف القابل للقياس

تمكين أي نظام في المنصة من إيصال حدث إلى المستخدم المستهدف خلال **≤ 10 ثوانٍ (p95)** من وقوعه للأجهزة المتصلة، وضمان ظهوره في مركز الإشعارات خلال **≤ 3 ثوانٍ من أول فتح للتطبيق** للأجهزة غير المتصلة — مع قدرة الإداري على بث إعلان لـ 5,330 مستخدماً بعملية كتابة واحدة.

## 1.3 — معايير النجاح

| معيار | القيمة المستهدفة | كيف يُقاس |
|---|---|---|
| زمن التسليم (حدث → إشعار على جهاز متصل) p95 | < 10 ثوانٍ | طابع زمني في payload يقارن عند الاستلام (عينة أجهزة اختبار) |
| نسبة نجاح تسليم Push | ≥ 97% من التوكنات الفعّالة | تقارير FCM + سجل delivery_log |
| زمن تحميل مركز الإشعارات p95 | < 400ms لآخر 50 عنصراً | قياس زمن الاستعلام من جانب العميل |
| بث إعلان عام | صف واحد في DB مهما كان عدد المستقبلين | مراجعة الكود / خطة الاستعلام |
| فقدان الإشعارات عند انقطاع الشبكة | 0% (المزامنة عند الفتح تلتقط كل شيء) | اختبار تكاملي: قطع الشبكة → أحداث → إعادة اتصال |
| دقة تنفيذ الإعلان المجدول | ± 60 ثانية من الموعد | مقارنة `scheduled_at` بـ `published_at` |

## 1.4 — ما ليس هذا النظام (Anti-Goals)

- **ليس نظام مراسلة:** لا يُنشئ محادثات ولا ردوداً — الدردشة نظام مستقل يرسل إلينا أحداثاً فقط
- **ليس ضامن تسليم Push بنسبة 100%:** FCM لا يضمن ذلك لأي أحد (أجهزة مطفأة، توفير بطارية) — الضمانة الحقيقية هي المزامنة عند الفتح، وPush مجرد تسريع
- **ليس محرك قرارات:** لا يقرر «من هو متكرر الغياب» — يستعلم تعريفاً تملكه أنظمة الحضور/الدفعات
- **ليس أرشيفاً دائماً:** الإشعارات الفردية تُؤرشف بعد 180 يوماً؛ الإعلانات فقط تُحفظ دائماً (soft delete)

---

# القسم 2: القيود التقنية واختيار التقنيات 🔧

## 2.1 — القيود المفروضة (Hard Constraints)

| نوع القيد | التفصيل | مصدره |
|---|---|---|
| تقني | المنصة Flutter + Supabase (Postgres, RLS, Realtime, Edge Functions) | A-03 — متسق مع بقية أنظمة المنصة |
| تقني | العمل دون اتصال بعد أول مزامنة (Hive) | MVP 3.1.2 |
| فريق | فريق صغير (1–3 مطورين) — لا طاقة لتشغيل بنية رسائل مستقلة (Kafka/RabbitMQ) | سياق المشروع |
| ميزانية | خطة Supabase Pro (~25$/شهر) + FCM مجاني | سياق المشروع |
| تنظيمي | قرارات الإداري تُوثَّق ولا تُحذف (MVP 5.0) → soft delete إلزامي للإعلانات | MVP v3.5 |
| وظيفي | الإداري هو الناشر الوحيد؛ الطالب قراءة فقط؛ المحاضر قراءة فقط ضمن هذا النظام | Roles Matrix — MVP القسم 7 |

## 2.2 — مصفوفة اختيار التقنيات

### [D-01] البنية الأساسية للنظام

| الخيار | المزايا | العيوب | لماذا قُبل/رُفض |
|---|---|---|---|
| **Supabase: Postgres + Realtime + Edge Functions ✅** | RLS أصلي يطبّق فصل الأدوار بلا كود إضافي؛ Realtime يغطي التسليم داخل التطبيق مجاناً؛ نفس بنية بقية الأنظمة (تكامل عبر triggers مباشرة) | Realtime محدود بـ ~500 اتصال متزامن على الخطة الحالية (يكفي — ذروة الاتصال المتزامن المتوقعة ~300)؛ Edge Functions لها cold start ~200-400ms | يقبل: صفر بنية إضافية، وتكامل الأنظمة الأخرى يصبح trigger واحداً |
| Firebase (Firestore + Functions) ❌ | تكامل FCM أسهل | يقسم البيانات بين قاعدتين (Postgres للمنصة + Firestore للإشعارات) → فقدان الـ triggers والـ joins والـ RLS الموحد | رُفض: كسر وحدة مصدر الحقيقة |
| خدمة Node.js مستقلة + Redis Pub/Sub ❌ | تحكم كامل، أداء أعلى نظرياً | مكوّنان إضافيان للتشغيل والمراقبة؛ مبالغة لحمل 5,330 مستخدماً | رُفض: تعقيد تشغيلي لا يبرره الحجم |

### [D-02] قناة الدفع (Push Delivery)

| الخيار | المزايا | العيوب | لماذا قُبل/رُفض |
|---|---|---|---|
| **FCM (توكنات فردية + Topics) ✅** | مجاني بلا حدود عملية؛ Topics تحل بث الإعلانات العامة برسالة API واحدة بدل 5,330 | لا ضمان تسليم (best-effort)؛ تأخير محتمل على أجهزة Android الموفّرة للطاقة؛ إدارة دورة حياة التوكنات علينا | يقبل: المعيار الفعلي لـ Android، والمزامنة عند الفتح تعوّض نقص الضمان |
| OneSignal ❌ | لوحة إدارة جاهزة، تقسيم جمهور مدمج | طرف ثالث إضافي يرى بيانات المستخدمين (حساسية الفئات المالية)؛ الخطة المجانية محدودة | رُفض: تسريب PII لطرف ثالث بلا مقابل جوهري |
| Self-hosted (ntfy / Gotify) ❌ | سيادة كاملة على البيانات | يتطلب اتصال WebSocket دائماً من التطبيق → استهلاك بطارية؛ لا يعمل موثوقاً مع قيود Android في الخلفية | رُفض: غير عملي على الموبايل |

### [D-03] نموذج التوزيع (Fan-out Model) — القرار الأهم في الوثيقة

| الخيار | المزايا | العيوب | لماذا قُبل/رُفض |
|---|---|---|---|
| **هجين: fan-out-on-read للإعلانات + fan-out-on-write للإشعارات الفردية ✅** | الإعلان العام = صف واحد + جدول حالة قراءة يُملأ عند التفاعل فقط؛ الإشعار الفردي = صف لكل مستقبل (وهو أصلاً فردي بطبيعته)؛ استعلام «غير المقروء» يبقى بسيطاً في الحالتين | منطقان مختلفان في العميل (دمج مصدرين في قائمة واحدة)؛ حساب عدّاد غير المقروء يتطلب استعلامين (يُدمجان في view) | يقبل: يجمع كفاءة الكتابة مع بساطة القراءة |
| fan-out-on-write للجميع ❌ | نموذج واحد بسيط، استعلام قراءة واحد | إعلان عام واحد = 5,330 صفاً؛ 20 إعلاناً/أسبوع = ~1.4 مليون صف زائد/فصل بلا قيمة | رُفض: تضخم كتابي بلا مبرر |
| fan-out-on-read للجميع ❌ | صفر تضخم | الإشعار الفردي (نتيجة التماسك، دفعتك رُفضت) يفقد معناه كصف مشترك؛ الاستهداف الديناميكي يتطلب إعادة حساب الفئة عند كل قراءة → نتائج غير مستقرة زمنياً | رُفض: الفئات يجب أن تتجمد لحظة الإرسال (قرار أمني — 0.2 س4) |

### [D-04] جدولة الإعلانات المؤجلة

| الخيار | المزايا | العيوب | لماذا قُبل/رُفض |
|---|---|---|---|
| **pg_cron (داخل Postgres) كل 60 ثانية ✅** | مدمج في Supabase؛ لا مكوّن خارجي؛ transactional مع بيانات الإعلان (لا نشر مزدوج) | دقة ± 60 ثانية (مقبولة — معيار النجاح 1.3)؛ يتطلب أن تكون دالة النشر idempotent | يقبل: أبسط حل يحقق الدقة المطلوبة |
| Edge Function مع مجدول خارجي ❌ | مرونة أكبر في المنطق | مكوّن إضافي + مشكلة تزامن لو تنفّذ مرتين بلا قفل | رُفض: نفس النتيجة بتعقيد أعلى |
| جدولة من جهاز الإداري (client-side) ❌ | صفر بنية خلفية | تفشل لو أُغلق التطبيق — غير مقبول لإعلان مؤسسي | رُفض: موثوقية معدومة |

### [D-05] التخزين المحلي والعمل دون اتصال

**Hive** (مفروض من MVP — لا مقارنة): صندوقان `announcements_box` و`notifications_box`، مزامنة تفاضلية عند الفتح عبر `?since=last_sync_at`، وحالة القراءة تُخزن محلياً وتُزامَن كدفعة عند عودة الاتصال (القسم 6.1 — idempotent).

## 2.3 — الديون التقنية المقبولة عمداً

| الدين | لماذا الآن | متى يُسدد |
|---|---|---|
| لا تفضيلات إشعارات للمستخدم (كتم مادة/قناة) | يقلل نطاق الـ MVP؛ حجم الإشعارات الحالي محتمل | عند تجاوز متوسط 10 إشعارات/يوم/طالب أو أول موجة شكاوى |
| الفئات الديناميكية تُنفَّذ كاستعلامين ثابتين (متأخرون مالياً، متكررو غياب) لا كمحرك segments عام | فئتان فقط مطلوبتان في MVP 5.2 | عند طلب فئة ثالثة |
| لا retry queue مستقل لفشل FCM — نعتمد المزامنة عند الفتح كشبكة أمان | يوفر بناء طابور رسائل كامل | إذا هبطت نسبة تسليم Push دون 90% |
| أرشفة الإشعارات = حذف بعد 180 يوماً (لا أرشيف بارد) | الإشعار الفردي بيانات عابرة؛ الإعلانات محفوظة دائماً | عند ظهور متطلب تدقيق يتجاوز 180 يوماً |

---

# القسم 3: معمارية النظام والمخططات 🏗️

## 3.1 — المخطط العام (System Context)

\`\`\`mermaid
graph TB
    subgraph Producers["🏭 الأنظمة المصدرة للأحداث"]
        SCH[نظام الجدولة والأحداث]
        PAY[نظام الدفعات]
        ATT[نظام الحضور والالتماسات]
        FIL[مستودع الملفات]
        CHT[نظام الدردشة]
        HAL[إدارة القاعات]
    end

    subgraph Core["🔔 نواة نظام الإشعارات — Supabase"]
        TRG[DB Triggers<br/>التقاط الأحداث]
        NTB[(notifications<br/>إشعارات فردية)]
        ANB[(announcements<br/>إعلانات البورد)]
        CRON[pg_cron<br/>مجدول: نشر مؤجل + تذكير 30 دق + أرشفة]
        EF[Edge Function: dispatch<br/>بناء وإرسال Push]
        RT[Supabase Realtime]
    end

    ADMIN[👔 الإداري<br/>الناشر الوحيد] -->|نشر / جدولة / تثبيت / استهداف| ANB
    SCH & PAY & ATT & FIL & CHT & HAL --> TRG
    TRG --> NTB
    NTB --> EF
    ANB -->|عند النشر| EF
    CRON --> ANB
    CRON -->|تذكير الأحداث| NTB
    EF --> FCM[Firebase Cloud Messaging]
    NTB & ANB --> RT

    FCM --> APP[📱 تطبيق Flutter<br/>Hive Cache]
    RT --> APP
    APP -->|مزامنة عند الفتح ?since=| NTB
    APP -->|مزامنة عند الفتح| ANB
\`\`\`

## 3.2 — جدول المكونات والمسؤوليات

| المكون | مسؤوليته الوحيدة | ماذا لو سقط؟ | استراتيجية التعافي |
|---|---|---|---|
| DB Triggers | تحويل حدث نظام مصدر إلى صف إشعار موحّد | لا تُنشأ إشعارات جديدة (البيانات الأصلية سليمة) | جزء من Postgres — سقوطه = سقوط DB كاملة (انظر صف DB) |
| جدولا notifications/announcements | مصدر الحقيقة الوحيد لكل ما يُعرض | توقف كامل للنظام | نسخ Supabase الاحتياطي اليومي + PITR |
| Edge Function `dispatch` | قراءة الصفوف غير المرسلة → إرسال FCM → تحديث حالة الإرسال | يتوقف Push فقط — التطبيق يلتقط كل شيء عند الفتح (شبكة الأمان) | stateless — إعادة تشغيل تلقائية؛ عمود `push_sent_at` يمنع الإرسال المزدوج |
| pg_cron | نشر المجدول + توليد تذكيرات الـ 30 دقيقة + الأرشفة | تتأخر الإعلانات المجدولة والتذكيرات | مهام idempotent — التشغيل التالي يلتقط المتراكم |
| FCM | إيصال Push للأجهزة | تأخر تنبيه فقط — لا فقدان بيانات | خارج سيطرتنا؛ الاعتماد الجوهري على المزامنة عند الفتح |
| Realtime | تحديث حي لمركز الإشعارات المفتوح | يتراجع العميل إلى polling كل 60 ثانية | fallback مدمج في العميل |
| Hive Cache | عرض دون اتصال + تقليل الاستعلامات | إعادة مزامنة كاملة عند فتح التطبيق (مقبول) | الكاش قابل للتفريغ دائماً — DB هي الحقيقة |

## 3.3 — تدفق البيانات للعمليات الحرجة

### المسار 1: الإداري ينشر إعلاناً عاجلاً على البورد الأسود

\`\`\`mermaid
sequenceDiagram
    participant AD as 👔 الإداري
    participant DB as Postgres
    participant EF as Edge Fn: dispatch
    participant F as FCM
    participant ST as 📱 أجهزة المستخدمين

    AD->>DB: INSERT announcement (priority=urgent, target=all, status=published)
    Note over DB: RLS: role=admin فقط<br/>⚠️ نقطة فشل: رفض RLS → 403
    DB->>EF: Webhook (database trigger → invoke)
    Note over EF: ⚠️ نقطة فشل: cold start أو timeout<br/>→ صف الإعلان يحمل push_sent_at=NULL<br/>→ مهمة cron sweeper تلتقطه خلال 60 ثا
    EF->>F: send to topic "all_users" (رسالة API واحدة)
    F-->>ST: push خلال 1–5 ثوانٍ (best-effort)
    EF->>DB: UPDATE push_sent_at = now()
    DB-->>ST: Realtime broadcast (للتطبيقات المفتوحة حالياً)
    ST->>DB: عند الفتح لاحقاً: GET announcements?since=last_sync
    Note over ST: الطبقة الثالثة تضمن الوصول 100%<br/>حتى لو فشل Push وRealtime معاً
\`\`\`

### المسار 2: نظام مصدر يولّد إشعاراً فردياً (مثال: رفض دفعة)

\`\`\`mermaid
sequenceDiagram
    participant PAY as نظام الدفعات
    participant TR as Trigger
    participant DB as notifications
    participant EF as dispatch
    participant F as FCM
    participant ST as 📱 جهاز الطالب

    PAY->>TR: UPDATE payment SET status='rejected'
    TR->>DB: INSERT notification (user_id, type='payment_rejected', ref_id)
    Note over TR,DB: داخل نفس الـ transaction<br/>= استحالة ضياع الإشعار إذا نجحت العملية الأصلية
    DB->>EF: trigger invoke
    EF->>F: send to device tokens الخاصة بالطالب
    F-->>ST: push
    Note over F,ST: ⚠️ فشل token (منتهٍ/أُلغي)<br/>→ يُعلَّم is_valid=false ويُحذف بعد 30 يوماً
\`\`\`

### المسار 3: تذكير الـ 30 دقيقة قبل حدث حضوري

\`\`\`mermaid
sequenceDiagram
    participant CR as pg_cron (كل 5 دقائق)
    participant SCH as جداول نظام الجدولة (قراءة فقط)
    participant DB as notifications
    participant EF as dispatch

    CR->>SCH: SELECT الأحداث الحضورية التي تبدأ خلال [30, 35] دقيقة<br/>ولم يُذكَّر بها بعد
    SCH-->>CR: قائمة (event_id × الطلاب المسجلون في المادة)
    CR->>DB: INSERT ... ON CONFLICT (user_id, type, ref_id) DO NOTHING
    Note over CR,DB: ⚠️ التشغيل المزدوج آمن — القيد الفريد يمنع التكرار
    DB->>EF: dispatch → FCM
\`\`\`

## 3.4 — قرارات معمارية جوهرية

### [D-06] المركزية: قناة تسليم واحدة أم كل نظام يشعّر بنفسه؟

| الخيار | المزايا | العيوب | لماذا قُبل/رُفض |
|---|---|---|---|
| **قناة مركزية واحدة — الأنظمة تنتج أحداثاً فقط ✅** | نقطة واحدة لإدارة التوكنات والقنوات والمراقبة؛ إضافة نوع إشعار جديد = trigger + سطر في قاموس الأنواع | الـ triggers توزّع منطق الالتقاط على جداول الأنظمة الأخرى (يُدار بتسمية موحدة `notify_on_*` وتوثيق مركزي في القسم 4.5) | يقبل: يمنع 6 تطبيقات متكررة لنفس المنطق |
| كل نظام يرسل FCM مباشرة ❌ | استقلالية الأنظمة | تكرار إدارة التوكنات 6 مرات؛ لا مركز إشعارات موحّداً ممكناً | رُفض: كسر تجربة المستخدم الموحدة |
| Message broker وسيط ❌ | فصل نظيف نظرياً | بنية إضافية كاملة لحمل لا يتجاوز آلاف الرسائل يومياً | رُفض: over-engineering صريح |

### [D-07] الإرسال: متزامن أم غير متزامن؟

**غير متزامن بالكامل** — كتابة الصف في DB هي «نجاح» العملية من منظور المصدر؛ الإرسال Push مسؤولية `dispatch` اللاحقة. **المقايضة المقبولة:** تأخر يصل إلى 60 ثانية في أسوأ حالة (فشل الـ trigger invoke والتقاط الـ sweeper) مقابل أن فشل FCM لا يفشل أبداً العملية الأصلية (قبول دفعة، تغيير جدول).

---

# القسم 4: نماذج البيانات وتصميم قاعدة البيانات 🗄️

## 4.1 — مخطط الكيانات (ERD)

\`\`\`mermaid
erDiagram
    USERS ||--o{ NOTIFICATIONS : "يستقبل"
    USERS ||--o{ DEVICE_TOKENS : "يمتلك"
    USERS ||--o{ ANNOUNCEMENT_STATES : "حالة قراءته"
    USERS ||--o{ ANNOUNCEMENTS : "ينشر (إداري فقط)"
    ANNOUNCEMENTS ||--o{ ANNOUNCEMENT_STATES : "لها"
    ANNOUNCEMENTS ||--o{ ANNOUNCEMENT_RECIPIENTS : "استهداف فئوي مجمّد"

    ANNOUNCEMENTS {
        uuid id PK
        uuid author_id FK
        text title
        text body
        text priority "urgent|important|normal"
        text target_type "all|students|lecturers|segment"
        text segment_key "late_payers|frequent_absentees|null"
        boolean is_pinned
        timestamptz scheduled_at
        timestamptz published_at
        text status "draft|scheduled|published|retracted"
        timestamptz push_sent_at
        timestamptz deleted_at "soft delete"
    }

    NOTIFICATIONS {
        bigint id PK
        uuid user_id FK
        text type "قاموس الأنواع 4.5"
        text ref_type
        uuid ref_id
        uuid course_id FK "للتصفية حسب المادة — nullable"
        text title
        text body
        timestamptz created_at
        timestamptz read_at
        timestamptz push_sent_at
    }

    ANNOUNCEMENT_STATES {
        uuid announcement_id PK,FK
        uuid user_id PK,FK
        timestamptz read_at
    }

    ANNOUNCEMENT_RECIPIENTS {
        uuid announcement_id PK,FK
        uuid user_id PK,FK
    }

    DEVICE_TOKENS {
        uuid id PK
        uuid user_id FK
        text fcm_token UK
        text platform
        boolean is_valid
        timestamptz last_seen_at
    }
\`\`\`

## 4.2 — تعريف الجداول (Schema)

\`\`\`sql
-- announcements: إعلانات البورد الأسود — ينشرها الإداري حصراً، صف واحد مهما كان عدد المستقبلين [D-03]
CREATE TABLE announcements (
    id             UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    author_id      UUID NOT NULL REFERENCES users(id),          -- الإداري الناشر — للتوثيق الإلزامي (MVP 5.0)
    title          TEXT NOT NULL CHECK (char_length(title) BETWEEN 3 AND 150),
    body           TEXT NOT NULL CHECK (char_length(body) <= 5000),
    priority       TEXT NOT NULL DEFAULT 'normal'
                   CHECK (priority IN ('urgent','important','normal')),  -- 🔴🟡🟢 (MVP 5.2)
    target_type    TEXT NOT NULL DEFAULT 'all'
                   CHECK (target_type IN ('all','students','lecturers','segment')),
    segment_key    TEXT CHECK (segment_key IN ('late_payers','frequent_absentees')),
                   -- non-null فقط عند target_type='segment' — الفئتان الوحيدتان في MVP (دين تقني 2.3)
    is_pinned      BOOLEAN NOT NULL DEFAULT false,              -- التثبيت في الصدارة
    scheduled_at   TIMESTAMPTZ,                                 -- non-null = نشر مؤجل [D-04]
    published_at   TIMESTAMPTZ,                                 -- يُملأ لحظة النشر الفعلي — مصدر ترتيب العرض
    status         TEXT NOT NULL DEFAULT 'draft'
                   CHECK (status IN ('draft','scheduled','published','retracted')),
    push_sent_at   TIMESTAMPTZ,                                 -- NULL = لم يُرسل Push بعد → يلتقطه الـ sweeper
    created_at     TIMESTAMPTZ NOT NULL DEFAULT now(),
    deleted_at     TIMESTAMPTZ,                                 -- soft delete حصراً — قرارات الإداري لا تُحذف
    CONSTRAINT segment_consistency
        CHECK ((target_type = 'segment') = (segment_key IS NOT NULL)),
    CONSTRAINT scheduled_needs_time
        CHECK (status != 'scheduled' OR scheduled_at IS NOT NULL)
);

-- announcement_recipients: تجميد الاستهداف الفئوي لحظة النشر (قرار أمني — القسم 0.2 س4)
-- تُملأ فقط عند target_type='segment' — الإعلانات العامة لا تلمسها إطلاقاً
CREATE TABLE announcement_recipients (
    announcement_id UUID NOT NULL REFERENCES announcements(id),
    user_id         UUID NOT NULL REFERENCES users(id),
    PRIMARY KEY (announcement_id, user_id)
);

-- announcement_states: حالة قراءة المستخدم للإعلان — fan-out-on-read: صف عند أول تفاعل فقط
CREATE TABLE announcement_states (
    announcement_id UUID NOT NULL REFERENCES announcements(id),
    user_id         UUID NOT NULL REFERENCES users(id),
    read_at         TIMESTAMPTZ NOT NULL DEFAULT now(),
    PRIMARY KEY (announcement_id, user_id)                      -- يجعل تكرار وسم القراءة idempotent
);

-- notifications: الإشعارات الفردية النظامية — fan-out-on-write، BIGINT لأنه الجدول الأسرع نمواً (0.2 س5)
CREATE TABLE notifications (
    id            BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    user_id       UUID NOT NULL REFERENCES users(id),
    type          TEXT NOT NULL,                                -- من قاموس الأنواع (4.5)
    ref_type      TEXT NOT NULL,                                -- الكيان المصدر: event/payment/file/post/excuse...
    ref_id        UUID NOT NULL,                                -- للتنقل العميق (deep-link) عند النقر
    course_id     UUID REFERENCES courses(id),                  -- nullable — يمكّن تصفية الطالب «حسب المادة»
    title         TEXT NOT NULL,
    body          TEXT NOT NULL CHECK (char_length(body) <= 500),
    created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
    read_at       TIMESTAMPTZ,                                  -- NULL = غير مقروء
    push_sent_at  TIMESTAMPTZ,
    -- منع التكرار المطلق لنفس الإشعار (تذكير مزدوج، trigger أعيد تنفيذه):
    CONSTRAINT uq_notification_dedup UNIQUE (user_id, type, ref_id)
);

-- device_tokens: دورة حياة توكنات FCM
CREATE TABLE device_tokens (
    id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id      UUID NOT NULL REFERENCES users(id),
    fcm_token    TEXT NOT NULL UNIQUE,                          -- التوكن قد ينتقل بين مستخدمين على نفس الجهاز → UNIQUE عالمي مع UPSERT يعيد ربطه
    platform     TEXT NOT NULL DEFAULT 'android' CHECK (platform IN ('android','ios')),
    is_valid     BOOLEAN NOT NULL DEFAULT true,                 -- false عند رفض FCM → حذف بعد 30 يوماً
    last_seen_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
\`\`\`

## 4.3 — استراتيجية الفهرسة

| الفهرس | الاستعلام الذي يخدمه | تكلفته على الكتابة |
|---|---|---|
| `notifications (user_id, created_at DESC)` | مركز الإشعارات: آخر 50 عنصراً للمستخدم — الاستعلام الأكثر تكراراً في النظام كله | ~10% إبطاء على INSERT — مقبول لأن القراءة 100:1 |
| `notifications (user_id) WHERE read_at IS NULL` (partial) | عدّاد غير المقروء (badge) — يُستدعى عند كل فتح | ضئيلة — الفهرس الجزئي يغطي غير المقروء فقط وينكمش ذاتياً مع القراءة |
| `notifications (user_id, course_id)` | تصفية الطالب «حسب المادة» | ~8% على INSERT — مقبول |
| `announcements (published_at DESC) WHERE status='published' AND deleted_at IS NULL` (partial) | قائمة البورد الأسود | شبه معدومة — كتابة الإعلانات نادرة (~20/أسبوع) |
| `announcements (scheduled_at) WHERE status='scheduled'` (partial) | مسح pg_cron كل 60 ثانية — يبقى فهرساً شبه فارغ دائماً | معدومة عملياً |
| `device_tokens (user_id) WHERE is_valid` (partial) | جمع توكنات مستخدم عند dispatch | ضئيلة |
| `announcement_recipients (user_id)` | RLS: «هل هذا الإعلان الفئوي موجّه لي؟» | ضئيلة — كتابة دفعة واحدة عند النشر |

## 4.4 — الأسئلة الإلزامية

- **الترحيلات (Migrations):** عبر Supabase migrations المتسلسلة؛ التغييرات إضافية فقط (add column nullable → backfill → قيود) — لا `ALTER` مانع للكتابة على `notifications` بعد الإطلاق، ولا حاجة لـ downtime لأن كل التغييرات المتوقعة إضافية.
- **الحذف:** الإعلانات **soft delete حصراً** (`deleted_at` + status=`retracted`) — التزام مباشر بمبدأ «يُوثَّق ولا يُحذف». الإشعارات الفردية **hard delete بالأرشفة** بعد 180 يوماً (مهمة cron أسبوعية تحذف على دفعات 10,000 صف لتجنب قفل الجدول) — بيانات عابرة لا قيمة تدقيقية لها، والحدث الأصلي محفوظ في نظام المصدر.
- **التوسع:** عند تجاوز `notifications` حاجز **~20 مليون صف** (متوقع بعد ~4 سنوات بالحجم الحالي، أو سنة واحدة لو صح خطأ A-01)، يُفعَّل range partitioning على `created_at` شهرياً — المفتاح طبيعي لأن الأرشفة والقراءة كلاهما زمنيان.
- **التوافقية:** الكتابة الأصلية (حدث المصدر + صف الإشعار) **strong consistency** — نفس الـ transaction عبر trigger. التسليم (Push/Realtime) **eventual** بسقف 60 ثانية ([D-07]). عدّاد غير المقروء على الجهاز eventual (قد يتأخر ثوانٍ) — مقبول صراحة.

## 4.5 — قاموس أنواع الإشعارات (Event Type Registry)

المرجع الموحّد لكل الأنظمة المصدرة — إضافة نوع جديد تتطلب صفاً هنا + trigger في نظام المصدر:

| type | المصدر | المستقبل | ref_type | مثال النص |
|---|---|---|---|---|
| `event_reminder` | pg_cron ← جداول الجدولة | طالب | event | «محاضرة تشريح تبدأ خلال 30 دقيقة — قاعة 3» |
| `event_changed` / `event_cancelled` | نظام الجدولة | طلاب المادة | event | «تغيّرت قاعة امتحان الفسيولوجي إلى المدرج الكبير» |
| `file_added` | مستودع الملفات | طلاب المادة | file | «ملف جديد في مادة الباطنة» |
| `contribution_reviewed` | مستودع الملفات | طالب | contribution | «اجتهادك: مقبول ✅» |
| `chat_post` / `chat_comment` / `chat_reply` | نظام الدردشة | طلاب المادة / محاضر / إداري | post | «منشور جديد من د. أحمد في قناة الجراحة» |
| `payment_confirmed` / `payment_rejected` / `payment_due` | نظام الدفعات | طالب | payment | «تم تأكيد دفعة رسوم الجامعة» |
| `excuse_result` | نظام الحضور | طالب | excuse | «التماس عذر الغياب: مقبول — سُجّل حضورك» |
| `hall_request_result` | إدارة القاعات | محاضر | hall_request | «طلب قاعة المحاضرة: مقبول — قاعة 5، الأحد 10ص» |
| `final_result_published` | التقارير والنتائج | طلاب المادة | course_result | «نتيجتك النهائية في مادة X متاحة الآن» |
| `support_message` | الدعم الفني | طالب / إداري | ticket | «رد جديد على استفسارك» |

---

# القسم 5: عقود الـ API والواجهات 🔌

## 5.1 — مبادئ التصميم

**[D-08]** الوصول عبر **PostgREST المدمج في Supabase** (قراءة الجداول والـ views تحت RLS) + **Edge Functions للعمليات المركّبة** (نشر إعلان فئوي، dispatch). البدائل المرفوضة: REST API مخصص كامل (❌ إعادة بناء ما يقدمه PostgREST مجاناً مع فقدان تكامل RLS التلقائي)، GraphQL (❌ طبقة تعلم وتشغيل إضافية لأنماط استعلام بسيطة ومعروفة سلفاً). النسخ: مسار `/functions/v1/` — كسر التوافق مستقبلاً = دالة جديدة `v2`. صيغة الخطأ الموحدة: `{"error": {"code": "...", "message": "..."}}`.

## 5.2 — توثيق الـ Endpoints

### `POST /functions/v1/announcements`
**الغرض:** نشر أو جدولة إعلان (يشمل تجميد الفئة عند الاستهداف الفئوي) | **المصادقة:** JWT + role=admin | **Rate Limit:** 30/دقيقة/إداري

**المدخلات:**
\`\`\`json
{
  "title": "string — مطلوب، 3-150 حرفاً",
  "body": "string — مطلوب، حتى 5000 حرف",
  "priority": "urgent | important | normal — افتراضي normal",
  "target_type": "all | students | lecturers | segment",
  "segment_key": "late_payers | frequent_absentees — مطلوب فقط عند segment",
  "is_pinned": "boolean — افتراضي false",
  "scheduled_at": "ISO-8601 UTC — اختياري؛ وجوده = جدولة، غيابه = نشر فوري",
  "idempotency_key": "uuid — مطلوب (القسم 6.1)"
}
\`\`\`

**المخرجات الناجحة (201):**
\`\`\`json
{
  "id": "uuid",
  "status": "published | scheduled",
  "recipients_count": 4210,
  "published_at": "ISO-8601 | null"
}
\`\`\`

**حالات الخطأ:**
| كود | متى تحدث | استجابة العميل المتوقعة |
|---|---|---|
| 400 | حقل ناقص/تجاوز حدود/`segment_key` بلا `target_type=segment` | عرض رسالة التحقق على الحقل |
| 401 | توكن منتهٍ | إعادة توجيه لتسجيل الدخول |
| 403 | الدور ليس admin | شاشة «غير مصرّح» — لا يظهر الزر أصلاً لكن الدفاع في العمق |
| 409 | `idempotency_key` مكرر | اعتبار العملية ناجحة وعرض الإعلان الموجود |
| 422 | `scheduled_at` في الماضي (بتوقيت الخادم) | «اختر وقتاً مستقبلياً» |
| 429 | تجاوز rate limit | retry مع exponential backoff |
| 500 | خطأ داخلي | «حدث خطأ — أعد المحاولة»؛ الإعادة آمنة بفضل idempotency_key |

### `GET /rest/v1/rpc/my_feed`
**الغرض:** القائمة الموحدة (إعلانات + إشعارات) مع التصفية — يغطي MVP 3.2 و4.2 | **المصادقة:** JWT (أي دور) | **Rate Limit:** 120/دقيقة/مستخدم

**المدخلات (query):** `filter=all|unread|pinned` · `course_id=uuid` (طلاب فقط) · `since=ISO-8601` (المزامنة التفاضلية) · `limit≤50` · `cursor`

**المخرجات الناجحة (200):**
\`\`\`json
{
  "items": [
    {
      "kind": "announcement | notification",
      "id": "uuid|bigint",
      "title": "...", "body": "...",
      "priority": "urgent|important|normal|null",
      "is_pinned": false,
      "course_id": "uuid|null",
      "ref_type": "payment", "ref_id": "uuid",
      "created_at": "ISO-8601",
      "is_read": true
    }
  ],
  "next_cursor": "opaque-string|null",
  "unread_count": 7
}
\`\`\`

**حالات الخطأ:** 400 (فلتر غير معروف / limit>50) → تجاهل وإعادة الافتراضي · 401 → تسجيل دخول · 429 → إبطاء polling · 500 → عرض كاش Hive مع شارة «غير محدّث».

> ملاحظة RLS مدمجة: الدالة تُرجع تلقائياً — الإعلانات المطابقة لدور المستخدم أو الموجّهة له في `announcement_recipients`، وإشعاراته الفردية فقط. لا معامل `user_id` في العقد إطلاقاً (منع IDOR).

### `POST /rest/v1/rpc/mark_read`
**الغرض:** وسم عناصر كمقروءة (دفعة — يخدم المزامنة بعد الأوفلاين) | **المصادقة:** JWT | **Rate Limit:** 60/دقيقة

**المدخلات:** `{"announcement_ids": ["uuid"], "notification_ids": [123]}` — حتى 200 عنصر/طلب
**المخرجات (200):** `{"updated": 14}`
**حالات الخطأ:** 400 (تجاوز 200 عنصر) → تقسيم الدفعة · 401 · 429 · 500 → إعادة المحاولة آمنة (idempotent — UPSERT/`read_at` لا يتغير إن سبق ضبطه). عناصر لا تخص المستخدم تُتجاهل صامتة (لا 403 — منع استكشاف المعرّفات).

### `POST /rest/v1/device_tokens` (UPSERT)
**الغرض:** تسجيل/تحديث توكن FCM عند كل إقلاع للتطبيق | **المصادقة:** JWT | **Rate Limit:** 10/دقيقة
**المدخلات:** `{"fcm_token": "string", "platform": "android|ios"}`
**المخرجات (201/200):** `{"id": "uuid"}` — التوكن الموجود لمستخدم آخر يُعاد ربطه بالمستخدم الحالي (جهاز مشترك).
**حالات الخطأ:** 400 (توكن فارغ) · 401 · 429 · 500 → إعادة عند الإقلاع التالي.

### `POST /functions/v1/announcements/{id}/retract` و `PATCH .../pin`
**الغرض:** سحب إعلان (soft) / تبديل التثبيت | **المصادقة:** admin | **Rate Limit:** 30/دقيقة
**الأخطاء المميزة:** 404 (غير موجود أو محذوف) · 409 عند retract لإعلان `retracted` أصلاً → عرض الحالة الحالية.

## 5.3 — العقود بين الخدمات الداخلية

| العقد | الصيغة | الضمانة |
|---|---|---|
| نظام مصدر → notifications | Trigger داخل نفس transaction العملية الأصلية | **exactly-once للكتابة** (ذرّية transaction + قيد `uq_notification_dedup`) |
| notifications/announcements → dispatch | Database webhook (invoke) + cron sweeper كل 60 ثا لصفوف `push_sent_at IS NULL` | **at-least-once** — الإرسال المزدوج النادر مقبول (إشعاران متطابقان على الجهاز أهون من صفر) |
| dispatch → FCM | HTTP v1 API؛ topics للبث العام (`all_users`, `role_students`, `role_lecturers`)، توكنات فردية للباقي | best-effort — شبكة الأمان: المزامنة عند الفتح |
| اشتراك Topics | العميل يشترك عند تسجيل الدخول حسب دوره؛ إلغاء الاشتراك عند الخروج | idempotent من طرف FCM |

---

# القسم 6: حالات الحافة، أنماط الفشل، والأمان 🛡️

## 6.1 — جرد حالات الحافة

| السيناريو | ماذا يحدث في التصميم؟ | المعالجة |
|---|---|---|
| طلبان متزامنان يعدلان نفس المورد (إداريان يثبّتان/يسحبان نفس الإعلان) | آخر كتابة تكسب على `is_pinned` (غير ضار)؛ `retract` محمي | `UPDATE ... WHERE status='published'` — الطلب الثاني يصطدم بـ 409 |
| المستخدم يرسل مدخلات 10x المتوقع (إعلان 50k حرف / دفعة mark_read بـ 5000 معرف) | قيود CHECK على مستوى DB + حد 200 عنصر في العقد | رفض 400 قبل أي معالجة — التحقق في DB لا في العميل فقط |
| انقطاع الشبكة في منتصف نشر إعلان | الإداري لا يعرف هل نجح | `idempotency_key` إلزامي — إعادة الإرسال تُرجع 409 مع الإعلان الموجود، لا نشر مزدوج |
| انقطاع الشبكة أثناء mark_read | حالة القراءة محلية فقط في Hive | طابور محلي يُعاد إرساله عند الاتصال — العملية idempotent بالكامل |
| خدمة خارجية بطيئة وليست ساقطة (FCM يستجيب في 30 ثا) | dispatch قد يتجاوز timeout الدالة | timeout صريح 10 ثوانٍ/دفعة FCM + إرسال على دفعات 500 توكن؛ الفاشل يبقى `push_sent_at=NULL` ويلتقطه الـ sweeper |
| رسالة مكررة (trigger أعيد تنفيذه / cron مزدوج) | خطر إشعار مكرر للمستخدم | `uq_notification_dedup (user_id, type, ref_id)` + `ON CONFLICT DO NOTHING` — التكرار مستحيل على مستوى DB |
| التوقيت (timezones / DST / انحراف ساعة الجهاز) | `scheduled_at` قد يُفسَّر بتوقيت الجهاز | كل التخزين والمقارنات **UTC بتوقيت الخادم حصراً**؛ العرض فقط بتوقيت الجهاز؛ رفض 422 لأي جدولة ماضية بساعة الخادم (لا اليمن DST لكن المبدأ ثابت) |
| مستخدم عُطّل حسابه وله توكنات فعّالة | قد يستمر وصول Push | dispatch يستبعد بـ join على `users.status='active'`؛ تعطيل الحساب يُبطل توكناته فوراً (trigger) |
| إعلان مجدول والفئة تغيّرت قبل موعد النشر (طالب سدّد متأخراته) | من يستقبل: فئة وقت الجدولة أم وقت النشر؟ | **قرار صريح:** الفئة تُجمَّد **لحظة النشر الفعلي** لا لحظة الجدولة — `announcement_recipients` تُملأ داخل مهمة النشر |
| جهاز مشترك بين طالبين (توكن FCM واحد) | إشعارات المستخدم السابق تصل للحالي | UPSERT على `fcm_token` يعيد ربط التوكن بآخر من سجّل دخولاً + إبطاله عند تسجيل الخروج |
| طالب محجوب لتأخر الاشتراك (MVP 3.7.2) | هل تصله الإشعارات؟ | **نعم** — الإشعارات ضمن المستثنى وظيفياً (كيف يعرف أن دفعته قُبلت أو أن امتحانه اقترب وهو محجوب؟)؛ الحجب يطبَّق على الميزات الأخرى في طبقة التطبيق |

## 6.2 — تحليل أنماط الفشل

| المكون | نمط الفشل | الاحتمالية | الأثر | الكشف | التعافي |
|---|---|---|---|---|---|
| Postgres | تعطل كامل | منخفضة | 🔴 حرج — النظام كله | Supabase health + تنبيه | إدارة Supabase + PITR؛ RTO ~15 دق |
| Edge Fn dispatch | فشل/timeout متكرر | متوسطة | 🟡 تأخر Push ≤ 60 ثا | مقياس `push_lag` (6.5) | sweeper cron يلتقط `push_sent_at IS NULL` |
| pg_cron | توقف صامت | منخفضة | 🟡 تأخر مجدولات وتذكيرات | heartbeat: مهمة تكتب طابعاً كل دقيقة + تنبيه عند غياب 5 دق | إعادة تفعيل؛ المهام idempotent فتلتقط المتراكم |
| FCM | انقطاع إقليمي | منخفضة | 🟢 لا Push مؤقتاً — البيانات سليمة | هبوط معدل النجاح في delivery metrics | لا فعل — المزامنة عند الفتح تغطي |
| Realtime | انقطاع القناة | متوسطة | 🟢 المركز المفتوح لا يتحدث حياً | كشف انقطاع القناة في العميل | fallback تلقائي إلى polling كل 60 ثا |
| device_tokens | تراكم توكنات ميتة | عالية (طبيعي) | 🟢 هدر إرسال + تشويش مقاييس | ردود FCM `UNREGISTERED` | وسم `is_valid=false` فوراً + حذف بعد 30 يوماً |
| Triggers | خطأ في trigger يفشل العملية الأصلية | منخفضة | 🔴 رفض دفعة يفشل لأن الإشعار فشل! | اختبارات تكاملية لكل trigger | كل trigger يلف الإدراج بـ `BEGIN...EXCEPTION WHEN OTHERS THEN` مع تسجيل الخطأ — **فشل الإشعار لا يُفشل العملية الأصلية أبداً** |

## 6.3 — نموذج التهديدات (STRIDE)

| التهديد | مثال ملموس على هذا النظام | الدفاع المحدد |
|---|---|---|
| Spoofing | طالب ينتحل دور إداري لنشر إعلان مزيف على البورد الأسود | JWT عبر Google Sign-In + فحص role=admin في RLS **وفي** Edge Function (دفاع مزدوج) |
| Tampering | تعديل إعلان منشور لتغيير موعد امتحان | RLS: UPDATE على announcements للإداري فقط؛ السحب soft يبقي الأصل موثقاً بمؤلفه وتوقيته |
| Repudiation | إداري ينكر نشر إعلان مسيء | `author_id` + `created_at` غير قابلين للتعديل (لا UPDATE policy عليهما) + soft delete حصراً |
| Information Disclosure | طالب يستعلم `announcement_recipients` ليعرف زملاءه المتأخرين مالياً — **أخطر تهديد في النظام** | RLS على الجدول: `user_id = auth.uid()` حصراً (يرى صفه فقط)؛ نص الإشعار الفئوي لا يذكر اسم الفئة؛ `recipients_count` يظهر للإداري فقط |
| Denial of Service | سيل طلبات mark_read أو تسجيل توكنات وهمية | Rate limits لكل endpoint (5.2) + حد 200 عنصر/دفعة + حد 10 أجهزة/مستخدم |
| Elevation of Privilege | محاضر يستدعي دالة النشر مباشرة متجاوزاً الواجهة | التحقق من الدور داخل الدالة نفسها من JWT — لا ثقة بأي شيء يرسله العميل |

## 6.4 — قائمة التدقيق الأمنية

- [x] **المصادقة والتفويض:** Supabase Auth (Google) + RLS على كل الجداول الخمسة؛ التحقق من الدور في DB لا في العميل
- [x] **التشفير:** in-transit عبر TLS 1.2+ (Supabase/FCM افتراضياً)؛ at-rest عبر AES-256 على بنية Supabase
- [x] **الأسرار:** مفتاح خدمة FCM في Supabase Secrets (بيئة Edge Functions) — لا يلمسه العميل ولا المستودع أبداً
- [x] **الحقن:** parameterized queries حصراً في الدوال؛ PostgREST محصّن بنيوياً؛ لا SQL ديناميكي في أي trigger
- [x] **سجلات التدقيق:** يُسجَّل: نشر/سحب/تثبيت الإعلانات (المؤلف والتوقيت في الجدول ذاته). **لا يُسجَّل:** محتوى FCM payload في اللوجات (يحمل عناوين إشعارات مالية = PII)، ولا قوائم أعضاء الفئات خارج `announcement_recipients` المحمي

## 6.5 — الملاحظة والمراقبة (Observability)

| المقياس | التعريف | عتبة التنبيه |
|---|---|---|
| `push_lag_p95` | الفارق بين `created_at` و`push_sent_at` | > 90 ثانية لمدة 5 دقائق → تنبيه 🔴 (يعني تعطل dispatch والـ sweeper معاً) |
| `push_success_rate` | نجاح FCM ÷ توكنات is_valid المستهدفة (نافذة ساعة) | < 90% → تحقيق 🟡؛ < 75% → 🔴 |
| `pending_dispatch_count` | صفوف `push_sent_at IS NULL` أقدم من دقيقتين | > 100 → 🔴 |
| `cron_heartbeat_gap` | آخر نبضة pg_cron | > 5 دقائق → 🔴 (يجمّد المجدولات والتذكيرات) |
| `feed_query_p95` | زمن استعلام `my_feed` | > 400ms لمدة 10 دقائق → 🟡 (مؤشر مبكر لمشكلة فهرسة/نمو) |

---

# القسم 7: خطة التنفيذ وخارطة الطريق 🗺️

## 7.1 — ترتيب المخاطر (Risk-First Ordering)

**أخطر افتراض تقني:** أن سلسلة «trigger → Edge Function → FCM Topic» توصل إعلاناً لآلاف الأجهزة خلال ≤ 10 ثوانٍ p95، وأن Push يصل فعلاً على أجهزة Android منخفضة التكلفة مع أوضاع توفير الطاقة العدوانية (شائعة في سياق المستخدمين المستهدفين). إن فشل هذا، ينقلب التصميم من «push أولاً» إلى «polling أولاً» — وهذا يغيّر معايير النجاح جذرياً. لذلك هو موضوع المرحلة 0.

## 7.2 — المراحل

### المرحلة 0: إثبات الجدوى (Spike) — 3 أيام
- **الهدف:** التحقق من زمن ونسبة تسليم المسار الكامل trigger → dispatch → FCM Topic
- **المخرج القابل للاختبار:** سكربت يُدرج إعلاناً ويقيس وصوله إلى 5 أجهزة Android حقيقية (بينها جهاز اقتصادي بوضع توفير طاقة) + 1,000 توكن محاكى
- **معيار النجاح/الفشل:** p95 ≤ 10 ثوانٍ ونسبة وصول ≥ 90% على الأجهزة الحقيقية خلال دقيقة. **إن فشل:** الخطة البديلة — إبقاء Push «أفضل جهد» وتقليص وعد التسليم إلى «عند فتح التطبيق» مع polling خلفي كل 15 دقيقة (WorkManager)، وتحديث معايير القسم 1.3 رسمياً

### المرحلة 1: النواة — Schema + RLS + مركز الإشعارات (قراءة) — أسبوع
- **المهام:** (1) migrations للجداول الخمسة والفهارس (2) سياسات RLS الثلاث للأدوار + اختباراتها (3) دالة `my_feed` مع الفلاتر والـ cursor (4) شاشة مركز الإشعارات في Flutter مع Hive (5) `mark_read` + الطابور المحلي الأوفلاين
- **الاعتماديات:** نظام المصادقة (المرحلة 1 من خارطة MVP)
- **المخرج القابل للاختبار:** صفوف تُدرج يدوياً في DB تظهر في التطبيق، تُقرأ، وتُفلتر — أونلاين وأوفلاين
- **اختبارات المرحلة:** unit لدالة الفيد (فلاتر × أدوار)؛ **اختبارات RLS العدائية** (طالب يحاول قراءة إشعارات غيره وجدول recipients) — تغطية 100% للسياسات

### المرحلة 2: النشر الإداري — أسبوع
- **المهام:** (1) دالة النشر مع idempotency (2) واجهة الإداري: إنشاء/تصنيف/تثبيت/سحب (3) الجدولة المؤجلة عبر pg_cron + الـ sweeper (4) تجميد الفئتين الديناميكيتين لحظة النشر (بالتنسيق مع جاهزية استعلامات الدفعات والحضور — A-06)
- **الاعتماديات:** المرحلة 1
- **المخرج القابل للاختبار:** إداري ينشر إعلاناً عاجلاً مثبّتاً مجدولاً بعد 5 دقائق → يظهر لكل المستخدمين في موعده ± 60 ثا
- **اختبارات المرحلة:** integration للجدولة (بما فيها إعادة تشغيل cron المزدوجة) + idempotency النشر + تجميد الفئات

### المرحلة 3: قنوات التسليم الحية — Push + Realtime — أسبوع
- **المهام:** (1) device_tokens بدورة حياتها الكاملة (تسجيل/UPSERT/إبطال عند الخروج) (2) دالة dispatch بدفعات 500 + timeout (3) اشتراكات Topics حسب الدور (4) Realtime مع fallback إلى polling (5) deep-linking من نقرة الإشعار إلى الكيان المصدر عبر `ref_type/ref_id`
- **الاعتماديات:** المرحلتان 0 و2
- **المخرج القابل للاختبار:** إعلان يُنشر → push يصل خلال ≤ 10 ثا للأجهزة المتصلة، والنقر يفتح الشاشة الصحيحة
- **اختبارات المرحلة:** e2e على أجهزة حقيقية + اختبار توكن ميت (يُبطَل تلقائياً) + اختبار جهاز مشترك

### المرحلة 4: ربط الأنظمة المصدرة — أسبوع (تتوزع مع تسليم كل نظام مصدر)
- **المهام:** trigger لكل نوع في قاموس 4.5 مع نمط `EXCEPTION` الحامي للعملية الأصلية + مهمة تذكير الـ 30 دقيقة + مهمة الأرشفة (180 يوماً / حذف توكنات ميتة 30 يوماً)
- **الاعتماديات:** المرحلة 3 + جاهزية جداول كل نظام مصدر
- **المخرج القابل للاختبار:** رفض دفعة تجريبية → إشعار يصل؛ حدث جدولة بعد 30 دق → تذكير يصل مرة واحدة فقط
- **اختبارات المرحلة:** integration لكل trigger + اختبار «فشل الإشعار لا يُفشل المصدر» + اختبار dedup القيد الفريد

### المرحلة 5: التقسية والمراقبة — 3 أيام
- **المهام:** المقاييس الخمسة (6.5) بعتباتها + rate limits + مراجعة قائمة 6.4 + اختبار حمل: بث لـ 5,000 توكن محاكى وقياس p95
- **المخرج القابل للاختبار:** لوحة مقاييس حية + تقرير اختبار الحمل مطابقاً لمعايير 1.3

## 7.3 — مخطط الاعتماديات

\`\`\`mermaid
graph LR
    P0[مرحلة 0: Spike تسليم FCM<br/>3 أيام] --> P3
    P1[مرحلة 1: النواة + RLS + المركز<br/>أسبوع] --> P2[مرحلة 2: النشر الإداري<br/>أسبوع]
    P2 --> P3[مرحلة 3: Push + Realtime<br/>أسبوع]
    P3 --> P4[مرحلة 4: ربط الأنظمة المصدرة<br/>أسبوع موزّع]
    P3 --> P5[مرحلة 5: التقسية والمراقبة<br/>3 أيام]
    P4 --> P5
\`\`\`

> المرحلتان 0 و1 تعملان بالتوازي (مسار الأولى تحقق تقني، مسار الثانية بناء) — إجمالي المسار الحرج: **~4.5 أسابيع**.

## 7.4 — تعريف «الانتهاء» (Definition of Done)

- [ ] كل الاختبارات تمر — تغطية ≥ 85% للمسارات الحرجة (RLS، dispatch، dedup، الجدولة)
- [ ] اختبارات RLS العدائية تمر بنسبة 100% (لا وصول متقاطع بين الأدوار إطلاقاً)
- [ ] توثيق الـ API (القسم 5) مطابق للتنفيذ الفعلي
- [ ] المقاييس الخمسة تعمل والتنبيهات مضبوطة على عتبات 6.5
- [ ] قائمة الأمان 6.4 مراجعة بالكامل مع توقيع المراجع
- [ ] اختبار الحمل: بث 5,000 مستقبل يحقق معايير 1.3
- [ ] خطة rollback: migrations قابلة للعكس + إمكانية تعطيل dispatch بمتغير بيئة (kill switch) دون إيقاف بقية المنصة

---

*— نهاية الوثيقة — Notifications & Announcements TDD v1.0 —*